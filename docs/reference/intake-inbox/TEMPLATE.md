---
# Copy this file to YYYY-MM-DD-slug.md and fill it in. Only `url` is required.
# This is a raw drop, not a Learnings-Log entry — do NOT add a `stance`; scout assigns that.
# Schema + lifecycle: see README.md in this directory.
url:                        # the item to ingest — a real URL, required
source:                     # who published it (person, org, or channel) — recommended
medium: post                # post | video | podcast | blog | paper | docs | other
dropped:                    # YYYY-MM-DD you dropped it — optional
---
Why this caught your eye, or what in this repo to weigh it against (a rule, a skill, an ADR).
One or two sentences — a hint for scout, not a writeup. Delete this line if you have nothing to add.
