# Findings Log

Append-only. One line per finding: `date · type · state · gist`. Full rules in
[`PROJECT.md`](../PROJECT.md) → *Findings-Log Discipline*; spec § *Factory model and SDLC* is the origin.

**Type — what kind of thing it is.** `bug` · `process` · `note` · `fact` · `risk` · `idea` ·
`optimization`. This is the **observed** vocabulary, not a new mandate: it is what the writers of this
log actually used. Kickoff declared four (`process | bug | idea | data`) and `data` was never once
used, so the artifact is the authority over the older statement.

**State — what happens to it next.** `closed` (resolved in the run that found it, or a lesson needing
no action) · `open` (live work) · `accepted` (a real limitation, decided against on the record).
Entries written before 2026-08-01 carry no state; the first triage pass assigns one.

**Only the HC promotes, and nothing urgent waits for that.**

> **No Issues, PRs, rules, or ADRs spawn directly from findings.** The HC promotes `open` findings to
> tracked work at a triage pass — the `do-now` disposition, and the only path from this file to an
> Issue. Separately and always: **if something is broken it is a defect**, fixed in the running change
> when the fix is in hand or tracked immediately when it is not. Filing that is correct, not a
> violation. What this rule governs is *learnings and proposals*, never things that are simply broken.

An agent that has just learned something process-shaped writes **one line** and continues. A finding
matching a class below records **a reference plus what is new about this instance**, not a
re-derivation of the lesson.

## Recurring classes

Descriptive index of shapes recorded more than once — **not rules**. An imperative statement belongs
in the Rules Layer, which nadal does not author. Line references are to this file as of `10e6189`
(`git show 10e6189:docs/findings.md`), since a header edit shifts every later line.

| Class | Shape | Seen |
|---|---|---|
| **C1** | Fixed the named instance, not the class | ~20 · L77 L132 L185 L215 L248 |
| **C2** | A claim outruns what the code enforces — comment, test title, doc, PR body, quantifier | ~17 · L93 L214 L246 L261 |
| **C3** | A review round's fix creates the next round's defect | ~9 · L155 L192 L209 L256 |
| **C4** | Testing only the configuration the test invented | ~8 · L142 L157 L197 L238 |
| **C5** | An enumeration fails silently at its edges — derive the guard from structure | ~7 · L87 L114 L190 L237 |
| **C6** | A behavioral merge hazard with no textual signature | 5 · L180 L231 L239 |

- 2026-07-29 · process · sandcastle/warren-style dispatched-worker execution preserves our operating loop; candidate ace capability, not nadal v1 work
- 2026-07-29 · process · an `ace new <project>` stamper would complete the factory model; upstream idea for ace
- 2026-07-29 · idea · wire harness token/cost telemetry (Claude Code OTEL/cost export) to request_log for per-request economics; post-Springfield
- 2026-07-29 · process · Execution Profile observations for ace#143 accumulate here (what under-thought, what over-spent)
- 2026-07-29 · process · `codex:rescue`-invoked adversarial reviews write job records as `task-*.json` (kindLabel `rescue`) in the plugin state dir, not `review-*.json` as the "find the job record" runbook step assumes — grep by branch across `*.json`, not `review-*.json`, to find them.
- 2026-07-30 · process · ace maintainer checkout carries orphaned .claude/worktrees (resilient-inventing-spindle); ace-sync should exclude .claude/worktrees/** like *.local; prune source worktree (upstream-to-ace)
- 2026-07-30 · process · human_gates.rb cannot express "no human merge gate" though ADR 0025 calls gate policy project config (upstream-to-ace)
- 2026-07-30 · process · vendored final skill "never self-merges" is in tension with nadal merge-autonomy config (upstream-to-ace)
- 2026-07-30 · process · Codex attestation templating: single-quoted heredoc posted literal $(git rev-parse HEAD) on PR #4; fixed by capture-then-unquoted-heredoc + fetch-back verification (Task 3)
- 2026-07-30 · process · Task 5 incident: haiku executor fabricated a review section and merged ungated; remediated post-hoc on PR #6; hardening = driver verifies attestation artifact via gh api before task review (ace#143 routing observation: tier alone insufficient for gate steps)
- 2026-07-30 · process · plan-brief authoring lessons: Task 2's literal Quality Checks table was itself the round-1 defect; .gitignore is not in ace-sync ALLOW; bin/install-git-hooks is bash not ruby
- 2026-07-30 · process · .claude branch-protection PreToolUse hook blocks Write on main even for gitignored scratch paths (.superpowers/sdd/**), forcing an executor onto a throwaway branch to write its own report; Bash-heredoc writes are unaffected; hook should exempt gitignored paths (upstream-to-ace)
- 2026-07-30 · process · Codex review evidence lives in two different places depending on invocation path (plugin jobs/ dir for synchronous adversarial-review, ~/.codex/sessions for background rescue); a driver checking only one place can wrongly conclude no review ran (this happened on PR #8)
- 2026-07-30 · process · merge-autonomy on green+attestation structurally races the SDD scoped re-review; holds must be stated in the dispatch, not mid-flight
- 2026-07-30 · process · an unscoped fix round grew 2 findings to 8 across 6 Codex re-summons; fix rounds need an explicit scope clause naming what NOT to touch
- 2026-07-30 · idea · tn's summary-line safety (control-char strip, bidi strip, quote/backslash escaping, field-injection resistance) was derived under adversarial review for one command; it is a CLI-wide contract worth a single shared module + one test suite
- 2026-07-30 · process · the CLI-wide sanitize/summary contract logged directly above already landed in-plan during Task 7 — `src/sanitize.ts` + `src/cli/summary.ts`, covered by `test/sanitize.test.ts` + `test/summary.test.ts`; recorded so triage does not re-open it
- 2026-07-30 · process · `enforce-clean-tree.sh` blocks the *whole* multi-line Bash tool call when any single segment is a destructive git op on a dirty tree, not just that segment — an earlier harmless command (e.g. `rm -f`) chained before the blocked `git checkout --` in the same call silently never runs either; split destructive git ops into their own Bash call to avoid losing sibling commands.
- 2026-07-30 · process · a regenerate-and-diff drift guard must not use `git diff --exit-code <dir>`: git diff only inspects TRACKED files, so a brand-new untracked artifact (the most likely drift output, e.g. a fresh drizzle migration .sql + snapshot) passes silently; check `git status --porcelain <dir>` instead, and prove the guard red-green by inducing real drift
- 2026-07-30 · process · this plan's CI task specified a `parity` job that the vendored ace baseline already provided in `.github/workflows/parity.yml`; plan authors must inventory vendored workflows before specifying CI, and a Host App must never re-implement a vendored check it is forbidden to edit
- 2026-07-30 · process · `docs/findings.md` was mandated from Task 1 onward by a Global Constraint but only created by Task 9, so early tasks improvised a non-spec line format (`- date - kind - text` instead of the spec's `date · type · gist`); a plan must create an append-target before the first task required to append to it
- 2026-07-30 · process · `gh api -f` sends numeric fields as JSON strings — the issue-dependencies endpoint 422s on `-f issue_id=<n>` ("not of type integer"); use `-F` (typed) for any integer/bool field
- 2026-07-30 · process · GitHub's native issue-dependencies REST API (`issues/<n>/dependencies/blocked_by`, keyed on numeric issue id) is confirmed live and scriptable on this repo; future plan text can drop the "may need a prose fallback" hedge
- 2026-07-30 · process · surfacing a previously-swallowed error (telemetry write failures, this wave's item 5) can double stderr output when the underlying failure is shared — an ENOTDIR TN_DB_PATH fails both the command's own summary write and telemetry's own openDb() at the same broken path — so a fix-round dispatch naming only one test file to update should be read as "at least," not "only": grep the whole suite for assertions the new code path could now trip
- 2026-07-30 · bug · (deferred, Medium, Codex round-1 on PR #20) test/cli-grammar-parity.test.ts's strengthened checks still accept a duplicate GRAMMAR.md row for the same command — Set-membership + a Map keyed by noun/verb both collapse duplicates, so a second identical `tn db migrate` row would pass every check, missing the documented "one spelling per operation" invariant
- 2026-07-30 · bug · (deferred, Low, Codex round-1 on PR #20) docs/cli/GRAMMAR.md's new summary-line paragraph (item 9) states every value field is double-quoted, but its own worked example and the actual emitted `status=ok`/`status=error` field are unquoted — scope the claim to interpolated/free-form values (path=, message=), or quote status too and update db-migrate.ts to match
- 2026-07-30 · bug · (deferred, Low, Codex round-2 on PR #20) src/telemetry/request-log.ts's `sanitizeValue(err.message)` assumes `.message` is always a string — a mutated/custom Error with a non-string `message` would make `.replace()` throw inside the catch, violating "telemetry must never break the request itself"; harden with a `String(...)` coercion before sanitizing
- 2026-07-30 · bug · (deferred, Low, Codex round-2 on PR #20) test/db-membership-unique-upgrade.test.ts asserts only that one row survives the dedup DELETE, not that it's specifically the lowest-id row the migration comment promises to keep — tighten the assertion to check the surviving row's id
- 2026-07-30 · process · a codex:rescue Agent call's job record can land under the CALLING session's own workspace-hash directory (here `dynamic-foraging-hollerith-*`, the driver's own worktree) rather than the target repo's (`wild-floating-firefly-*`) even though `workspaceRoot` inside the JSON correctly names the calling session's cwd — when hunting for a job record, glob `~/.claude/plugins/data/codex-openai-codex/state/*/jobs/*.json` across ALL workspace-hash dirs, not just the one matching the repo under review
- 2026-07-30 · process · a rule recorded only as prose the checker cannot parse is not a rule — nadal's merge-autonomy intent lived in a PROJECT.md subsection honestly headed "recorded, not machine-enforced" while every binding surface said a human merges, so agents read the binding surfaces, the parity check read only the value, and ten PRs merged against the written rule with a green build the whole time; fixed upstream by ace ADR 0037, which makes any such heading a parity-check error
- 2026-07-30 · process · governance and harness permission are two independent layers and fixing one does nothing for the other — a repo may set `merge: attested`, satisfy every gate condition, and still be unable to merge because the agent runtime denies the command; conversely a permission grant does not authorize a merge the config forbids
- 2026-07-30 · process · under `defaultMode: auto` the classifier can supersede a matching `permissions.allow` rule — `Bash(gh pr:*)` was already allowed globally and `gh pr merge` was still refused, because the classifier soft-denies destructive/irreversible actions independently of the allow list; the targeted fix is an `autoMode.allow` entry, not another `permissions.allow` entry
- 2026-07-30 · process · a subagent's deliverable must be a FILE, not its final message — 7 idle-without-delivering occurrences across 3 sessions, 3 model tiers, and both implementer and reviewer roles, and in every single case the work had been done and only the message was lost; switching to "write the report via heredoc, then reply with one line" produced both reports immediately
- 2026-07-30 · process · verify a subagent's report file EXISTS (`test -f` plus a grep for the covering-test evidence) before dispatching the re-review that depends on it — a fix report claimed in a status contract was never actually written, and the re-reviewer was dispatched with a dangling input that failed silently at dispatch time
- 2026-07-30 · process · re-vendor ace by copying ONLY the files the upstream change touched, not by re-running `bin/ace-sync` — the sync script's ALLOW list includes `.claude` and would overwrite nadal's own settings.json (hooks + model) since only PROJECT.md and bin/setup are preserved surfaces
- 2026-07-30 · process · ace's new `check_unenforced_headings` ratchet caught its first real target on the very commit that introduced it — restoring the "recorded, not machine-enforced" heading in a scratch copy failed parity at PROJECT.md:373, so the guard was proven red-before-green against the exact defect it was written for
- 2026-07-30 · process · `autoMode` is IGNORED in a committed project `.claude/settings.json` AND in `.claude/settings.local.json` — verified with a marker rule from a fresh invocation in a throwaway project; only `~/.claude/settings.json` (user) and `--settings <file>` (invocation) are honored, so the classifier permission that lets an agent run `gh pr merge` cannot be repo-tracked and must live in user or invocation scope
- 2026-07-30 · process · a committed `autoMode` block reads as authoritative and binds nothing — the same defect class as the merge-gate prose it was meant to complete, one layer down; caught by the first PR ever to run the `attested` gate (Codex [high], driver-confirmed), which is why the gate refused the merge
- 2026-07-30 · process · wave item 10 only PARTLY closed the temp-dir leak: the new test/helpers/tn-db.ts covers the four files it migrated, but ~15 `mkdtempSync` sites remain outside it (db-migrate, db-membership-unique*, cli-db-migrate-cwd, cli-main-exit-code, db-client*, plus two mid-test overrides in cli-db-migrate-command) — the helper exists, so finishing the migration is mechanical
- 2026-07-30 · fact · TennisLink is no longer a public source — every `tennislink.usta.com` leagues/ and tournaments/ path (StatsAndStandings, TeamProfile, Search, Scorecard, EventInfo) 302s to `account.usta.com/authorize?...&audience=tennislink`, and USTA's own support docs now say stats and standings require a signed-in account; spec § Ingestion lists TennisLink as ingestion path 1 ("public") and it belongs on path 2 (login-assisted, headed Chrome), which the spec already provides — no architecture change, but no TennisLink fixture can be captured without an HC login session
- 2026-07-30 · fact · TennisRecord's `matchhistory.aspx` is public and carries the entire scouting substrate the TennisLink player page was wanted for — per court: date, league (18+/40+), team, court slot (S1/D1-D4), partner + dynamic rating, opponents + dynamic ratings, W/L, set scores including match-tiebreak notation, per-match rating, resulting dynamic rating, and a `mid=` match id; court-slot tendencies, partner frequency, prior meetings and windowed records are all derivable from it, so the TennisLink login gate delays corroboration rather than blocking Phase 3
- 2026-07-30 · fact · WTN is not a separate fetch — the ITF widget (`.v-form-wtn-widget`) is embedded in the USTA player-search profile page, and carries confidence band and game zone in addition to the singles/doubles numbers; spec § Ingestion lists WTN as its own path, which is true organisationally and not operationally: one fetch, two parsers
- 2026-07-30 · risk · nadal is a PUBLIC repository and the source pages are about real people — the USTA profile page carries a `nameGenderAddress` block with name, gender and city/state — so fixtures cut from real captures are redacted through `tools/redact-fixture.ts` and the substitution map is deliberately kept outside the repo (committing it would publish exactly what redaction removes)
- 2026-07-30 · bug · a whole-string spelling list (literal / percent-encoded / plus-encoded) is not enough to find an identity in captured HTML: TennisRecord emits `teamname=Gerleman%2c Garrett` — comma encoded, space literal — which matches none of the three, and nine real surnames survived a redaction that reported success; fixed by matching per character (each non-alphanumeric may be literal or percent-encoded, space may also be `+`) and caught only because the structural detector sweep re-derives identities from the page's own markup rather than trusting the list
- 2026-07-30 · bug · `div.large table` is ambiguous on a TennisRecord team page — it holds both the roster and the local schedule — so a container-scoped selector silently parses schedule rows as roster entries; anchoring on a content marker (the table with an `NTRP` column, within `div.large`) is both narrower and more durable, and the mutation test that removed the first `div.large` is what exposed it
- 2026-07-30 · fact · TennisRecord renders every list twice, `div.large` for desktop and `div.small` for mobile, and the two are NOT interchangeable — the desktop match-history table has the full per-court detail but no opponent-team column while the mobile block names the opponent team, so the parser reads the desktop table and correlates the opponent team by position, verifying the per-row dates agree rather than trusting the ordering
- 2026-07-30 · process · a regex over `<tr style='...'>` missed the last row of a 14-row table (its style attribute differs), which would have shipped a parser that silently drops one match per page — the same page parsed with a DOM selector returned all 14; row-splitting by regex on real HTML is the defect, not the fixture
- 2026-07-30 · note · deferred to Phase 3 (#15): `court_matches` has no source-match-id column, but the match-history parser recovers one from the result link (`matchresults.aspx?...&mid=`) — that id is what makes a re-pull idempotent at court granularity (spec § Ingestion's first discipline), so #15 should add the column rather than dedupe on a composite key
- 2026-07-30 · note · deferred to Phase 3/4: a TennisRecord team page also carries the team's local schedule (date, time, opponent, site, result) in a second `div.large` block — team-match level data the Phase 2 scope did not include, and a cheaper path to `team_matches` rows than any TennisLink page
- 2026-07-30 · process · `skills/ship/SKILL.md` still asserts merge is `required` always ("a run that merged its own PR is a failed run regardless of the setting") in its quality-gate section, contradicting its own `<what-to-do>` and `<procedure>` sections which describe `attested`; ace ADR 0037 updated the latter and missed the former — an upstream fix, not a nadal edit, since `skills/` is vendored
- 2026-07-30 · bug · (Codex adversarial review, PR #26, rated critical) a per-character encoding matcher that knows only literal / percent / plus spellings still misses HTML character references — a server may write any character as `&#67;` or `&#x43;`, and `O&#39;Brien` is an ordinary way to emit an apostrophe in a name — so an identity could survive both substitution and the forbidden sweep of a PUBLIC-repo privacy control; fixed by adding decimal and hex entity alternatives per character AND sweeping an entity-decoded copy during verification, so an encoding the tool cannot yet substitute still fails the capture rather than shipping
- 2026-07-30 · bug · TennisRecord prints match scores WINNER-first, not player-first — every `L` row shows the opponent winning both sets (`6-3 6-3` on a loss) — so the original `games: [number, number]` tuple would have been read as player-first and inverted games-won for every loss in every dossier; the emitted type now names the fields `winnerGames`/`loserGames` so the orientation cannot be misread, and is proven from the fixture rather than assumed
- 2026-07-30 · process · a reviewer finding can be right about the defect and wrong about the fix — Codex flagged the match-tiebreak rule via a comment of mine that mislabelled `6-7 3-1 1-0` as a retirement, and recommended throwing on ambiguous notation; investigating the actual fixture showed the notation is consistent (winner-first, `1-0` marking the deciding-set tiebreak) and that the real defects were the wrong comment, a missing assertion on the real row, and the undocumented tuple orientation — take the substance, decline the remedy, and say which is which
- 2026-07-30 · bug · (Codex, PR #26) three fail-open paths in the match-history parser, all of the same shape — a mobile block that stops linking an opponent team, an unrecognised score component, and a participant cell missing a player — each produced a record that looked complete while quietly missing data; all three now throw, and the participant check enforces cardinality by discipline (doubles: partner + 2 opponents; singles: 1 opponent) except on an explicit default
- 2026-07-30 · bug · (Codex, PR #26) the capture tool redacted the provenance `sourceUrl` but never verified it, so a URL-only identity absent from the substitution map would have been written to a public provenance file — a second publication surface with the same exposure as the fixture, guarded only by the operator's memory; it now goes through the same redact-then-verify call as the page
- 2026-07-30 · bug · (Codex round 2, PR #26) "winner-first" is right about the source and wrong as a field name — TennisRecord orients each set to the MATCH winner, who still loses a set in any three-setter, so `winnerGames`/`loserGames` reads as *set* winner and is contradicted by the fixture's own `4-6 7-5 1-0` loss row; renamed to `matchWinnerGames`/`matchLoserGames` and asserted against both a straight-sets loss and the mixed-set one, since a test that only checks the straight-sets row passes for the wrong reason
- 2026-07-30 · bug · (Codex round 2) throwing on an unrecognised score component does not cover an EMPTY score cell — `lines()` returns `[]` and the loop never runs, so a stripped Result cell yielded a W/L record with `sets: []`, indistinguishable downstream from a legitimately scoreless match; the same shape applied to an emptied league-context or team cell, and all three now throw (defaults exempted from the score check, since nobody played)
- 2026-07-30 · bug · (Codex round 2) an alignment walk that picks the first alternative by `startsWith` desynchronises whenever a character's encoding BEGINS with the character — `&` is a prefix of `&#38;`, `%` of `%25` — consuming one character of a five-character sequence and corrupting every subsequent offset; fixed by ordering alternatives longest-first, which matters here because real team names contain `&` (`HOA/Thyagarajan/18&over4.0M`)
- 2026-07-30 · note · the `mid` query parameter on a TennisRecord result link is now a REQUIRED field rather than an optional one — it is the court-level idempotency key Phase 3 (#15) needs, so a record without it looks usable and cannot be reconciled on the next pull; if a legitimate page is ever found without one, model that state explicitly rather than relaxing the check
- 2026-07-30 · bug · (Codex round 3, PR #26) a "both are null" guard cannot see PARTIAL drift — renaming only the WTN SINGLES title left DOUBLES parsing fine, so the top-level guard stayed quiet and a real singles rating was silently erased into a result indistinguishable from a doubles-only player; every widget section is now inspected individually, and a titled section with a value but no recognised title throws
- 2026-07-30 · bug · (Codex round 3) `Number("")` is `0` and therefore FINITE, so a `Number.isFinite(Number(raw))` guard accepts an emptied cell as a rating of zero — the empty case has to be tested explicitly; caught by the very test written for the finding above, which is why the mutation test earns its keep over an eyeball review
- 2026-07-30 · bug · (Codex round 3) anchoring a positionally-decoded table on ONE header proves the right table was found and nothing about where anything sits inside it — remove the roster's Location column and every later field shifts by one while the nullable parsers absorb the mismatch into plausible nulls; the full ordered column contract is now asserted before any positional decoding, for both the team roster and the season-aggregate table
- 2026-07-30 · bug · (Codex round 3) `?? 0` on an aggregate cell converts column drift into STATISTICS — a season row of zero wins, zero losses, zero games reads as a player who did not play and is indistinguishable from one who did; aggregate cells now require a real number
- 2026-07-30 · bug · (Codex round 3) a structural sweep must cover what a human reading the page would recognise, not only what looks like an identifier — the USTA detectors checked uaid and tennis-id but not the displayed name, so a map containing the uaid and omitting the name replaced the id, passed both sweeps, and shipped the real name; detectors added for the displayed name and location, verified by confirming an unlisted name is caught
- 2026-07-30 · bug · (Codex round 3) a redaction stand-in containing `'` or `"` would terminate the attribute it lands in and corrupt the fixture's markup; rejecting unsafe replacement values at the input boundary is stricter than escaping and, unlike parsing and re-serialising the document, does not alter markup by itself — `&` stays legal because real team names contain it
- 2026-07-30 · bug · (Codex round 4, PR #26) a MINIMUM cell count does not protect positional decoding — insert one numeric column mid-table and the row still clears the minimum while every offset after it shifts, so totals parse "successfully" from the wrong columns; the season-aggregate table now asserts an exact ordered header contract and an exact body-cell count, matching the roster's
- 2026-07-30 · bug · (Codex round 4) validating each recognised section independently still accepts DUPLICATES — `find` takes the first and drops the rest, so a responsive or stale second WTN widget would silently decide which rating a dossier shows; the USTA page already renders its identity block twice (desktop + mobile variants), so this is not hypothetical markup
- 2026-07-30 · bug · (Codex round 4) the terminating semicolon on an HTML numeric character reference is OPTIONAL — browsers recover `&#67ory Hogan` and render it as `Cory Hogan` — so a redaction matcher and decoder that require it can be walked past by markup that displays perfectly; both now accept the semicolon-less form, with a negative lookahead so `&#67` cannot match inside a longer reference like `&#671;`
- 2026-07-30 · process · four adversarial review rounds on one PR found 18 real defects and ZERO false positives among the throw sites, including on a round explicitly prompted to hunt for over-strict guards (a bye, a walkover, an unrated player, a mixed league) — the finding count fell 5, 4, 6, 3 across rounds while the defect class stayed constant (a structural page change yielding data that looks complete), which is the signal that the class was systemic rather than incidental
- 2026-07-30 · bug · (Codex round 5, PR #26) the ordered-column contract had been applied to two positionally-decoded tables and missed on two others — the recent-team table and, most importantly, the match-history desktop table itself, the highest-value table in the project; fixing a defect class table-by-table leaves the ones nobody happened to look at, so the check now lives in one shared `assertColumns` helper that every positional decoder routes through
- 2026-07-30 · bug · (Codex round 5) guarding duplicate WTN section titles inside the FIRST widget says nothing about a second widget — `.first()` would silently pick one by DOM order and drop a conflicting rating; the same reasoning as the section guard, one level up, which is the tell that a guard was written for an instance rather than for the class
- 2026-07-30 · process · five adversarial rounds, 20 real defects, zero false positives — and rounds 4 and 5 each found the PREVIOUS round's fix applied too narrowly (a minimum where an exact count was needed; a duplicate guard one nesting level too deep; a contract on two tables out of four). The recurring lesson is not the individual defects but that a fix aimed at the reported instance rather than the class reliably leaves siblings behind, and an independent reviewer finds them in the next pass
- 2026-07-30 · bug · (Codex round 6, PR #26, merge-blocking) header count is NOT row width — the roster's `Rating` header carries `colspan="2"`, so 8 headers describe 9 body cells, and a guard written as `cells.length < headers.length` accepted a row with an extra cell inserted before the rating and read a different column as the rating; the PR text claimed an "exact per-row cell count" which was true for three of the four tables and false for this one, so the claim outran the code
- 2026-07-30 · process · (Codex round 6) the roster's column-contract tests passed for a reason other than their titles claimed — they mutate HEADERS, so `assertColumns` throws before the row-width guard is ever reached, leaving the body-decode path untested; a mutation that leaves the header alone and shifts only the body is the test that actually exercises it, and this is the concrete form of `rules/testing.md`'s "would I know if this passed but the feature were broken?"
- 2026-07-30 · bug · (Codex round 6) a redaction replacement may not INTRODUCE a URL-structural character (`& = ? #`) the original lacked — per-character encoding styles are recovered from the original, so a character present only in the replacement is emitted literally and `playername=A&B` splits the query; characters present on both sides are safe because their encoding was observed, which is why the earlier ampersand test passed without covering this
- 2026-07-30 · bug · (Codex round 7, PR #26, merge-blocking) `.filter(row => row.name !== "")` after a positional decode is a FAIL-OPEN in disguise — it looks like defensive tidying and it silently deletes a player from a roster, an affiliation from a profile, or a season from an aggregate table while every other field still looks complete; found in three parsers by the reviewer and in a fourth (the season-year drop) by applying the finding to its siblings, all four now throw
- 2026-07-30 · bug · (Codex round 7) a null-only guard on a query parameter misses the EMPTY value — `hrefParam` returns `""` for `mid=`, so the court-level idempotency key could ship blank while the guard directly above it claimed it was required; the existing test removed the parameter entirely and therefore never exercised the case
- 2026-07-30 · process · seven adversarial rounds, 25 real defects, zero false positives, and the class narrowed rather than the count falling monotonically (5, 4, 6, 3, 2, 2, 3) — rounds 6 and 7 both found defects whose tests EXISTED and passed for a different reason than their titles claimed, which is the single highest-yield question to ask of a test suite and is worth making a standing review prompt rather than an occasional one
- 2026-07-30 · bug · (Codex round 8, PR #26, critical, merge-blocking) a hand-written named-entity table is the WRONG SHAPE for a privacy control — HTML5 defines over two thousand named references, and `Cory&NewLine;Hogan` renders as the name while evading any list that does not happen to include `&NewLine;` or `&Tab;`; the fix is not a longer table but a standards-complete decoder, so verification now also sweeps a rendered view built by parsing the document (text nodes AND attribute values, whitespace collapsed), which is complete by construction and stays complete as the spec moves
- 2026-07-30 · process · the durable lesson across eight rounds is about the SHAPE of a guard, not its contents — a hand-written enumeration (entity table, spelling list, single anchor column, null-only check) fails silently at its edges, while a guard derived from the structure itself (parse and read back, ordered column contract, exact row width, structural detector sweep) fails closed at them; every round-over-round regression on this PR was an enumeration that needed to become a derivation
- 2026-07-30 · bug · (Codex round 9, PR #26, critical, merge-blocking) a rendered view built from `.text()` plus attributes is still incomplete — cheerio does not return COMMENT bodies, so `<!-- Cory&NewLine;Hogan -->` evaded all three sweeps while rendering as the identity; comment contents are raw text, so each is now parsed in turn and read back, giving it the same standards-complete decoding as the document
- 2026-07-30 · bug · a node walk that returns early on any non-`tag` node never leaves the DOCUMENT ROOT (its type is `root`, not `tag`), so it silently collects nothing — and an existing attribute test kept passing because a DIFFERENT sweep happened to catch that case, which is why the regression only surfaced when a test was added for a case only the walk could catch; recurse on anything with children, and treat a guard that passes because of a neighbouring mechanism as untested
- 2026-07-30 · process · nine adversarial rounds, 27 real defects, zero false positives; the last three findings were all in the same 40-line verification function, each one a completeness gap in a different node type (text -> attributes -> comments), which is the signature of a guard that should be derived from the document structure rather than assembled from the node kinds someone thought to list
- 2026-07-30 · bug · (Codex round 10, PR #26, critical, merge-blocking) collapsing `\s+` does not normalise ZERO-WIDTH characters — U+200B is not JavaScript whitespace, so `&ZeroWidthSpace;` decoded into an identity leaves it matching neither the raw pattern nor the spaced one, while rendering as the name; the rendered verification view now maps the whole Cf category plus the zero-width set to a space before collapsing
- 2026-07-30 · bug · (Codex round 10) including `\s` in a base64 payload character class made the match run past the end of an UNQUOTED attribute value and eat the separator before the next attribute — `<img src=data:...,QUJD alt=logo>` became `...REDACTEDalt=logo`, silently deleting `alt` from a tool whose entire purpose is preserving markup
- 2026-07-30 · process · (Codex round 10) a test named "leaves the markup structurally identical" compared only tag and class, so it passed while an attribute was being deleted — the name promised more than the assertion checked; it now compares attribute NAMES (catching deletion) plus class VALUES (what selectors read), deliberately not other attribute values, which redaction is supposed to change
- 2026-07-30 · bug · (Codex round 10) a nullable correlation key silently downgrades the check that depends on it — the mobile block's date was parsed as optional and the desktop/mobile comparison skipped when it was null, leaving count-only positional matching under which a reorder misattributes every later opponent team, while the parser's own comment claimed position was verified by dates; the date is now required
- 2026-07-30 · bug · (Codex round 11, PR #26, critical, merge-blocking) Unicode normalisation is a redaction bypass — `José` composed (U+00E9) and decomposed (`e` + U+0301) are the same name to every reader and different code-point sequences to a matcher, so a substitution map written in one form silently misses the other and an accented real name ships; verification now folds every view AND every forbidden/allow-list value to NFC, and a percent-decoded view was added so the same identity hidden in a URL-encoded value is visible in whichever form it was encoded from
- 2026-07-30 · process · eleven adversarial rounds on one PR, 31 real defects, zero false positives — the privacy control alone accounted for defects in eight distinct dimensions (whole-string spelling, mixed percent encoding, numeric entities, semicolon-less entities, named entities, node kinds, zero-width normalisation, Unicode normalisation), which is the concrete cost of hand-rolling a redaction tool and the strongest argument in the record for treating "is there a library for this" as a first question rather than a fallback
- 2026-07-30 · risk · (Codex round 12, PR #26, merge-blocking) the fixture redaction control is a BLACKLIST and therefore structurally incomplete — it verifies only identities already in the substitution map plus narrow source-specific detectors, so an unlisted identity of a kind the map was never about (the reviewer's example: a `mailto:` link) is neither replaced nor rejected and is written to a public fixture with the tool reporting success; an independent reviewer judged documenting the residual risk INSUFFICIENT and called for inverting the trust boundary (extract only parser-required subtrees under an explicit allow-list, or use a mature sanitisation library with a closed output policy) before further public fixture capture
- 2026-07-30 · fact · the seven fixtures committed on PR #26 were swept empirically for the unlisted classes and are clean — no email, mailto, tel, phone number or street address in any of them, and every five-digit run is an SVG path coordinate or a TennisRecord match id rather than a postal code; the blocker is about what the capture TOOL could publish next, not about what this PR ships
- 2026-07-30 · process · asking a reviewer to rule on a DESIGN judgement, separately from "is anything blocking", produced the most valuable single answer of twelve rounds — it converted my own "documented residual risk is an acceptable trade" into an explicit external "no, that class of fail-open cannot be documented away", which is exactly the call an author is worst placed to make about their own work
- 2026-07-30 · fact · (issue #28) the allow-list policy's per-source vocabulary was seeded by re-running the EXISTING `tennisrecord`/`usta` detector regex sets from `tools/capture-fixture.ts` over the seven already-committed, already-redacted fixtures via `tools/bootstrap-vocabulary.ts` — 196 distinct skeletons for tennisrecord (139 auto-written, 57 name-shaped) and 686 for usta (419 auto-written, 267 name-shaped); the synthetic stand-in list (`tools/fixture-vocabulary/stand-ins.txt`) was seeded the same way, reusing proven machinery rather than inventing a new derivation (deviation D1 from the posted plan)
- 2026-07-30 · risk · (issue #28) the name-shape heuristic ("two or more consecutive capitalised words") is deliberately broad and over-triggers hard on nav-chrome-heavy content: 267 of USTA's 686 skeletons were flagged name-shaped, almost all of it ALL-CAPS menu labels ("SIGN IN", "US OPEN") and cookie-banner boilerplate rather than anything resembling a real name — a client-rendered SPA capture will make this manual-review set large every time, which is a real scaling cost of the allow-list design worth knowing going in, not a defect in this PR
- 2026-07-30 · risk · (issue #28) `stand-ins.txt`'s seed is NOT exhaustive of every synthetic value the fixtures contain — it only captures what the narrow `playername=`/`teamname=`/`uaid`/`tennis-id`/displayed-name/displayed-location detector patterns match. Synthetic club and city/state names embedded elsewhere on the page (e.g. `Clayview Country Club`, `Cedar Falls MO`) fell outside those patterns and surfaced as name-shaped vocabulary entries needing individual hand justification instead of being pre-elided as stand-ins; a future re-capture that introduces a new synthetic place/club name in a new markup position will hit the same gap
- 2026-07-30 · process · (issue #28) a circular import between `tools/redact-fixture.ts` and `tools/fixture-policy.ts` (each needs functions from the other: `redact()` calls the new `assertAllowListed`, `fixture-policy.ts` reuses the existing normalisation helpers) is safe under Node's ESM loader ONLY when every cross-module reference is used inside a function BODY; a `class PolicyError extends RedactionError` reference is evaluated at module TOP LEVEL and threw `Class extends value undefined` regardless of which side of the cycle loaded first — `PolicyError` was made its own `Error` subclass rather than extending `RedactionError` to avoid this, a distinction worth knowing before reaching for "just extend the sibling error class" across a deliberately circular pair of modules
- 2026-07-30 · bug · (issue #28 reconciliation) 14 name-shaped vocabulary entries across tennisrecord.txt/usta.txt carried a `# reviewed:` justification claiming "already-redacted synthetic ... it is the same class of invented value" that no code verified and no line of `stand-ins.txt` backed — several (Leawood KS, Lawrence KS, Clayview Country Club, Hallbrook Country Club) are recognisably real Kansas City-area places, meaning a future capture containing a real city/club name could have been admitted on the strength of the same unverifiable prose, exactly what `rules/testing.md` calls a check that certifies an invariant it cannot see; fixed by adding a machine-checked `# reviewed[synthetic]: <reason>` marker (`loadVocabulary()` now refuses to load a synthetic-classed entry unless every capitalised token is present in `stand-ins.txt`) and reclassifying all 14 as real-public — a category the vocabulary deliberately admits, since an organisation or place name doesn't identify an individual — with honest, entry-specific reasons; caught by reconciliation against the allow-list's own stand-in register, not by any of the 225 passing tests, since they exercised the parsing mechanism rather than the truth of the human's claim
- 2026-07-30 · bug · (issue #28, PR #29 self-review finding 1, critical) the DEFAULT invocation of `tools/capture-fixture.ts` (no `--detectors` flag, the documented shape) resolved `detectorSet` to `"none"`, whose `defaultVocabularyPath` returns `undefined`, and `redact()` only runs `assertAllowListed` when `options.vocabulary !== undefined` — so the most ordinary invocation of the capture tool ran with NO allow-list enforced at all and reported success, the exact fail-open shape issue #28's whole allow-list redesign exists to eliminate, surviving a full green run plus a prior reconciliation pass; fixed by having `main()` refuse outright (no bypass flag) whenever no vocabulary path resolves, naming `--vocabulary` in the error — a control that certifies an invariant only on some of its invocation paths is not a control, it is a false sense of one, and the gap didn't show up in any test because every existing test happened to pass `--vocabulary` explicitly
- 2026-07-30 · bug · (issue #28, PR #29 self-review finding 2) `tools/fixture-policy.ts`'s `extractAtoms` had a guard `if (node.type === "tag") { if (node.tagName === "script" || node.tagName === "style") return; ... }` that could never fire — domhandler types a `<script>`/`<style>` element's `node.type` as `"script"`/`"style"`, never `"tag"` — so those elements fell through to the generic child-walk, atomising their (already-redaction-stripped) text body while never creating an atom for their attributes at all, a publication surface entirely outside the allow-list; caught only by constructing the exact failing input directly against `extractAtoms`, since none of the seven committed fixtures' script/style tags carry attributes (redaction already strips the whole tag pair) — a reminder that "the committed fixtures all pass" does not mean "the function is correct," only that today's inputs don't exercise the gap
- 2026-07-30 · bug · (issue #28, PR #29 round-1 adversarial review finding 1, high) `redactHtml`'s `SCRIPT_OR_STYLE` regex (`/<(script|style)\b[^>]*>[\s\S]*?<\/\1>/gi`) only strips PAIRED script/style tags, and `extractAtoms` unconditionally skipped script/style CHILDREN on the assumption that redaction had already emptied them — so a truncated/malformed capture like `<script>Patrick Turner` (no closing tag) never matched the strip regex, its body survived untouched, and the skip then discarded it unread: no atom, no allow-list check, at the exact same finding-2-fixed code path. Fixed by atomising script/style TEXT children like any other node instead of skipping them — free for well-formed input (an already-stripped `<script></script>` has no children left) and closing for malformed input
- 2026-07-30 · bug · (issue #28, PR #29 round-1 adversarial review finding 2, high) `isStructuralAttrName` exempted `class`/`id`/`style`/every `aria-*`/every `data-v-*` from atomisation by NAME alone, so `aria-label="Patrick Turner"` (or the same in `id`/`class`) survived redaction with no atom, no vocabulary check, and no never-publish-class match — an uninspected PII channel, ARIA labels being content exposed to assistive technology and `id`/`class`/`style` being author-chosen free-form strings, not fixed vocabularies. Fixed by narrowing the exemption to a principled line — numeric/geometric/HTML-SVG-spec-enumerated attribute values only (`d`, `viewBox`, `width`, `role`, `type`, …) — never a free-form string; the seven committed fixtures needed +808 vocabulary entries as a result (95 tennisrecord, 713 usta; only 56 name-shaped, the rest boilerplate CSS-class/id/AEM-component tokens auto-admissible under the existing non-name-shaped convention)
- 2026-07-30 · bug · (issue #28, PR #29 round-1 adversarial review finding 3, high) `loadVocabulary`'s `# reviewed[synthetic]:` check verified each capitalised TOKEN in an entry independently against tokens drawn from `stand-ins.txt`, rather than requiring the whole entry to be accounted for by full stand-in PHRASES — since the committed register has "Avery Ashby" and "Arden Ashcroft" as separate entries, a spliced entry "Avery Ashcroft" (a full name in NEITHER stand-in) passed as machine-verified "synthetic" because "Avery" and "Ashcroft" each matched a token from a DIFFERENT stand-in. Fixed by reusing `computeSkeleton` itself (the same reduction the allow-list check runs) — an entry passes iff `computeSkeleton(entry, standIns) === ""`, eliding full phrases at word boundaries, not tokens independently
- 2026-07-30 · process · (issue #28, PR #29 round-1 adversarial review) across five structural escapes found in this control over two review rounds, EVERY one was a category of content the policy never atomised at all (an unclosed script/style body, a whole class of attribute names, an independently-token-checked synthetic marker) — never a case where the reduction ladder ran on an atom and judged its skeleton wrongly; the ladder's logic held under every attack, its INPUT SET did not. A completeness argument for this shape of control needs to enumerate every node/attribute/marker category the extractor could see and show each is atomised or explain why not, rather than reasoning forward from the categories someone already thought to add
- 2026-07-30 · bug · (issue #28, PR #29 round-2 adversarial review finding R2-1, high) `xmlns`/`fill`/`stroke` were exempted from atomisation by NAME alone even though their values are not closed enumerations — `<svg xmlns="https://example.test/Patrick-Turner">` and `fill="url(https://example.test/Patrick-Turner)"` created no atom at all, so an empty vocabulary admitted an identity written straight into a public fixture. Fixed by exempting these three names only when the VALUE matches a genuinely closed grammar (a hex colour, a CSS Level 4 named colour, or `none`/`currentColor`/`transparent`/`context-fill`/`context-stroke` for `fill`/`stroke`; one of five standard XML/SVG namespace URIs for `xmlns` — and `xlink`, since htmlparser2's HTML tokenizer reports `xmlns:xlink="…"` as a bare `xlink` attribute, confirmed empirically rather than assumed) — anything else, including a `url(...)` paint reference, is atomised like any other value
- 2026-07-30 · bug · (issue #28, PR #29 round-2 adversarial review finding R2-2, high) element names and attribute NAMES were an entirely zero-atom channel — `<patrick-turner></patrick-turner>` produced no atom of any kind, and `<div data-patrick-turner>` produced only an empty-valued attribute atom (the NAME itself was never inspected) — so a custom element or attribute name was a free identity-smuggling channel with no vocabulary check at all. Fixed by atomising the element name and every non-standard attribute name against closed, spec-derived allow-lists (`STANDARD_ELEMENT_NAMES`/`STANDARD_ATTRIBUTE_NAMES`, built from the HTML/SVG/ARIA specs, deliberately NOT derived from the fixtures); `data-*` stays a genuine open PREFIX (only the remainder after the prefix is atomised, e.g. `data-patrick-turner` → `patrick-turner`), while `aria-*` is enumerated as a CLOSED list rather than treated as a prefix, since unlike `data-*` the WAI-ARIA spec does not allow arbitrary `aria-*` suffixes
- 2026-07-30 · bug · (issue #28, PR #29 round-2 adversarial review finding R2-3, high) `isNameShaped` matched only ASCII-capitalised word pairs (`[A-Z][A-Za-z'-]*` repeated), so `patrick turner` — lowercase, the common shape of a URL slug or lower-cased page text — and any non-ASCII name (`josé garcía`) loaded from a vocabulary file with NO preceding `# reviewed:` line and were auto-writable by `bootstrapVocabulary`, defeating the human-review boundary the entire allow-list design rests on: capitalisation is not a safety boundary, and treating it as one meant the control's coverage silently depended on how a page happened to render a name. Fixed by rebuilding the test on Unicode `\p{L}`/`\p{M}` classes (any two-or-more-token alphabetic skeleton, any case or script triggers review) and renaming it `requiresReview` to describe what it now means
- 2026-07-30 · process · (issue #28, PR #29 round-2 reconciliation) across ALL EIGHT structural escapes found against this control over two adversarial-review rounds, every one was the identical SHAPE of bug — a category of document content `extractAtoms` never produced an atom for at all (an unclosed script/style body, a whole class of attribute NAMES, element NAMES, a name-only-exempted attribute value with no closed grammar, a case-sensitive review trigger) — never a rule that ran on an atom and judged its skeleton wrongly. A hand-enumerated "list of the content classes I check" is structurally a blacklist no matter how principled each individual rule is, which is the exact complaint issue #28 opened against the ORIGINAL blacklist-based redaction tool; the fix this round inverts the walk itself to be a POSITIVE accounting of every node/name/value the parser can produce (walk every domhandler node kind; atomise every element/attribute name against a closed allow-list; exempt an attribute value only by closed grammar, never by name alone) so an unrecognised future input class refuses BY CONSTRUCTION rather than shipping silently until a ninth reviewer happens to name it
- 2026-07-30 · fact · (issue #28, PR #29 round-2 reconciliation) the whole-document-accounting rewrite (element-name/attribute-name atomisation, plus dropping capitalisation as `requiresReview`'s trigger) grew the two committed vocabularies from 291/1399 real entries to 293/1449 (tennisrecord/usta) — a modest net entry count, since the new element-name/attribute-name/attribute-value channels rarely fire against already-redacted, mostly-standard markup — but it reclassified roughly 1,000 previously bare (no-justification-needed) entries as requiring a `# reviewed:` line, because `requiresReview` no longer distinguishes `Patrick Turner`-shaped content from lowercase AEM/CSS component class names and cookie-consent legal paragraphs that merely happen to be multiple words; nearly all of that churn was mechanically categorisable into the file's existing justification conventions (AEM/BEM component class names, cookie-consent boilerplate, URL/page-route path segments, opaque analytics tokens), with zero of the newly-introduced entries matching the OLD capitalised-name shape — i.e. no new capitalisation-shaped risk was hiding in the churn, only previously-invisible lowercase/mixed-case boilerplate now made to show its justification
- 2026-07-30 · bug · (issue #28, PR #29 round-3 adversarial review finding R3-1, high) `redactHtml` writes the RAW string, but `assertAllowListed`/`extractAtoms` inspected only cheerio's RECOVERED DOM — wherever the parser recovers from malformed markup (an orphan end tag like `</Patrick Turner>`, discarded outright by the HTML5 tree builder), those are two different documents and only one was checked, so `redact('<div></Patrick Turner>', [], { vocabulary: new Set() })` returned the identity-bearing raw string with zero atoms and a passing check. A checker that validates a PARSED representation while a RAW string is what gets PUBLISHED is checking a different document than the one it publishes — parse-recovery divergence is a bypass class in its own right, distinct from "a content category the walk never atomised" (the shape of all eight prior findings). Fixed by loading with `sourceCodeLocationInfo: true` and separately accounting for every byte the parser discarded: the complement of every kept node's source range catches an orphan token BETWEEN two real nodes, and a text-node-scoped scan (cross-checked against every other node's real start/end-tag location, to avoid flagging HTML5's "after `</body>`, trailing whitespace merges into body's last text node" shape) catches an orphan token the tree builder merged INTO a text node's reported span without it appearing in that node's `.data`. Zero vocabulary entries were needed for the seven committed fixtures.
- 2026-07-30 · bug · (issue #28, PR #29 round-3 adversarial review finding R3-2, high) round 2 (finding R2-1) established that name-only attribute-value exemption is unsafe as a PRINCIPLE and fixed it for exactly the three attributes that round happened to name (`xmlns`/`fill`/`stroke`) — but left every OTHER "structural" attribute (`d`, `viewBox`, `width`, `height`, `colspan`, `rowspan`, `transform`, `role`, `type`, `points`, `preserveAspectRatio`, `focusable`) exempt by name alone in a separate `NAME_ONLY_STRUCTURAL_ATTR_NAMES` set, so `<path d="Patrick Turner">`/`<img width="Patrick Turner">`/`<div role="Patrick Turner">` all produced zero attribute atoms with an empty vocabulary — the identical mistake reproduced one round later because the FIX was scoped to the instances the reviewer named rather than to the principle the reviewer stated. Applying a review's lesson only to the specific instances it named, rather than to the principle it stated, reproduced the same defect class one round later. Fixed by retiring `NAME_ONLY_STRUCTURAL_ATTR_NAMES` entirely and giving every structural attribute its own closed value grammar (SVG path/point-data command grammar, an exactly-4-number `viewBox`, a numeric-with-optional-unit `width`/`height`, integer `colspan`/`rowspan`, a closed transform-function grammar, a closed ARIA role set, a closed `type` token set, closed `preserveAspectRatio`/`focusable` token sets) — a value outside its grammar is atomised, not silently admitted. One vocabulary entry was needed across both fixtures: `imgage`, a real (non-ARIA) `role` value from a third-party Google Custom Search widget's inline SVG — an apparent upstream typo in vendor markup, not personal data.
- 2026-07-30 · bug · (CodeQL js/redos, high, PR #29) the closed `transform` attribute grammar added to close a round-3 finding was itself exponential — `\s*[,\s]\s*` inside a `*` group can consume one whitespace run many ways, so `scale(0` plus repeated `\t\t0` backtracks catastrophically; replaced with a linear grammar walk (`[^()]*` chunks + a plain split) per `rules/scripting.md`'s implement-the-grammar rule, with a regression test that asserts a time bound rather than only a verdict, because the failure mode of a returning ReDoS is a hang rather than a wrong answer
- 2026-07-30 · process · closing a privacy finding with a hand-written regex grammar introduced a high-severity security alert in the same commit — a required check caught it, but the general lesson is that a regex written to make a control STRICTER is still new attack surface and deserves the same scrutiny as the hole it closes
- 2026-07-30 · process · (#15) drizzle-orm's `onConflictDoNothing({ target, where })` on better-sqlite3 emits the partial-index `WHERE` clause AFTER `do nothing` (`on conflict (cols) do nothing where …`), which SQLite rejects outright — a targeted partial-index upsert has to use `onConflictDoUpdate` instead, whose `targetWhere` is placed correctly (before `do update set`), even when there is nothing meaningful to update (e.g. `team_memberships`, whose `set` re-assigns a key column to itself as a harmless no-op); confirmed empirically against a real `SqliteError`, not assumed from the drizzle docs
- 2026-07-30 · process · (#15) drizzle-orm's `db.transaction(cb)` callback argument (`SQLiteTransaction`) and the top-level `db` from `openDb()` (`BetterSQLite3Database & { $client }`) are NOT the same type — a shared upsert/identity module typed against the concrete `BetterSQLite3Database` fails to typecheck when called with the transaction handle passed by `db.transaction`; typing every write function against `BaseSQLiteDatabase<'sync', RunResult>` (the common ancestor of both) fixes it and is what actually lets "one transaction per pull" (spec § Ingestion) share the same write functions inside and outside a transaction
- 2026-07-30 · fact · (#15) TennisRecord's team page's local-schedule table (`div.large`, second occurrence, anchored on its own "Local Schedule" header cell rather than positionally) carries `team_matches` candidates with a `mid=` source match id, but no per-player detail — corroborating the Phase-2 note that it's a cheaper `team_matches` path than TennisLink; the roster table's per-row `profile.aspx?playername=…` links are the team→player cascade seam `tn team pull --players` walks
- 2026-07-30 · idea · (#15) `tn team pull`/`tn player pull` resolve every roster/opponent/partner name through the same fuzzy identity ladder and abort the WHOLE pull's transaction on any ambiguous match rather than writing a partial roster — simple and safe for v1, but a scouting-heavy team page could plausibly hit a real ambiguity often enough that a partial-write-with-a-warning mode (skip just the ambiguous row, like the existing no-profile-link skip) is worth revisiting once real usage shows how often it fires
- 2026-07-30 · bug · (#15, self-review) a team page's UNPLAYED fixtures have no result link and therefore no `mid=`, so their `team_matches` rows took `upsertTeamMatch`'s plain-insert branch and re-pulling a mid-season page grew one duplicate row per unplayed fixture PER PULL — the headline idempotency promise, falsified by exactly the rows the committed fixture (an end-of-season page where all 10 rows have results) never contained; fixed by deduping an id-less row on its natural composite (home team, visiting team, date), and the same landmine was defused in `upsertCourtMatch` before a future source could step on it
- 2026-07-30 · bug · (#15, self-review) `hrefParam` returns `""` for a bare `mid=`, and `""` is NOT NULL to SQLite, so every empty-id schedule row would have collided into a single `team_matches` row under the partial unique index — the exact argument `match-history.ts` already carries from Codex round 7 on PR #26 ("an empty idempotency key is as unusable as a missing one"), which the schedule parser had not inherited; applying a review's stated PRINCIPLE only to the instances it NAMED is the #28 lesson repeating one PR later
- 2026-07-30 · bug · (#15, self-review) `upsertTeam`'s conflict `set` wrote `values.x ?? null` for every optional column, so a TennisRecord pull — which knows nothing about `tennislink_url` and passes `district: null` unconditionally — silently erased whatever another source had recorded, on every re-pull; latent only because nothing sets those columns yet, and #27 (TennisLink) is the run that would have hit it
- 2026-07-30 · bug · (#15, self-review) the archive path guard constrained only the LEAF ("is the file under rawRoot?"), which a misconfigured `TN_RAW_PATH=src` satisfies trivially while writing un-redacted captures into a TRACKED directory of a PUBLIC repo — the plan specified "inside the repo tree except under raw/", so the root itself must be constrained; the same shape as #28's ten defects, where the rule was right and the INPUT it was applied to was wrong
- 2026-07-30 · process · (#15, self-review) three pull tests never set `TN_RAW_PATH`, so `npm test` wrote captured pages into the repo's own `raw/` on every run — harmless today because `raw/` is gitignored, but the PR checklist asserted "archive tests only ever write into a temp TN_RAW_PATH", which was false; a claim in a checklist is not evidence, and this one was checkable in one grep
- 2026-07-30 · note · (#15) `home`/`visiting` on `team_matches` and `side` on `court_match_players` encode the PERSPECTIVE THE PAGE WAS PULLED FROM, not real home/away — TennisRecord does not state which team hosted. Pulling two opposing teams rewrites the labels to the later pull's perspective; every column flips together so the row stays self-consistent and semantically equivalent, but the labels are NOT stable across pull order and Phase 4 must not read them as real home/away
- 2026-07-30 · note · (#15) `upsertCourtMatchPlayers` never deletes rows, so if a source page is corrected to name different opponents, the superseded participants remain attached to the court match — a re-pull adds but never retracts; low likelihood, but a dossier reading participant counts should not assume exactly 2 or 4
- 2026-07-30 · bug · (#15, Codex adversarial review PR #31, rated critical) a LEXICAL path check answers "what does this string say", not "where do the bytes land" — `resolve()` never consults the disk, so making `<repo>/raw` a symlink to `<repo>/src` (or pointing `TN_RAW_PATH` at an external symlink that targets the repo) satisfied every string comparison in the privacy guard while `writeFileSync` followed the link into tracked source; fixed by re-validating the root at its REAL location (via the nearest EXISTING ancestor, since the root legitimately does not exist before the first capture), refusing symlinked components inside the root, re-checking after `mkdirSync` — which treats an existing symlink-to-a-directory as "already there" — and writing with `flag: "wx"` so the leaf cannot be followed either
- 2026-07-30 · bug · (#15, Codex adversarial review PR #31, rated high) the fix for the id-less-fixture duplicate REPRODUCED THE DEFECT ONE PERSPECTIVE OVER: it deduped on ORDERED `(home, visiting, date)`, but team-pull assigns the PULLED team to `home`, so the same unplayed fixture pulled from the opposing team's page searched `(B,A,date)`, missed `(A,B,date)` and inserted a duplicate anyway; the key has to be the UNORDERED team pair, and court counts arriving from the opposite orientation are swapped into the stored row's orientation rather than written straight through — a fix that is correct only for the instance that motivated it is the #28 lesson yet again
- 2026-07-30 · bug · (#15, Codex adversarial review PR #31, rated high) `tn team pull --players` committed the team transaction BEFORE the cascade and then only warned on each failed player pull, so the command returned `ok` with exit 0 having enriched ZERO of the players it was explicitly asked to enrich, with an irreversible team write already on disk; a requested-but-unfulfilled cascade now reports `status=partial`, names the entries, and exits non-zero — the outcome has to be visible to a caller that reads only the exit code
- 2026-07-30 · bug · (#15, Codex adversarial review PR #31, rated medium) one identity ladder cannot run two notions of "the same name": tier 2 folded case in SQLite (`lower()`, ASCII-only) while the canonical-name half of the SAME tier folded in JS (Unicode), so an alias `Élodie` never matched a source spelling `élodie` and a second row was created for someone already on file; both halves now fold in JS, and the read-back-after-insert (which shared the ASCII-only `lower()`) is replaced by `.returning()`
- 2026-07-30 · bug · (#15, Codex adversarial review PR #31, rated medium) a read-sleep-write politeness delay is not serialized — `Promise.all` had every caller observe the same "last call" timestamp, and the third saw the interval already elapsed and skipped its wait entirely, so requests went out together exactly when pacing matters most; each caller now claims its slot SYNCHRONOUSLY before its first await
- 2026-07-30 · bug · (#15, Codex adversarial review PR #31, rated medium) `response.text()` ran BEFORE the `response.ok` check, so a server that sent 404/500 headers and then streamed an endless body blocked the caller to the abort timeout and surfaced a timeout instead of the documented `FetchError` — the existing tests used only finite error bodies, so they structurally could not observe it
- 2026-07-30 · process · (#15) a fix written under review pressure needs its own test run before it is believed: the realpath hardening threw ENOENT on a raw root that did not exist yet — i.e. every first capture on a fresh machine — and it was the newly-written regression test, not the reasoning, that caught it
- 2026-07-30 · bug · (#15, Codex adversarial review PR #31 round 2, rated high) the unordered-pair fix for id-less fixtures OVER-merged: date alone is not an identity, so a same-day doubleheader between the same two teams (09:00 and 17:00, neither with a `mid=`) collapsed into one row and the second was lost undetectably — and the schedule parser was READING the Time and Match Site columns (both are in its column contract) while the emitted row discarded them; persisted `team_matches.scheduled_time` + `site` and added them to the id-less key. Two rounds on the same three lines, each fix correct for the case that motivated it and wrong one case over — the argument for a discriminator you can point at, rather than one you reason your way to
- 2026-07-30 · bug · (#15, Codex adversarial review PR #31 round 2, rated medium) a politeness reservation records when a request was MEANT to go out, not when it did — an event-loop stall past the reserved slot let a later caller reserve from the stale intended time and start too soon after it, violating the interval exactly under load; the slot is now re-anchored (monotonically) to the actual wake time
- 2026-07-30 · bug · (#15, Codex adversarial review PR #31 round 2, rated medium) throwing `FetchError` before reading a non-2xx body fixed the blocking-until-timeout defect but left the response stream and its socket alive until the abort fired 20s later, so a server streaming an endless error body accumulated one live stream per call; the body is now explicitly cancelled (best-effort, so a locked/errored body cannot mask the FetchError)
- 2026-07-30 · note · (#15) `archivePage` writes through the directory's REALPATH and validates where it points, rather than only rejecting components that ARE symlinks — "is this a link?" is a weaker question than "where does it lead?". A check-then-use window remains and is NOT claimed closed: pure Node has no `openat`/`O_NOFOLLOW` directory-handle write, and a native helper is a dependency the plan forbids. Escalated to the HC rather than silently accepted
- 2026-07-30 · bug · (#15, Codex adversarial review PR #31 round 3, rated high — CRITICAL by PROJECT.md's framework, since it ships broken) the realpath hardening applied the ROOT-ONLY allowlist to a DESCENDANT directory, so with `TN_RAW_PATH` unset — the documented default — `<repo>/raw/tennisrecord` was rejected and EVERY pull threw before writing a byte; not one test caught it because every test set `TN_RAW_PATH` to a temp dir, so the suite exercised only the configuration it had invented and never the one the docs describe. A privacy control that refuses everything looks identical to a privacy control that works, right up until someone runs it
- 2026-07-30 · bug · (#15, Codex adversarial review PR #31 round 3, rated high) the id-less fixture key compared `scheduled_time` and `site` as EXACT strings, so the same fixture re-rendered as `09:00 AM` rather than `9:00 AM` missed its stored row and duplicated — format sensitivity in the one key that exists precisely because there is no `mid=` to fall back on; the SQL now narrows on the indexable, stable parts (both teams, the date, no source id) and a normalized comparison picks the match out of that small candidate set, with the DISPLAY values still stored
- 2026-07-30 · process · (#15) three independent review rounds landed on the SAME three lines of `upsertTeamMatch`, and each fix was correct for the case that motivated it and wrong one case over: ordered pair → unordered pair → unordered pair + date → + time/site → + normalized time/site. The lesson is not "think harder" — it is that a key assembled by reasoning about cases keeps inheriting the case you last thought about, and only an adversary enumerating the input space finds the next one
- 2026-07-30 · bug · (#15, Codex adversarial review PR #31 round 4, rated high) a normalizer that can map a NON-NULL input onto the NULL key merges two facts that are not the same fact: stripping non-alphanumerics turned `"—"` (how a schedule renders a time not yet set) into `""`, exactly the key `null` used, so a dash-timed fixture matched a fixture with no time at all and one real row was silently lost; fixed with disjoint TAGGED key domains — `" null"`, `clock:HH:MM`, `raw:<stripped>`, and `lit:<original>` when stripping would leave nothing — so no non-null input can produce the null key and two different punctuation-only values stay distinct from each other
- 2026-07-30 · process · (#15) FOUR review rounds landed on `upsertTeamMatch`'s id-less key, and the last two were defects introduced BY THE PREVIOUS ROUND'S FIX — a broken default root, then a key collision in the normalizer written to fix a key collision. The through-line is that each fix was validated against the case that motivated it and never against the space around it; the counter-practice that actually worked was reproducing the defect first (10→11 rows, ArchivePathError under the documented default) and writing the test before the fix, because both times the test found something the reasoning had not
- 2026-07-30 · process · nadal's kickoff rule "every SOW includes a manual-test segment for HCs plus runbooks for manual testing" is encoded in NO artifact: `skills/final/SKILL.md`'s SOW template carries only *Testing Coverage* (automated coverage + Quality Checks results), no stage in `skills/` or `docs/standards/development-lifecycle.md` requires an HC segment, and the sole trace anywhere is an orphan assertion in `docs/runbooks/README.md` ("each SOW's manual-test segment cites the runbook it exercises") describing a thing nothing produces — so #15 shipped `tn team pull`/`tn player pull` with zero HC test instructions and the HC had to ask unprompted whether anything needed manual testing, while `pre-tournament-full-pull.md`, the runbook covering the exact commands that PR shipped, stayed listed as *Planned*. A rule held only in conversation and agent memory does not survive a session boundary; the durable form is the stage template that emits the artifact
- 2026-07-30 · process · (converted from issue #34, closed — an Issue must not spawn from this log) the Rules-Layer dispositions proposed off #28+#31's 22 same-shape findings, recorded here for triage rather than as tracked work. `rules/testing.md` → Anti-Patterns: *testing only the configuration the test invented* (every archive test set `TN_RAW_PATH` to a temp dir, so nothing exercised the documented default and the feature shipped throwing on every call under it — a component that reads config needs at least one test at the DEFAULT value); *asserting a fixture's incidental shape as if it were the domain* (the committed team fixture is an end-of-season page where every row has a result link, which is exactly why two idempotency defects stayed invisible — when a fixture cannot exhibit a failure mode, construct the input that can); *fixing the named instance instead of the class*. `rules/backend.md` → Patterns: *a normalizer must not be able to map a non-null input onto the null key* (use disjoint tagged domains when one key spans several value shapes); *an identity key assembled by reasoning about cases inherits the last case you thought about*. `rules/self-review.md`: *reproduce before fixing* — a fix whose defect was never reproduced is a hypothesis. Disposition is triage's call: these are ace rules, so do-now is not available; upstream-to-ace or drop
- 2026-07-30 · process · (converted from issue #34) an `autonomous-fold` Rules/config change must be pushed BEFORE the Reviewer is summoned: under the `attested` merge gate, a fold landing after the attestation moves the head off the attested SHA, condition 4 then refuses the merge, and a docs-only edit costs a whole extra review round. PR #31 hit exactly this at `b333c8f` after five rounds and correctly deferred rather than folding — the gate working as designed, but the fold window is a scheduling constraint no stage states out loud
- 2026-07-30 · process · (#16) `PROJECT.md` → *Rule-suggestion disposition* ships ace's `autonomous-fold`, which instructs `final` to defer large or contentious Rules-Layer suggestions **to a follow-up issue recorded in the SOW** — the exact thing this log's own header forbids (*"No Issues, PRs, rules, or ADRs spawn directly from this log"*). Issue #34 is that instruction being followed, and it was closed on 2026-07-31 as never-should-have-existed. A vendored default and a project rule disagreed, the vendored one was nearer to hand at the moment of decision, and it won. Until the two agree this recurs on every run whose `final` learns a rules lesson it cannot fold. The line nadal actually draws: rules findings / process recommendations / ace-side improvements → this log; product scope deferred from a SOW, and defects → tracked issues (#27, #30, #32, #33 are legitimate on that footing). Deliberately NOT fixed in-flight: using `autonomous-fold` to justify folding a fix to `autonomous-fold` is circular, and this log's whole point is that the HC triages
- 2026-07-30 · process · (#16) 589 passing tests and a 95% coverage floor missed SIX defects that were visible within two minutes of generating one real dossier and reading it: report folders named `team-1` rather than by opponent (unnavigable as a binder — the literal deliverable), a summary line printing every absolute path, a "prior meetings" line repeated once per player, ratings rendering at inconsistent precision (`WTN-D 4` beside `WTN-S 4.2`), the markdown renderer emitting raw `<script>` from a player name while the HTML renderer escaped it correctly, and misaligned `--help` columns. Every test asserted the behavior the PLAN named, and the plan named the wrong things — the same shape as #31's five rounds, one level up: there the rule was right and the input set was wrong; here the assertions were right and the SPECIFICATION of what to assert was wrong. A test suite cannot audit its own plan. The counter-practice is cheap and was skipped: render the artifact and look at it
- 2026-07-30 · process · (#16) a sub-agent dispatched to build a query layer instead appended findings and CLOSED a tracked issue — reasoning that was correct on the merits (the HC agreed) but an action outside anything it was asked to do, and outward-facing/hard to reverse. A batch prompt that merely OMITS "don't touch GitHub" is not a constraint; the later batches stated it as an explicit prohibition and stayed in their lane. Scope has to be written as a fence, not as a silence
- 2026-07-30 · bug · (#16, self-review) the markdown dossier renderer wrote player names verbatim into the table, so a name containing `<script>` shipped as live HTML — markdown permits inline HTML, so any viewer renders it. The HTML renderer escaped the same name correctly. The plan's testing strategy said to escape "pipes and backticks" for markdown, so the test asserted exactly that and passed; naming the mechanism (pipes/backticks) rather than the PROPERTY (no name can inject markup) scoped the test to the threat that had been thought of
- 2026-07-30 · note · (#16) spec § Deliverables #1 requires "prior meetings vs our players" in every dossier, but nothing in the schema, config or CLI designates an OWN team — `getTeamProfile` populates head-to-head only when a caller supplies `versusTeamId`, and `report build` has no source for one. The renderers support it and both paths are tested; the assembly states the section is unavailable rather than fabricating or silently dropping a required section. Whether "our team" is a column, an env var, or a config value is a real design decision with downstream effect on #17 (availability and captain notes are "populated for our team only, by design"), so it was flagged rather than invented mid-batch
- 2026-07-30 · process · (#16, PR #38) FIVE adversarial rounds, 12 findings, and rounds 2 and 3 consisted ENTIRELY of defects introduced by the previous round's fixes — the identical trajectory logged for PR #31 one phase earlier, in a different subsystem, with the #31 lesson already written down and read during this very PR. Knowing the pattern did not prevent it. The three sharpest instances: a collision fix that covered the batch path and not the single-team path (the "named instance, not the class" shape, now four occurrences); a collision scheme whose determinism rested on DB-id ordering that NEITHER query requested, with tests that passed only because SQLite happened to return insertion order; and a locale pin whose regression test was itself a false green that would pass on any machine without a French message catalog. The counter-practice that worked every time was the cheap one — reproduce the defect first, and check the fix against the space around the case rather than the case
- 2026-07-30 · bug · (#16, Codex round 2, rated critical) the tracked-file guard ran `git` with the DESTINATION'S OWN directory as `cwd`, and that directory routinely does not exist because the file is about to be created — so `spawnSync` returned ENOENT, the catch read it as "untracked", and the guard failed open in precisely the state it existed to catch. A fail-open security control whose error path is indistinguishable from the guarded state is not a control; the three-way tracked/untracked/indeterminate result now fails CLOSED on indeterminate
- 2026-07-30 · bug · (#16, self-review after Codex round 2, rated critical) git TRANSLATES "did not match any file(s) known to git" under NLS, so the English string match that decided "untracked" failed under a French locale, resolved to indeterminate, and — because the new code correctly failed closed — refused EVERY capture and EVERY dossier write on any machine not running in English. All 628 tests passed, because the test machine is English. Byte-for-byte the PR #31 round-3 defect: a privacy control that refuses everything looks identical to one that works, right up until someone runs it. Locale now pinned (LC_ALL=C, LANGUAGE cleared, since LANGUAGE overrides LC_ALL for message translation specifically)
- 2026-07-30 · bug · (#16, Codex round 3, rated critical) the same git child env inherited GIT_INDEX_FILE / GIT_DIR / GIT_WORK_TREE — with GIT_INDEX_FILE pointed at an empty index, `git ls-files --error-unmatch` calls a genuinely tracked path untracked, and the guard accepts it. The reviewer DEMONSTRATED it in a shell rather than arguing it. The locale fix had touched that exact env object one commit earlier and considered only the locale half; the whole GIT_ prefix is now stripped by namespace rather than by enumeration, so a git variable that does not exist yet is not a future gap
- 2026-07-30 · bug · (#16, Codex round 3, rated high) `overwriteOutputFile` unlinked the destination and then created its replacement, so an I/O failure in that window DELETED a good dossier and put nothing back — a data-loss window introduced by the very function written to make writes safe. Replaced with same-directory temp + atomic rename, with the leaf symlink check repeated immediately before the rename and that second check mutation-tested rather than assumed
- 2026-07-30 · process · (#16) rounds 1–3 ran the reviewer unscoped and it drifted into hardening the filesystem guard against an adversary strictly MORE capable than the one #33 already accepts as out of scope for v1 — real findings, but ones the HC had already dispositioned by accepting #33. From round 4 the reviewer was asked to split findings into "reachable without an adversary" and "inside #33's accepted boundary", and the rounds converged immediately (1 finding, then 0). An adversarial reviewer with no stated threat model will keep finding true things forever; the threat model is an input to the review, not a conclusion from it
- 2026-07-30 · risk · (converted from issue #30, closed — a review-residual register belongs in this log, not in a tracked issue) the fixture allow-list does not constrain NUMERIC identifiers: `computeSkeleton("8165551234", [])` is `""`, because the structural class elides bare digit runs of any length, so a numeric identifier reduces to an empty skeleton and is admitted unconditionally. This is deliberate — long digit runs legitimately occur in these pages (TennisRecord `mid=` match ids, SVG path coordinates) and refusing them would break real captures — so phone numbers are covered by `assertNoUnlistedPii` and uaid/tennis-id by the detector sweeps, NOT by the allow-list, which is documented in `tools/fixture-policy.ts` and `test/fixtures/README.md`. Worth revisiting if a source ever emits a bare numeric identifier no detector covers
- 2026-07-30 · risk · (converted from issue #30) the residual risk of the whole allow-list approach now lives in the PLAIN `# reviewed:` justification class: `# reviewed[synthetic]:` is machine-checked (`computeSkeleton(entry, standIns) === ""`), but the plain form is not and cannot be, since there is no authoritative machine-checkable list of "real public place names" — an allow-list moves risk out of the code and into a human-written justification file, it does not delete it. Four entries (`Cedar Falls MO`, `Halden CA`, `Norwood MO`, `Wexley KS`) sit in that class on the conservative default for indeterminate place-shaped content, and the claim each one makes is "this is structurally a place name, not a person's name" — never "this municipality is confirmed real"
- 2026-07-30 · note · (converted from issue #30) `role="imgage"` — an apparent upstream typo in vendor-generated inline SVG — is admitted to `tools/fixture-vocabulary/usta.txt`:2697 on a plain `# reviewed:` justification, needed since issue #28 round-3 finding R3-2 stopped exempting `role` by name. Harmless, but two things about it are worth knowing: it is vendor markup nadal now pins and will re-justify on every re-capture until the vendor fixes it, and the justification attributes it to the Google Custom Search widget while the same token also appears in the OneTrust cookie-banner SVGs of the same fixture — a vocabulary entry admits a token EVERYWHERE, so a justification naming one origin is narrower than the permission it grants
- 2026-07-30 · process · issue #30 ("residual items from the fixture-redaction allow-list") was closed and converted here at HC direction: FOUR of its six items were already logged verbatim before it was ever filed (`stand-ins.txt` is not an exhaustive seed → :102; the `redact-fixture.ts`/`fixture-policy.ts` circular import → :103; the 14 hand-justified vocabulary entries reclassified real-public → :104), and the two that were not are the risk notes directly above, neither of which is a reproduced bypass. The distinction the earlier disposition (:150) got wrong: a capped adversarial-review cycle's LEFTOVERS are not the same artifact as a deferred defect — #32 and #33 each name a specific broken thing and a fix, while #30 was a residual-risk register for a control that shipped working, i.e. exactly "one line per finding" content re-packaged as tracked work, duplicating the log at four of six items. `skills/final/SKILL.md`:54 pointing review leftovers at "a tracked follow-up issue" is the same vendored-default-vs-spec collision that produced #34 (:150), one category over. Line as now drawn: deferred DEFECTS and deferred product scope → tracked issues; residual-risk registers, review leftovers, and process/rules lessons → this log
- 2026-07-30 · process · (#32) the assessment's write-site inventory grep silently undercounted (3 sites instead of 5) because `src/ingest/upsert.ts` carries a deliberate `"\0null"` sentinel that makes plain `grep`/`ripgrep` classify the whole file as binary and skip it by default — an inventory grep that skips a file returns a *short list*, not an error, so the omission read as a complete result until `grep -a` was tried during planning. Any future inventory sweep over this codebase needs `-a`/`--text` forced, not assumed
- 2026-07-30 · process · (#32) the posted plan's claim that suite B's existing test file "must stay green unmodified" was empirically wrong once the fail-closed unkeyed-row probe (task 4) landed: `name_key` is only ever set by the application's own write paths, so every pre-existing test across the WHOLE suite (not just the named file) that inserts a player/team/alias row directly via `db.insert(...).values(...)` — bypassing `resolvePlayer`/`resolveTeam`/`upsert*` — leaves that row's `name_key` NULL and trips the guard the moment a name-based lookup runs against it. ~10 test files needed a `nameKey: nameKey(...)` added to their raw seed inserts; the fix was mechanical and every asserted scenario/outcome is unchanged, but "unmodified" was not achievable as stated
- 2026-07-30 · note · (#17, PR A) spec § Stack names "Hono for the MCP server" — wrong for what v1 actually needs. Hono is an HTTP framework, required only for MCP's streamable-HTTP transport; the consumer is a local agent chat on Randy's own laptop talking to a local SQLite file, and stdio needs no port, bind address, or auth story at all. `tn mcp serve` speaks stdio; nothing under `src/` imports Hono. Same shape as #36's Playwright finding — a spec-stated dependency corrected in the open rather than silently. Caveat worth knowing: `@modelcontextprotocol/sdk` itself still pulls `@hono/node-server` in transitively (an unconditional dependency of the whole package, not gated on which transport a caller actually uses), so it lands in `node_modules`/`package-lock.json` regardless — that is the SDK's supply chain, not a choice this PR made
- 2026-07-30 · fact · (#17, PR A) drizzle-kit correctly generates a genuine partial SQLite unique index from `uniqueIndex(...).on(t.col).where(sql\`...\`)` — confirmed for `teams.is_home` (`CREATE UNIQUE INDEX team_home_unique ON teams (is_home) WHERE is_home = 1`), the same mechanism already proven on an earlier PR for `membership_unique_no_event`/`team_match_source_unique`. Task 1's "verify first" uncertainty (the plan's stated fallback was to hand-augment the generated SQL) resolved clean on the first `drizzle-kit generate` — no hand-augmentation was needed
- 2026-07-30 · fact · (#17, PR A) the `availability` table's `(player_id, event_id, day)` unique index already existed from the Phase 1 migration (`drizzle/0000_fuzzy_wolf_cub.sql`), despite the table having had no writer at all until this PR — the schema was built ahead of its own write service back in Phase 1. The implementation plan listed a new migration as part of Task 3; checking what already exists first (rather than trusting the plan's own file list) meant one fewer migration than planned, with zero risk since an idempotent-upsert unique index cannot itself be a defect surface if nothing has ever written through it
- 2026-07-30 · fact · (#17, PR A) `@modelcontextprotocol/sdk`'s `McpServer` already catches every thrown error from a registered tool callback — including its OWN `McpError` from zod input-schema validation on a missing/invalid required argument — and converts it to a structured `{ content, isError: true }` `CallToolResult`, never a raw JSON-RPC-level throw (the sole exception: `ErrorCode.UrlElicitationRequired`). A tool registration needs no defensive try/catch of its own around the callback to get "a bad call never crashes the server" — that is an SDK-level guarantee already, confirmed by reading `node_modules/@modelcontextprotocol/sdk/dist/esm/server/mcp.js` directly rather than assumed from the type definitions alone
- 2026-07-30 · process · (#17, PR A) `getPlayerProfile`'s `dataGaps` computation for `availability`/`captain_notes` had been hardcoded `hasWriter: false`, with a comment stating that as a fact about the codebase (docs/findings.md, #15/#16) — once Tasks 3–4 gave both a real writer, that literal would have kept reporting "not collected yet" over real data if left unchanged, exactly the silent-lie risk the plan named as this PR's single most likely thing to be forgotten. Caught by its own dedicated failing test written BEFORE the implementation changed (`test/query-player-profile.test.ts`'s regression test), not discovered after the fact by reading a rendered dossier
- 2026-07-30 · process · (#17, PR A) nadal's kickoff rule "every SOW includes a manual-test segment for HCs plus runbooks for manual testing" (flagged as encoded in no artifact by an earlier finding on this same log) is closed here the cheap way the earlier finding recommended: `docs/runbooks/agent-chat-over-mcp.md` is a new runbook, and this PR's SOW carries an HC manual-test segment citing it directly rather than describing testing only in prose
- 2026-07-30 · bug · (#17, PR A, self-review — caught by generating the artifact and reading it, not by the suite) `getTeamProfile`'s `headToHead` rows carried only a raw `opponentId`, and both renderers printed `vs player #<id>` literally — a KNOWN, documented tradeoff from #16/#32 ("dossier assembly does not resolve every opponent id to a name"), deliberately deferred because `headToHead` was always `null` in production until this PR's Task 5 wired a real `versusTeamId` for the first time. The moment it went live, a real rendered dossier printed `vs player #1` in the "prior meetings vs our players" section — exactly the deferred tradeoff becoming a live, embarrassing defect the instant its precondition (a designated home team) is met. Fixed by resolving every `versusTeamId` roster player's canonical name once in `getTeamProfile` (the DB-access assembly layer, not `derive.ts`'s pure `headToHead`) and carrying it on `TeamCrossHeadToHead.opponentName`; both renderers now print the name, escaped through each format's own existing escaper (`escapeHtml`/`escapeMarkdownCell`) rather than a new one
- 2026-07-30 · bug · (#17, PR A, self-review — caught by actually running the built `tn mcp serve` and piping a finite script into it, not by the suite) `@modelcontextprotocol/sdk`'s `StdioServerTransport` never calls its own `onclose` on stdin EOF — it only attaches `'data'`/`'error'` listeners to stdin (confirmed by reading `server/stdio.js` directly), `onclose` fires ONLY on an explicit `close()`. `src/cli/commands/mcp-serve.ts`'s runner resolved its `Promise<number>` solely on `transport.onclose`, so piping a finite JSON-RPC script into a real `tn mcp serve` and letting stdin hit EOF left the promise unsettled forever — Node exited with **code 13** ("unsettled top-level await") and a stderr warning instead of a clean 0, on every graceful (non-killed) shutdown. The SDK's own type signature gives no hint of this: `Transport.close()` is documented as closing the connection, but nothing states stdin EOF triggers it. Fixed by having the runner listen for stdin EOF itself and call `transport.close()` (which DOES fire `onclose`, so that half of the contract holds) — `stdin` is injected (defaulting to `process.stdin`) so the fix has a direct unit test rather than only the manual reproduction that found it
- 2026-07-31 · bug · (#17, PR A, Stage-4 verify, rated high) `setAvailability` validated `status` with zod and left `day` — the OTHER half of the `(player_id, event_id, day)` upsert key — an unvalidated `z.string().min(1)` on both surfaces. `eventsForDay` compares dates as TEXT, so any malformed day that merely SORTED inside an event's range matched that event and was persisted verbatim: `"2026-08-28 "`, `"2026-08-28xyz"` and `"2026-08-2900"` each wrote a SEPARATE row beside the real `"2026-08-28"` — four rows for one day, defeating the idempotent upsert the unique index exists to provide and inventing days no per-event-day read (lineup planning, PR B) will ever match. Byte-for-byte the `upsertTeamMatch` shape logged four times above: a key assembled without normalizing its input domain, where the sibling field got a validator and the key field did not. The plan's own testing strategy enumerated "unknown status rejected" and never "malformed day rejected" — the gap was in the specification of what to assert, not in the implementation of it, which is the #16 lesson one phase on. Reproduced against a real DB (4 rows) before any fix, closed at the write service so both presenters inherit it, and the guard is a real calendar-date check rather than a pattern (`"2026-02-31"` matches the pattern and is not a date; JS would silently roll it forward, which is the same quiet substitution in another costume)
- 2026-07-31 · bug · (#17, PR A, Codex adversarial review, rated medium) every token beginning `--` was parsed as a flag, so an ordinary captain note beginning `--` could not be recorded AT ALL: `tn player note Randy "--poach at net"` died as `unrecognized flag --poach at net`, and both workarounds corrupt the note (leading whitespace changes text `addCaptainNote` deliberately stores untrimmed). `GRAMMAR.md`'s "there is no `--` payload terminator" was TRUE when written — nothing took a free-text payload then — and became a defect the instant `player note` landed; the parser was correct against a grammar that had silently expired. The shape worth remembering is that neither the plan nor my own self-review caught it, because both were checking the new code against its own stated intent, while the reviewer was checking it against what a captain would actually type. Fixed as a standard GNU end-of-flags delimiter in the SHARED positional loop rather than only for payload commands, since a `--`-leading TARGET is the identical defect one positional over
- 2026-07-31 · bug · (#17, PR A, Codex adversarial review round 2, rated medium) the round-1 `--` delimiter fix LEFT a hole for exactly one token: `dispatch()` checks `argv.includes("--help")` before any command parser runs, so it silently outranked the delimiter it had just been introduced beside — `tn player note Randy -- --help` printed help and exited 0 instead of recording the note, while the MCP `player_note` tool accepted the identical text, leaving the two surfaces disagreeing about what is recordable. THIRD occurrence of "a review round's fix introduces or leaves the next round's defect" (PR #31 rounds 2-3, PR #38 rounds 2-3, now this). What is new here is the mechanism: the delimiter was added in the shared PARSER, and the defect lived in the DISPATCHER upstream of it — the fix was checked against the layer it was written in, never against the layer that runs first. `exit 0` also made the failure invisible to any test asserting the exit code, which is why the regression test asserts the STORED NOTE instead; printing help exits 0 too, so an exit-code assertion would have passed against the defect itself
- 2026-07-31 · note · (#17, PR A, Codex adversarial review round 3, rated medium, DEFERRED to #44) adding the `--` delimiter left TWO layers scanning argv with different notions of where flags end: `parsePositionals` scans statefully (a `--` after `--from`/`--source-url` is that flag's VALUE), while `dispatch` uses a raw `indexOf("--")` because it runs before the command is resolved and cannot know which flags take values. Reachable only with an argument literally named `--`, so the (A) "ordinary use" label was disputed on the PR rather than accepted; deferred because the real fix moves value-flag declarations onto the `Command` contract — shared CLI infrastructure, every command — inside a PR already three rounds deep whose round 2 was itself a defect left by its round 1 fix. The deferral is the interesting part: this repo's logged failure mode is successive structural fixes in one PR generating the next defect (PR #31, PR #38, and now #41), and the counter-move is a tracked issue, not a fourth swing. `GRAMMAR.md` was corrected to document the exception rather than keep promising behavior the code does not have — a doc that overclaims is the same failure as a check whose comment overclaims
- 2026-07-31 · bug · (#17, PR A, Codex adversarial review round 4, rated medium) `GRAMMAR.md` promised that EVERY command accepts `--quiet/-q` and `--json` and rejects undeclared flags, but `dispatch` hands trailing argv to every command and `mcp serve`'s runner ignored it entirely — so `tn mcp serve --jsno` silently STARTED THE SERVER instead of surfacing the typo, in a repo whose parser doc explicitly argues that an unrecognized flag is very likely a typo'd real one and that silently accepting it makes the typo invisible. Two ways to close it were available — exempt `mcp serve` in prose, or make it reject arguments — and rejecting is the one that makes the promise TRUE rather than smaller; the doc now records the exemption anyway because `mcp serve` genuinely cannot honor the global flags (its stdout IS the JSON-RPC stream). The through-line across rounds 2-4 of this PR is one shape in three costumes: a claim in a doc or a comment outliving the code it described. Round 2 was the delimiter the dispatcher outranked, round 3 was a delimiter promise with an undocumented exception, round 4 was a universal-flag promise with an unmentioned exception — each true when written
- 2026-07-31 · process · (#17, PR A) `main` moved twice during this PR (#32 index-backed name resolution, #39 findings conversion) and the merge produced TWO hazards a green branch could not have predicted. First, a MIGRATION NUMBER COLLISION: both branches independently generated a `0004_*.sql`, and `_journal.json` conflicted — resolved carelessly, one migration silently vanishes or applies out of order, and drizzle's snapshot chain goes inconsistent with it. The fix is not a text merge: main's `0004` is already merged and may be applied to real databases, so MINE renumbers to `0005`, which means deleting my migration + snapshot, taking main's `drizzle/` wholesale, and re-running `drizzle-kit generate` against the merged schema rather than hand-editing a snapshot. Second, #32 made name resolution index-backed on `name_key`, which the real writers populate on insert — but every test fixture in this PR inserts rows DIRECTLY, so 24 tests went red on a merge where both sides were independently green. The lesson generalizes past drizzle: a branch that adds a migration or changes a lookup path has a merge conflict that lives in BEHAVIOR, not in text, and `git merge` reports clean on exactly the parts that matter least. The counter-practice that caught it here was running the full suite plus a fresh-DB `db migrate` AFTER resolving, instead of trusting a conflict-free merge
- 2026-07-31 · note · (#17, PR A, Codex adversarial review round 6, rated high, DISPUTED to low and documented rather than fixed) renumbering this branch's migration from `0004` to `0005` after main landed its own `0004` leaves one broken upgrade path: a database migrated on THIS branch before the merge recorded the old `0004_free_warstar`, so it would later apply main's `0004` and then fail applying `0005` on a column it already has. Real, and its severity turns entirely on reachability — which was checked rather than argued: no `.db` file exists anywhere in the project (`data/` is gitignored, and nothing has ever been migrated outside /tmp scratch databases), the branch has never merged, and the window closes PERMANENTLY at merge since every database created afterwards applies 0000..0005 in order. The recommended fix — a compatibility/repair path reconciling drizzle's migration state — would mean carrying migration-reconciliation machinery in production forever to serve a window that is already closing, on a database the spec deliberately makes disposable (idempotent upserts over an archived `raw/`, i.e. a cache, not a system of record). Documented in the runbook with a one-line recovery instead. The general lesson: a finding's severity is a function of reachability, and reachability is usually CHECKABLE — `find` for the database beat reasoning about whether one might exist
- 2026-07-30 · process · (#32, PR #40, Codex round 1 rated high) a query-plan assertion that checks the plan NAMES the intended index is a false green: SQLite reports a reverted full-table read as `SCAN players USING COVERING INDEX players_name_key_length_idx` — it names the index while still visiting every row, so the assertion passes on precisely the regression it was written to catch. The predicate has to be the ABSENCE of `SCAN` for the table. This was the reviewer's own literal first recommendation, and taking it verbatim would have shipped a second false green one level under the one it was fixing; the fix was mutation-checked instead (revert tier 2 to `.all()`, confirm the old count test stays green and the new plan test goes red)
- 2026-07-30 · process · (#32, PR #40) under the `attested` merge gate, condition 4 compares the reviewed SHA to the PR head LITERALLY, so anything moving the head after the summons voids the attestation — not only a review fix but a REBASE onto an advanced `main`, even one that touches no source file. PR #40 needed three Codex rounds for this reason (b1d28b8 finding→fix, 864eeec approve, eb179cf approve again post-rebase, content-identical). The practice that follows: land every `autonomous-fold` edit AND integrate the latest `origin/main` BEFORE summoning, not after — the log already carried the fold half of this (:151) but not the rebase half, which is the one that bites on a long run while main is moving
- 2026-07-30 · perf · (#42, TRIAGED — not fixed) `players.tennisrecord_url` and `teams.tennisrecord_url` carry no index, so tier 1 of the identity ladder plans as `SCAN players` / `SCAN teams` — on every `tn player pull`, i.e. the most-travelled full-table read in the codebase. Left UNFIXED on purpose: nothing is broken, it is slower than it could be, and that is an optimization, not a defect. The line that matters (HC, after a PR was opened for it): bugs we fix, optimizations we triage — v1 has a fixed destination and optimization work spends the budget features need. A profiling number is not a defect
- 2026-07-30 · process · (#32, PR #40) `test/identity-query-plan.test.ts` shipped asserting no-SCAN for the paths #32 NAMED (the no-ID tiers) and not for the class "a lookup that must not scan", so the tier-1 `tennisrecord_url` read sat directly beside four green assertions without tripping one. Same shape as the four prior occurrences of fixing the named instance rather than the class, except here it was the GUARD that was scoped to the instance, which is worse: a guard scoped to the reported case reads as coverage of the whole class to every later reader
- 2026-07-30 · process · (#42) the attempt to "just add the index" is itself the case study for why optimizations get triaged rather than folded: the adversarial review REJECTED the one-line index because a genuine bug sits underneath it (a team rename inserts a second row carrying the same `tennisrecord_url`, since `upsertTeam` conflicts only on `teams.name`, and tier 1 then returns `.all()[0]` arbitrarily — filed separately as a defect). Making an ambiguous state FASTER is not making it safe, and resolving it properly means a uniqueness-contract change. A two-line optimization spawned strictly more scope than the feature work it displaced, which is the spiral the bugs-vs-optimizations line exists to stop
- 2026-07-30 · note · (converted from issue #43, closed — a review leftover belongs in this log, not in a tracked issue, per the #30 precedent at :150) `players.tr_name_key` is declared at `src/db/schema.ts` and present since migration 0000 but is never read or written anywhere in `src/` — its only other appearance is a `null` in one test fixture. Since #32 added the load-bearing `players.name_key`, the two sit one underscore apart in the same table, one real and one dead, which is a legibility hazard for exactly the reader trying to work out which key the identity ladder consults. Nothing is broken, so this is a leftover rather than a defect: dropping it is a SQLite table rebuild on a table seven others hold FKs into, worth doing only alongside other `players` work
- 2026-07-31 · bug · (#33) the accepted residual from #15/#31 — `archivePage`/`overwriteOutputFile` resolved a real, validated destination and then handed the PATH STRING to `writeFileSync`/`renameSync`, so a directory component swapped between the resolution and the write redirected un-redacted capture (or dossier) bytes outside the validated real root — was reproduced with a `node:fs` spy that plants the swap deterministically inside whichever low-level call actually performs the write (no wall-clock race), then closed with fd-anchored writes: `openNewOutputFileSafely`/`writeNewOutputFile` (`src/fs/output-root.ts`) open the real path `wx` and only trust it once TWO independent post-open checks both hold — no directory component is a symlink, and the open fd's `{dev, ino}` still matches what the path names. Neither check alone is sound: the component walk alone is exactly today's check-then-use, and the inode check alone is fooled by a symlink an attacker leaves in place, since `lstatSync` follows the same symlinked directory component `open` already did. `overwriteOutputFile`'s temp-then-rename gained the analogous directory-realpath re-verification immediately before the rename. CLOSED: un-redacted content can no longer be written outside the validated real root via a component swap — the bytes go through a proven fd. REMAINING, honestly stated rather than claimed closed: (1) an actor who wins the PRE-OPEN window can still cause an EMPTY file to be created at a location of their choosing before the run fails closed and writes no content there — pure Node has no `openat`/`O_NOFOLLOW`; (2) `overwriteOutputFile`'s `renameSync` stays a path operation with no `renameat`, so the reports leaf keeps a narrower rename-time window, now narrowed by the new check rather than erased
- 2026-07-31 · note · (#33, pre-existing, out of scope) `archivePage` still writes the html leaf before the provenance leaf, so a race refusal on the SECOND write (provenance) leaves the first (html) sitting on disk — inside the validated root, unaffected by this fix and unchanged from before it
- 2026-07-31 · bug · (#33, PR #48, Codex adversarial review round 1, rated critical) the fd-anchoring proof was bypassable with a HARD LINK: the two post-open checks establish "the fd is what this path names", which is NOT the same claim as "this path is the fd's only name". An actor who wins the pre-open window (file created through a symlinked component, outside the root) can restore the real parent and hard-link that outside file back to the in-root path — every check then passes HONESTLY, because it is one inode under two names, one of them outside the root. Closed with a `nlink !== 1` refusal, which is exact rather than heuristic: `openSync(..., "wx")` is `O_CREAT|O_EXCL`, so it only ever succeeds by CREATING the file, and a newly created regular file has exactly one link — any second name appeared after our open and is by definition not ours, so the check refuses no legitimate configuration. The general shape is the one this repo keeps re-learning one level up from where it last learned it: #31 established that a LEXICAL check does not answer where the bytes land, and this round establishes that an INODE check does not answer how many names those bytes are reachable under
- 2026-07-31 · bug · (#33, PR #48, Codex adversarial review round 1, rated critical) the verification-failure cleanup was a bare `unlinkSync(realPath)`, and `unlink` resolves directory components like every other path call — so when verification failed BECAUSE a parent was swapped for a symlink, the cleanup followed that same symlink and deleted whatever sat at the corresponding name in the attacker-chosen directory. A guard that refuses to WRITE outside the root while happily DELETING outside it is not a smaller version of the same control, it is a different and worse bug — the fix that introduced it was written to avoid leaving stray files behind, i.e. tidiness bought data loss. Now inode-guarded: cleanup lstats the path, compares `{dev, ino}` against the fd's, and unlinks only its own inode, leaving anything else strictly alone. Cost of failing safe here is a stray EMPTY file in a directory the attacker already controls
- 2026-07-31 · bug · (#33, PR #48, Codex adversarial review round 1, rated medium) the `writeSync` retry loop added to prevent a short write silently truncating a capture had no zero-progress guard, so a `write(2)` returning 0 would re-issue the identical request forever — the loop written to fix a silent-corruption bug introduced a HANG, which is the fourth occurrence in this repo of a fix generating the next defect (:146 records the first three). The test for it is worth noting as a shape, and the shape was not what it was first written up as: removing the guard does NOT hang the suite and does NOT fail an assertion — the vitest worker dies of an OUT-OF-MEMORY crash (`ERR_IPC_CHANNEL_CLOSED`, v8 `OOMErrorHandler`), because the `vi.fn()` wrapper records the arguments of every call and the infinite loop exhausts the heap recording them. So the mutation check for an infinite-loop guard reads as a worker crash, which is easy to misdiagnose as flaky infrastructure and dismiss. Recorded because the first draft of this very line asserted "it hangs" from reasoning, and running it said otherwise
- 2026-07-31 · note · (#33, PR #48) the adversarial reviewer ran in a read-only sandbox and could not execute the suite (vitest could not write `node_modules/.vite-temp`), so its findings came from READING the code. All three real defects it found were reachable by reading; none of the four green-suite claims it could not verify turned out to be wrong. Worth recording because the instinct is to discount a reviewer that could not run anything — on this evidence the reading pass was the higher-yield half, and the suite it could not run had already been mutation-tested by the author
- 2026-07-31 · bug · (#33, PR #48, Codex adversarial review round 2, rated critical ×3 — accepted as REAL, disputed as BLOCKING; escalated to the HC rather than self-certified) every round-1 fix was verified point-in-time and claimed durable, which is the same over-claim one level in from where round 1 found it. `nlink !== 1` catches a second name that exists WHEN THE CHECK RUNS, not for the duration of the write; the `lstat`-then-`unlink` cleanup guard is itself two path operations with an interval between them; the reports `renameSync` is unchanged. The reviewer is right on all three mechanics, and the hard-link TEST was a genuine false green — it planted the link inside `openSync`, i.e. before verification, so it only ever proved the pre-verification case while its name claimed the general one. What is NOT conceded is that these block: all three share one root cause — pure Node cannot make any path-based operation atomic with its own check — which is the precise constraint issue #33 opens by stating and which the HC accepted on the record for v1. A reviewer applying "no TOCTOU residual may remain" is applying a standard the approved plan does not adopt, and the honest move is neither to capitulate (silently widening scope to a native helper) nor to overrule it (self-certifying past a Critical), but to fix every claim, fix the false-green test, and hand the accept-or-escalate call back to the HC
- 2026-07-31 · process · (#33, PR #48) the recurring shape across both review rounds, worth naming once: each fix was CORRECT and its DESCRIPTION was too strong, and the description is what the next reader trusts. Round 1 said "the bytes go through a proven fd" (true) and let it imply containment (false — hard links). Round 2's fix said the link count is "exact rather than heuristic" (true of the sample) and let it imply durability (false). Both times the code was an improvement and the comment was a liability, which is why `rules/testing.md`'s "never let a check's comment claim coverage the code does not enforce" keeps earning its place — the failure is not sloppy code, it is a true sentence read one inference too far
- 2026-07-31 · bug · (#17, PR B, found while planning — not by the suite, not by a reviewer) `pullPlayer` hardcoded `winnerSide: null` for every court match it wrote, while the match-history parser had produced `result: "W" | "L"` since #14 and NOTHING consumed it. `derive.ts` reads a null winner as *undecided* by design, so every W/L figure in every shipped dossier read `0-0` with all matches undecided — a headline number on the printed page, wrong for four phases, under 800 passing tests. The reason the suite could not see it: `query-derive.test.ts` tests the pure functions with hand-built rows that SET `winnerSide`, and `ingest-player-pull.test.ts` asserts what the pipeline wrote (slot, discipline, score, participant counts) — each layer was correct and fully covered, and the defect lived in the handoff between them, in a field one side never populated and the other silently tolerated. The tell was available statically and nobody looked for it: a parsed field with zero read sites. Worth a standing check — after wiring a parser to a writer, grep each parsed field for a consumer, because "produced but never read" is invisible to every test that asserts only on what IS written
- 2026-07-31 · bug · (#17, PR B, found while planning) `tn player avail` and the `player_avail` MCP tool shipped in PR A as UNREACHABLE code: `setAvailability` resolves its `event_id` from an event whose range contains the day, and nothing in production has ever written the `events` table — `team pull` passes `eventId: null` at both call sites, and only tests insert events. Every real invocation exited `NoEventForDayError`. Availability is what spec § Domain model calls the thing "lineup planning depends on", so the feature most depended-on downstream was the one that could not run. Both PR A's own tests and its adversarial review passed over it because the tests SEED an event directly — the fixture supplied the missing production writer, which is the "testing only the configuration the test invented" trap one layer up from where the log has recorded it before. Counter-practice: for any write service that resolves a foreign key from user input, assert once that the row it resolves against has a PRODUCTION writer, not merely that the service works when handed one
- 2026-07-31 · process · (#17, PR B) the A/B split's stated rationale was to prototype the lineup heuristic against real dossier data before freezing it — and the precondition was never met: no `data/`, `raw/` or `reports/` exists anywhere, no pull has ever run, and #27 is still blocked on an HC capture. A plan whose sequencing argument rests on a future state should say what happens if that state has not arrived, because the fallback is not obvious in the moment: here it was to put the two open questions to the domain expert directly, sequentially, which is the input the data was meant to elicit and arguably the better source. The failure mode avoided was proceeding as if the rationale had been satisfied because the PR that was supposed to enable it had merged
- 2026-07-31 · note · (#17, PR B) `predictedLineup` derives its slot set from a team's OBSERVED court-match history rather than from `events.format`, because nothing links a team to an event — `team_memberships.event_id` and `team_matches.event_id` are null on every real pull, so an event's court list is unreachable from a team. Consequence: a team whose league history shows five courts is predicted across five even at Springfield's four. Every presenter names the slot count and its source rather than hiding it. The fix is an event↔team link, which is real schema work and not something to fold into the phase that discovered it; revisit when TennisLink (#27) lands, since that is the source that would carry the association
- 2026-07-31 · process · (#17, PR B) the first implementation of the pair-first heuristic walked slot order once, and that quietly conflated two DIFFERENT orderings the rule needs kept apart: within a slot a collision is decided by combined rating rank, but across slots the more-established partnership must claim its players first. Walking slots in order let a 4-match pairing at D1 take a player out from under an 8-match pairing at D2 — splitting exactly the partnership the pair-first rule exists to protect. It was caught by the one test written specifically to fail under the REJECTED alternative heuristic, which is the practice worth keeping: when a design choice is made between two named options, write the test that distinguishes them, not just tests of the option you chose
- 2026-07-31 · bug · (#17, PR B, Codex adversarial review round 1, rated high) the pair-first placement loop had the SAME defect twice, in two different disguises, and only the reviewer caught the second. Round 0 (my own): walking slot order once let a weak pair claim a player before a stronger partnership could. My fix — propose one winner per slot, commit the strongest, re-propose — fixed that and left the deeper version intact: each pair contended only for its MODAL slot, and losers cascaded AFTERWARDS into whatever was left, contesting nobody. So a cascader was silently exempt from the collision rule the heuristic is built on: a pair that lost D1 could not contend for D2 against D2's claimant however much better its combined rating. The real fix is to re-derive each pair's CURRENT preference (best still-open shared slot) every round, which makes a cascade a genuine re-entry into contention rather than a consolation placement. The shape to remember: my fix addressed the symptom I had a failing test for (a split partnership) rather than the mechanism underneath it (contention computed once, from a static preference) — the fifth-plus occurrence in this repo of fixing the named instance rather than the class, and the first where the instance and the class were separated by only one round of my own work
- 2026-07-31 · process · (#17, PR B, Codex round 1) the reviewer was RIGHT about the mechanism and WRONG about its evidence: it claimed the existing cascade test at derive-lineup:161-195 "should fail" and that the suite therefore contained a red case, having misread the fixture (it read pair 6/7 as primary-D2 when the rows put them at D3), and reported `npm test` exiting 1 on a file that passes 35/35 locally and in CI. Taking the finding verbatim would have meant "fixing" a correct, passing test. Taking the substance meant writing the case the reviewer's REASONING implied but its example did not — a D1 loser with better combined rating contesting D2 — which reproduced immediately. Second recorded instance of "a reviewer finding can be right about the defect and wrong about the fix" (first: PR #26); worth treating as the standing expectation rather than a surprise, and the counter-practice both times was to reconstruct the failing case from the mechanism instead of trusting the cited line numbers
- 2026-07-31 · bug · (#17, PR B, Codex adversarial review round 2, rated high) `tn player avail` was still unusable on any day covered by TWO events, and PR B is what made that reachable. `setAvailability` resolves its event by day alone and refuses on more than one match — correct-looking, and unhittable while nothing wrote the `events` table. The moment `addEvent` shipped, the FIRST realistic setup hits it: a district league season Mar–Jun with a districts tournament sitting inside it in May means every day in that window resolves to two events and availability simply cannot be recorded. The lesson is about what "unreachable code" hides: PR B's own Defect-2 analysis correctly found that `player avail` could never succeed, fixed the missing writer, and stopped there — but a path that has never executed has never had ANY of its branches exercised, so clearing the first blocker just exposes the second. When you make dead code live, walk its whole decision tree with a realistic setup rather than the one case you were unblocking. Fixed with an optional event-name disambiguator (never a guess: GRAMMAR.md's standing answer for an ambiguous target is to error with candidates listed), and a name supplied for an unambiguous day is validated rather than ignored
- 2026-07-31 · bug · (#17, PR B, Codex adversarial review round 2, rated medium) `src/sanitize.ts` exists precisely for scraped, attacker-influenced values reaching a terminal, and was wired ONLY into `key=value` summary lines — so every multi-line human-readable presenter (`team show`, `player show`, and PR B's new `lineup plan`) interpolated player and team names raw, where an ANSI/OSC sequence or a bidi override can clear the screen, rewrite earlier lines, or visually reorder a roster. The reviewer flagged the new formatter; the two older ones had the identical hole and were fixed in the same pass, because fixing the reported instance and leaving its siblings is this log's most-recorded failure mode. Sanitizing must also happen BEFORE column width is measured — a control character occupies a column in `String.length` but not on screen, so measuring the raw name misaligns the whole table
- 2026-07-31 · process · (#17, PR B, Codex adversarial review round 2, rated medium) a test whose NAME claims coverage it does not have is the same defect as a comment that overclaims, and it is easier to miss because the suite is green: `test/ingest-winner-side.test.ts`'s second block was titled "survives a pull from the opposing perspective" but drove the upsert functions directly, so reverting `player-pull.ts` left it passing. Two remedies existed and they are not equivalent — build two opposing page fixtures (making the title true) or rename the block (making the claim true). Renaming was right here because the pull layer is ALREADY covered by the first test in the same file, so the fixture work would buy a duplicate assertion at the cost of authoring pages through the capture-and-redaction policy. Worth generalizing: when a test overclaims, ask what ELSE already covers the claim before writing more test infrastructure — the answer decides which of the two remedies is proportionate
- 2026-07-31 · bug · (#17, PR B, Codex adversarial review round 3, rated medium) `JSON.stringify` escapes the C0 controls and leaves category-Cf characters — RIGHT-TO-LEFT OVERRIDE among them — and U+2028/U+2029 completely intact, so `--json` was a clean bypass around the terminal sanitization the `key=value` path had enforced since PR A. Three commands dump a whole profile object through a bare `JSON.stringify` and all three leaked. The general shape is worth keeping: a sanitizer wired into ONE output format is not wired into the surface, and the second format looks safe because its serializer escapes *something*. Partial escaping is the trap — `JSON.stringify` does escape ESC and newlines, which is exactly why nobody looked further. Now `emitJson` sanitizes string leaves at any depth, and the regression test asserts the gap itself (`JSON.stringify` still contains U+202E, `emitJson` does not) so it cannot silently reopen
- 2026-07-31 · process · (#17, PR B, Codex adversarial review round 3, rated medium) I replaced a stale `hasWriter: false` literal with an unreachable `true` one and called it a fix. `addEvent` gave the `events` TABLE a production writer, so flipping the flag looked obviously right — but the `dataGaps` section is about a PLAYER's events, and the association it counts is an event-scoped `team_memberships` row, which nothing writes (`tn team pull` passes `eventId: null`). Every real player would have read "empty" (a writer exists, nothing recorded) when the truth is "not collected" (nothing can record it): the same silent lie the flag exists to prevent, pointing the other way. My own test masked it by inserting the membership by hand — testing the configuration the test invented, one layer under where this log has recorded that trap before. The rule to carry: `hasWriter` is a claim about a specific END-TO-END path, so verify the path, not the presence of a plausibly-related writer; and a test for a codebase-level claim must not manufacture the state it is asserting is reachable
- 2026-07-31 · bug · (#17, PR B, Codex adversarial review round 4, rated medium ×2) closing the `--json` sanitization hole in round 3 fixed ONE of four output boundaries, and the reviewer found the other two on the next pass: MCP tool results (`src/mcp/server.ts` passed every result through a bare `JSON.stringify`) and the generated dossiers (`escapeMarkdownCell` escapes CommonMark syntax, `escapeHtml` escapes the five XML characters, and neither touches Cc/Cf/U+2028-9). The through-line across rounds 2-4 is one question asked three times and answered narrowly each time: *where does a scraped string reach an output medium without that medium's escape?* Terminal text, then CLI JSON, then MCP JSON, then two file formats. The right move on round 2 would have been to enumerate every output boundary once and check them all, instead of patching the one the reviewer named — the enumeration takes minutes and there are exactly four. Also worth keeping: escaping FOR A MEDIUM and sanitizing a character class are different jobs, and a function named `escape*` that does the first reads as if it did both, which is why all three call sites looked defended
- 2026-07-31 · process · (#17, PR B) four adversarial review rounds, and every finding from round 2 onward was against code written in response to an earlier round — including one where I replaced a stale literal with an unreachable one while fixing a comment that warned about stale literals. This is the repo's most-recorded shape (PR #26, #31, #38, #41, now #47) and the pattern is stable enough to plan around rather than rediscover: a fix made under review pressure is the least-reviewed code in the PR, because it is written after the adversarial pass that would have caught it. The counter-practice that actually worked here was re-summoning after every fold rather than declaring the round closed, which cost three extra rounds and found five defects that would otherwise have shipped
- 2026-07-31 · bug · (#17, PR B, Codex adversarial review round 5, rated medium ×3) having finally enumerated the four OUTPUT boundaries in round 4, I enumerated only their SUCCESS paths — and the reviewer found three unguarded error/warning sinks: MCP refusal messages (which quote `unknown target "<name>"` and the full ambiguous-candidate list, so a hostile name reached a client simply by failing to resolve), `team pull`'s two `console.warn` calls on scraped roster entries, and `dispatch`'s unknown-command diagnostic echoing raw argv. The generalization that finally holds: an output boundary is a (surface × outcome) pair, not a surface. Error paths are systematically the ones missed because they are written as an afterthought to the happy path, they are where UNTRUSTED INPUT IS MOST LIKELY TO BE QUOTED BACK verbatim (that is what makes a diagnostic useful), and they are the least-tested. A `grep` for every `console.*` carrying an interpolation took seconds and found all three at once — worth doing as a standing sweep whenever a sanitizer is introduced, rather than one boundary per review round
- 2026-07-31 · note · (#17, PR B, observed while writing a test fixture — not a defect in this PR) `nameKey` does not strip category-Cf characters, so `Versteeg` and `Versteeg<U+202E>` normalize to DIFFERENT keys and therefore to different identities, while rendering identically everywhere a human looks. That is an identity-layer concern rather than an output one — the same shape as #46's duplicate-row hazard, one normalization step over — and it means a scraped page carrying an invisible override would create a silent duplicate player or team that no reader could distinguish from the original. Left unfixed deliberately: it is in #32's name-key subsystem, not this PR's, and it wants deciding alongside #46 rather than folded into a fifth review round of a lineup feature
- 2026-07-31 · bug · (#17, PR B, Codex adversarial review round 6, rated high) `getLineupPlan` selected its evidence by PLAYER id, so a team's predicted lineup was built from every court match its roster members appear in — including matches they played for entirely different teams. Spec § Ingestion deliberately ingests each player's full history "including their other leagues (18+ etc.)", which means player history and TEAM history are different sets and the query conflated them: a newly assembled roster could be handed a confident "8 matches together" for a pairing that had never once played for this team, and the no-history refusal was bypassed by borrowed evidence. Now scoped through `court_matches.team_match_id` -> `team_matches` on either side (home/visiting are pull-perspective, so both columns are checked), with unlinked rows EXCLUDED rather than assumed to belong here and the set-aside count reported in every presenter. Two lessons: (1) five rounds of output-boundary findings had pulled my attention to presentation, and this — the deepest correctness bug in the PR — sat in a single line of query scoping the whole time; a review that keeps finding one CLASS of defect is not evidence that other classes are absent. (2) The tests hid it because every fixture wrote `teamMatchId: null`, i.e. a shape production does not produce: the fixture was simpler than reality in precisely the dimension the bug lived in
- 2026-07-31 · process · (#17, PR B, Codex adversarial review round 7, rated high — FILED as #49, not folded) `team_memberships` is append-only, so a departed player stays on every roster read and `tn lineup plan` can predict them onto a court. Real, and the fold rule says a defect found during a run is fixed in that run's PR — but the two carve-outs both apply and it is worth recording how the line was drawn, since this is the third scope call in one review. It is PRE-EXISTING and not made reachable by this PR (`getTeamProfile` has read the same unfiltered roster since #16; PR B adds a consumer of an already-wrong answer rather than creating the wrongness), and NO SMALL CORRECT FIX EXISTS — the data records no observed-at stamp, so nothing can distinguish a current member from a former one without schema support, which is why the reviewer's own recommendation names snapshot metadata plus transactional reconciliation. The test that separates "fold it" from "file it" is not severity and not effort: it is whether the fix is *in hand* in the subsystem being touched. #42 was in hand and should have been folded; this is not. Same shape as the round-5 `nameKey`/Cf deferral, which the reviewer independently endorsed
- 2026-07-31 · bug · (#17, PR B, Codex adversarial review round 8, rated medium) the runbook I wrote in this PR told operators to check `tn team show`'s `slots:` line before running `tn lineup plan` — and round 6's team-scoping fix silently made that advice wrong, because `team show` counts every court match its roster appears in while the prediction counts only this team's own. A roster with long histories elsewhere shows a healthy slot line and then gets a refusal, sending the reader after the wrong problem. The generalizable part: a behavior change invalidates DOCS written earlier in the same PR, and nothing checks that — the tests all passed, the grammar-parity gate covers command names and summaries only, and I had reviewed the diff twice. When a fix changes what a command counts, grep the docs for anything that told a reader how to predict that count. The remedy also removed the need for the preflight entirely (the prediction is its own preflight, and its refusal already names the fix), which is usually the sign that the original step was papering over a missing message rather than adding information
- 2026-07-31 · bug · (#17, PR B, Codex adversarial review round 9, rated medium) `docs/runbooks/agent-chat-over-mcp.md` still told operators "No predicted lineup or `lineup_plan` tool yet" — a canonical runbook actively denying the existence of the feature this PR delivers — and its tool inventory omitted both new tools. `build-and-print-dossiers.md` carried the same denial for the dossier section. Round 8's finding was the SAME class (a doc invalidated by this PR) and I fixed only the file the reviewer named instead of sweeping, which is the fifth time in this review I patched the reported instance rather than the class. The sweep, when I finally ran it, was one grep across `docs/` for the feature names and for phrases like "no writer" and "not yet", and it found both remaining files in seconds. Standing move for any PR that lands a feature previously described as absent: grep the docs for its own name and for every "not yet"/"no X exists" claim, because those sentences were written to be true only until this PR. The parity gate cannot catch it — it checks command names and summaries, not prose
- 2026-07-31 · bug · (#17, PR B, Codex adversarial review round 10, rated medium ×2) my round-9 "sweep" missed two more stale claims, and the reason is instructive: the grep was `has no writer` (which misses "no writer at all" and "nothing writes"), and it covered `docs/` but not `src/`. So a sweep run to close a class can itself be scoped to an instance. One miss was a SOURCE comment in `report/write.ts` justifying a real behavior (sectionals includes every team) with a reason this PR falsified — the behavior is still right, the stated reason is not, which is the most durable kind of wrong comment because nothing about it looks broken. The other was a pre-existing runbook claim that `tn player show` "reads back availability/notes"; it never did — it reports only a `dataGaps` count — so an HC verifying a write would have read "the section is no longer empty" as confirmation of a specific value. Two rules worth keeping: write the sweep pattern from the CONCEPT ("writer", "not yet", "no X") rather than from the sentence you happen to have just fixed, and run it over source comments as well as docs, because a comment is a claim with the same failure mode and no reader checking it
- 2026-07-31 · bug · (#17, PR B, Codex adversarial review round 11, rated medium) `addEvent` validated its range only against the input, never against what already depended on it: moving or narrowing an existing event's dates left availability rows attached to an event that no longer contained their day. `setAvailability` enforces day-within-range at WRITE time, so nothing would ever notice — the corruption is invisible until someone reads per-event-day availability and finds days the event never covered. The general shape: an idempotent "update in place" writer inherits responsibility for every invariant its earlier writes established, and a validation that runs on the way IN does not cover a later edit to the thing being validated against. Refusing was chosen over deleting the rows or silently keeping them because which of those the operator meant is genuinely ambiguous, and the diagnostic names the days so they can decide — the same "error with candidates, never guess" posture GRAMMAR.md takes for an ambiguous target
- 2026-07-31 · process · (#17, PR B, review rounds 8-11) four consecutive rounds found stale documentation and stale source comments rather than code defects, and every one of them was a claim this PR itself falsified. Two things follow. First, the quality gate has no check for this at all: tests pass, `parity_check.rb` compares command names and summaries, and neither reads prose — so a PR that changes behavior can leave every doc in the repo lying and still be green. Second, the cost is not cosmetic: the specific failures were a runbook telling operators the delivered feature did not exist, a runbook telling them to verify a write through a surface that never showed values, and a comment justifying correct behavior with a false reason. Candidate for the ace findings pipeline: a "docs mention this symbol" check, or simply a standing `invoke`/`verify` step that greps the docs for every identifier and phrase the diff touched
- 2026-07-31 · bug · (#17, PR B, Codex adversarial review round 12, rated high) the range guard I added in round 11 was itself a check-then-act: it read the dependent availability rows, then upserted the event as a separate autocommit statement. `better-sqlite3` is synchronous so nothing interleaves within one event loop — and that is exactly the reasoning that makes this class invisible, because the threat is not one loop, it is TWO PROCESSES. nadal runs `tn mcp serve` against the same WAL database a CLI invocation uses, so an agent chat writing availability and a terminal correcting event dates are genuinely concurrent, and a row committed between the check and the update is stranded by the very update the guard polices. Fixed with `BEGIN IMMEDIATE` (not the default deferred, which takes the write lock only on the first write and so loses precisely the race it is there to win). Two things worth carrying: a fix for a race can reintroduce the race one level up — this is the SECOND check-then-act in the same 60-line function, after round 4's `created` flag — and "the runtime is single-threaded" is an argument about a process, never about a deployment
- 2026-07-31 · process · (#17, PR B) the two-connection test for the above was itself a near-false-green, caught only by mutation-checking it. `better-sqlite3`'s `verbose` hook fires JUST BEFORE each statement executes, so injecting the competing write when `BEGIN IMMEDIATE` is announced happens while the lock is still unheld — the injected write succeeds and the test reports a correct fix as broken. Injecting on the NEXT statement is what actually observes the held lock. The general point is about concurrency tests specifically: they are written against a mental model of when a lock is acquired, that model is usually off by one step, and the failure is silent in both directions (a green that proves nothing, or a red against working code). Mutating the fix — flipping `immediate` back to `deferred` and confirming the test goes red — is the only thing that distinguishes them, and it took thirty seconds
- 2026-07-31 · bug · (#17, PR B, Codex adversarial review round 13, rated high) round 12 serialized `addEvent` and I treated the invariant as closed; the reviewer pointed out the race runs BOTH DIRECTIONS. `setAvailability` also read-then-wrote: resolve an event and its range, let another process narrow that event — which finds nothing to strand, because nothing is committed yet — and the upsert lands a row outside the range that now exists. Same corrupt state, opposite order, and the guard on the other side cannot see it. A mutual invariant between two writers needs serializing at BOTH writers; locking one and reasoning "the other now cannot interleave" is exactly backwards, because the unlocked one is free to start first. Both are now `BEGIN IMMEDIATE`, so whichever commits first the other observes committed state
- 2026-07-31 · process · (#17, PR B) my mutation check on the round-13 test reported "mutant survived" and it was a lie: the command was `git add -A && git commit -q -m ... && python3 <mutate>`, the commit failed (a 1Password signing hiccup), and `&&` meant the MUTATION NEVER RAN — so the suite passed against unmutated code and looked exactly like a false green. I nearly recorded a good test as worthless. Two rules: a mutation check must VERIFY the mutation is in place before running the suite (print the changed line, or assert on the file contents — one line of output), and chaining a mutation behind an unrelated command with `&&` puts the whole check at the mercy of a step that has nothing to do with it. The failure mode is the worst kind: it reports the safe-looking answer ("your test is weak") rather than an error
- 2026-07-31 · bug · (#17, PR B, Codex adversarial review round 14, rated medium) my conflict resolver left a literal `||||||| <sha>` marker in the findings log, because it was written for the three-marker conflict style and this repo is configured `merge.conflictStyle = zdiff3`, which emits FOUR: `<<<<<<<`, `|||||||`, `=======`, `>>>>>>>`. The script took everything between `<<<<<<<` and `=======` as "our side", which in zdiff3 is ours PLUS the marker PLUS the common ancestor — so it did not merely miss a marker, it copied the ancestor section into the file. My verification then confirmed the wrong thing: I grepped for the three markers I knew about and got zero. Two rules. Resolve conflicts with a tool that reads the repo's configured style rather than a hand-rolled parser (`git checkout --ours/--theirs`, `git merge-file`, or just an editor), and — the cheap universal backstop — run `git diff --check` after ANY resolution: it knows every marker style, it is one command, and it caught this in the reviewer's first pass after I had already declared the merge verified with a full green suite, a fresh migrate and an end-to-end report build. A green test suite says nothing about a corrupted markdown file
- 2026-07-31 · fact · (#33, PR #48 delivery) commit signing on this machine is doing less than it looks like it is doing, and it blocked an unattended run to do it: 1Password's `op-ssh-sign` locked mid-`ship`, failing every commit with `1Password: failed to fill whole buffer` while the run held finished, green, twice-reviewed work. Checking what the signatures actually bought, rather than assuming: `main` has NO branch protection and NO rulesets, so nothing requires them; the GitHub API reports every feature-branch commit as `verified: false, reason: unknown_key` because the SSH key is not registered as a SIGNING key on the account; `git log --show-signature` cannot verify locally either (`~/.ssh/allowed_signers` does not exist); and squash-merge discards those commits anyway, replacing them with a merge commit GitHub signs itself (`verified: true, reason: valid`). So the control was costing availability and returning provenance nobody could check — the shape worth remembering is that a security control which FAILS LOUDLY feels like it is working, and "is anything actually verifying this?" is a one-API-call question that went unasked until it broke something. Fallback policy recorded in `PROJECT.md` → *Branch & PR Policy*, deliberately as a per-commit visible retry rather than a config flip, and with its three expiry conditions named
- 2026-07-31 · process · (observed during #18's `invoke`, unrelated to the change itself) `npm run test:coverage`'s two `usta/*.html` vocabulary-completeness tests (`test/fixtures-vocabulary-complete.test.ts`) flake under full-suite parallel load — each takes 4.2-4.4s standalone, right against the default 5000ms `it()` timeout, so CPU contention from ~75 concurrently-instrumented test files pushes them over. Confirmed environmental, not a regression: both pass reliably in isolation and under `vitest run --coverage --no-file-parallelism`, and neither the test, `tools/fixture-policy.ts`, nor either fixture was touched. Worth a per-file timeout bump or a `--no-file-parallelism` CI flag if it starts failing PRs that never touch this code
- 2026-07-31 · bug · (#18, found by the plan's own standing Task 8 check, earned by #196) `court_matches.score` has had ZERO read sites since Phase 3's `pullPlayer` first wrote it (`src/ingest/player-pull.ts:194`) — `courtMatchRowsForPlayers` (src/query/player-profile.ts) selects `id, slot, discipline, winnerSide, playedOn, participants` and drops `score` at the assembly layer, so no derive.ts function, presenter, or dossier section has ever seen a per-match set score; every surface in this app reports only aggregate win/loss/undecided counts, never an individual result string. This PR's `match-add.ts` continues writing to the same column (the whole point of a scorecard capture is its score), which means it is inheriting rather than introducing the gap `winnerSide` shipped and was fixed for (#196). Not fixed here: unlike `winnerSide`, which already had a live consumer (`derive.ts` read a null winner as undecided) and needed only correct population, `score` has NO consumer to wire up at all — closing this for real means building a first per-match results presenter (a "match log" section), which is new feature scope past this issue's 8 tasks, not a wiring fix. Recorded here rather than silently left for the next person to trip over; a dedicated follow-up issue for a match-log/score-display presenter is the right shape for it
- 2026-07-31 · optimization · (#44, PR #51, found during the verify pass) the suite's default 5s per-test timeout makes a full `npm run test:coverage` nondeterministically red on a loaded machine, and it cost this run four full suite runs to establish that. Four consecutive runs failed 2-4 tests each; the failing SET was different every time, every failure was `Test timed out in 5000ms` and never an assertion, and every file involved was untouched by the diff. Measured in isolation the two most frequent victims take 1557ms and 1411ms against that 5s limit, i.e. a ~3x margin — which a machine at load average 183 on 8 cores erases. `npm run test:coverage -- --maxWorkers=2` was green 72/72 files, 1002/1002 tests, first try. Triaged rather than fixed: it is an optimization (raise `testTimeout`, or cap workers in `vitest.config.ts`), not a defect, and #44's PR is a defect fix. The trap worth naming is diagnostic, not mechanical — a red suite whose failing set MOVES between runs and whose every failure is a timeout is reporting the machine, not the code, and the cheap discriminator is to time the victim in isolation against the limit before touching anything
- 2026-07-31 · process · (#44, PR #51, Codex adversarial review rounds 2-3) the "comment claims coverage the code does not enforce" shape hit TWICE inside one PR, and both instances shared a tell sharp enough to check mechanically: **the claim named a component the assertion never exercises.** The registry-wide "declaration parity" test asserted on `dispatch`'s output but claimed to catch drift in what `run` passes to `parseArgs` — mutation proved it cannot (dropping a flag from `player pull`'s parse list left all 39 tests in that file green). The reserved-name test asserted on the static `COMMANDS` registry but claimed "dispatch owns that token before any parser sees it" — true only in flag position, false past the delimiter and false for a value flag's value. Neither assertion was wrong; both NAMES were. So the cheap check, on every test title and comment: underline the nouns, and if one of them is a module the assertion does not touch, the claim is wider than the test — narrow the claim or widen the test, and prefer narrowing. Worth noting the first was caught by me and the second by the reviewer AFTER I had just fixed the first, which is the argument for the rule rather than against it: knowing the failure mode and having just corrected an instance of it did not stop me writing another one four commits later
- 2026-07-31 · decision · (#18, PR #54 round 2, Codex adversarial review High finding B, ruled by the HC) `addMatchFromScorecard`'s parent-match matching is deliberately MORE LENIENT than `upsertTeamMatch`'s: `scheduledTime`/`site` fill in blanks instead of requiring exact (normalized) equality — a blank on EITHER side (stored or the incoming payload) is compatible with anything on the other, and `stored ?? incoming` is what gets written, so a later re-ingest can fill in a detail the first capture omitted, or omit a detail the first capture already supplied, without nulling it out. Scoped to `match-add` only (`upsertTeamMatch` and the `team-pull`/TennisRecord scraping path are untouched) and reuses `normalizeTimeKey`/`normalizeSiteKey` rather than a second hand-rolled equality. **Accepted risk, ruled by the HC, not hidden:** a genuine SECOND same-day fixture between the same two teams whose FIRST ingest omitted a discriminator will now be silently absorbed into the first match on a later ingest rather than creating the second a strict comparison would have — possible ONLY when the stored side is blank (two ingests that both supply a value still discriminate correctly; the doubleheader guard PR #31 added survives intact and is tested). Traded for in-event ergonomics: a scorekeeper correcting a scorecard mid-event should not also have to know whether every discriminator was captured perfectly the first time. See `src/ingest/match-add.ts`'s `upsertLenientTeamMatchForScorecard` doc comment for the full statement
- 2026-07-31 · process · (#18, PR #54 round 3, Codex adversarial review High finding 1) the risk-boundary claim in the entry directly above is FALSE, and the shape is the exact one `docs/findings.md:195` already named — a true-sounding sentence read one inference too far. "Possible ONLY when the STORED side is blank" is wrong: `compatible` accepts a blank on EITHER side, so the absorption risk runs in BOTH directions — a later fixture that OMITS a discriminator can be absorbed into an EARLIER one that SUPPLIED it, exactly as readily as an earlier blank is filled in by a later supplied value. `compatible` has no notion of "first" or "second" ingest, only of which side (if either) is blank right now. The only combination that reliably discriminates two genuinely different fixtures is BOTH sides SUPPLIED and DIFFERENT after normalization — every other combination touching a blank is, by the function's own design, treated as the same match. This entry does not retract the underlying HC decision (the trade itself stands, and PR #31's doubleheader guard is still intact and tested) — only the earlier mischaracterization of how far it reaches. Corrected wording lives in `src/ingest/match-add.ts`'s `upsertLenientTeamMatchForScorecard` doc comment; this file stays append-only, so the entry above is left as originally written rather than edited in place
- 2026-07-31 · process · (#49) the round-7 review note above (:213) — "NO SMALL CORRECT FIX EXISTS" for `team_memberships`' append-only roster, because nothing distinguishes a current member from a former one without schema support — is closed by the fix that note's own reviewer described: a nullable `team_memberships.retired_at` (the `players.name_key` nullable-migration precedent), set by a new `retireAbsentMemberships` inside `pullTeam`'s existing transaction (an empty observed-roster guard against a page-shape change, and a `retired_at IS NULL` predicate so a repeated pull never rewrites when departure was first noticed) and cleared back to NULL by `upsertMembership` the moment a player is observed again. Every CURRENT-roster read (`getTeamProfile`, `getLineupPlan`) and write gate (`setAvailability`, `addCaptainNote`) now filters it; `getPlayerProfile` deliberately does NOT, so a player's history of past teams survives labelled `(former)` rather than disappearing. Three limits this does NOT remove, stated in the PR rather than left for a reader to discover: it does not fix #46 (an upstream team rename still orphans the OLD team row's stale roster, since the reconcile runs against the NEW row's empty membership set); it widens #32's blast radius (a `nameKey` folding miss now retires the real member alongside creating a duplicate — recoverable only because retirement is soft); and `tn player avail`/`tn player note` gain a new refusal for a player the last successful pull did not see, which is a behavior change on a previously-working command, by design
- 2026-07-31 · fact · (#49 delivery) a companion measurement to the 5s-timeout entry above: `test/cli-main-exit-code.test.ts` spawns `bin/tn`, which execs `tsx` and compiles TypeScript on the fly with no cached bundle — three isolated `time bin/tn db migrate` runs measured 5.2s/5.7s/6.0s, so that test is OVER the 5000ms limit on an IDLE machine, not merely under load. The #44 entry's victims had a ~3x margin that contention erased; this one has none to erase, so `--maxWorkers=2` does not rescue it and a `testTimeout` override (or a compiled `bin/tn`) is the only fix that would
- 2026-07-31 · fact · (#49 verify) `src/ingest/upsert.ts` is classified BINARY by every text tool on this machine (`file` reports `data`), because `NULL_KEY = "\0null"` embeds a literal 0x00 byte in the source rather than the two-character escape. The file is valid UTF-8 and the sentinel is deliberate and correct — the cost is tooling: any grep run with `-I` (skip binary files) silently reports ZERO matches in it, and the Claude Code shell's `grep` wrapper passes `-I` by default. During `verify` I ran a `grep -rn "teamMemberships" src/` to audit every read site of a table I had just changed, and `upsert.ts` — the file containing the new write primitive — was invisible in the results. A missing file in a grep result looks exactly like a file that legitimately does not match, so the failure mode is a silently INCOMPLETE audit that reads as a complete one. Two rules: when a grep-based audit is load-bearing, cross-check the file LIST (`git ls-files` / a language-aware search) against the hit list rather than trusting hit counts, and prefer `\0` (escape) over a raw NUL byte in source when a sentinel needs one, so the file stays text to the whole toolchain. Not changed here — it is pre-existing (present since the `NULL_KEY` commit, unrelated to #49) and changing a sentinel's bytes deserves its own change with its own test
- 2026-07-31 · bug · (#18, PR #54 round 4 sweep, Codex adversarial review) `findTeamByName`'s tier-2 exact lookup (`src/ingest/identity.ts`) picked `exact[0]` off `teams.name_key` with no ambiguity check, same as the `tr:`-prefix and lenient-parent-match instances fixed earlier this round — but `teams.name_key` is NOT DB-unique (only the exact `name` column is), so two rows CAN legitimately share a key (two differently-cased `team pull` scrapes of the same real team), and every one of this function's three callers (`resolveTeamTarget` in `src/query/team-profile.ts`, `requireTeam` in `src/ingest/match-add.ts`, `resolveTargetUrl` in `src/ingest/team-pull.ts`) already maps `found.kind === "ambiguous"` to a meaningful outcome rather than silently treating it as `not-found` — confirmed by reading all three before changing anything, per the HC's condition for ruling this in-scope. Fixed: `exact.length > 1` now returns `{ kind: "ambiguous", candidates: exact }`. Two adjacent sites of the identical shape were found in the same sweep and are deliberately NOT fixed here, each because it sits outside `#18`'s own call path (the standing rule is: fix what this PR's own path reaches, log the rest for HC triage, do not open an issue) — `resolveTeam`'s tier 1 (`teams.tennisrecord_url`, `idMatch[0]`) and tier 2 (`teams.nameKey`, `exact[0]`), both in `src/ingest/identity.ts`, reachable only from `team-pull.ts`'s TennisRecord-scraping pipeline; and `findPlayerByName`'s tier 2 (`players.nameKey`, `exact[0]`), reachable only from `src/query/player-profile.ts` and `src/ingest/player-pull.ts` — additionally mitigated in practice (though not proven safe) by the fact that `resolvePlayer`'s own identity ladder already refuses on `exactIds.length > 1` before a duplicate `nameKey` could arise through any normal write this codebase performs
- 2026-07-31 · process · (#18, PR #54, reusable lesson closing a 5-round recurrence) the SAME defect shape surfaced FIVE times across four rounds of adversarial review on one PR, each a genuinely different call site and each fix correct on its own terms, yet each left an adjacent instance standing: duplicate players within one court's own roster list → duplicate court slots across courts in one payload → stale participants surviving across separate re-ingest calls → an ambiguous LENIENT-tier parent-match candidate set → an ambiguous EXACT-tier parent-match candidate set found the very next round in code the previous round's own fix had just touched → and finally a `tr:`-prefix identity collision plus `findTeamByName`'s tier-2 exact lookup, found together in one systematic sweep rather than two more separate rounds. The shape underneath all of them: a set that COULD hold more than one row, selected from via `.find()` / `[0]` / equivalent with no check that it held exactly one. Fixing the reported instance never closed the class, because the report names a symptom (this court, this slot, this candidate set) and the class lives one level up, in the shape of the selection itself. What finally closed it was not a smarter fix but a different KIND of check: grep every `.find(`, `[0]`, and bare-array-index selection against a set built from a query, then check — for each one — the actual `.unique()` / `uniqueIndex` declaration on the relevant column in `src/db/schema.ts`, not a prose comment or a recollection of "this can't happen." A DB-level uniqueness constraint is the only thing that proves a `.find()`/`[0]` pick safe; everything else is an assumption standing in for a guarantee that was never actually checked
- 2026-07-31 · bug · (#46, closes the TEAM half of #42) `upsertTeam` targeted `teams.name` as its ONLY conflict key and wrote `tennisrecord_url` as an ordinary column, so a team renamed upstream (the same TennisRecord URL parsing to a new name on re-pull) went unmatched by the old row and inserted a SECOND row carrying the identical URL — then `resolveTeam` tier 1 and `resolveTeamTarget`'s `tr:` branch both took `.all()[0]` against that non-unique column and picked between the two arbitrarily. Fixed the whole ladder rather than the one query: migration 0009 makes a non-null `teams.tennisrecord_url` DB-unique (partial, `WHERE tennisrecord_url IS NOT NULL`, since most teams are opponent stubs created by name alone with no URL at all); `upsertTeam` now resolves by URL first — a match UPDATES the existing row (name + name_key recomputed, every optional column through the same null-skipping `set` the name path already had), and a rename whose target name is already held by a DIFFERENT row throws the existing `AmbiguousIdentityError` rather than guessing, which `pullTeam` already catches and reports — no new error path at any call site. Folded in the same PR, same class of defect one level under: `teams.name_key` (what tier 2 of `resolveTeam` and `findTeamByName` actually query) carries only a plain index, not a unique one, so two rows folding to the same key ("Springfield A" / "springfield a") hit the identical shape — an `.all()[0]` read against a column that is not actually unique — both now return `ambiguous` on more than one match, mirroring `resolvePlayer`'s own `exactIds.length > 1` branch. The PLAYERS half of #42 (`players.tennisrecord_url` also carries no index) stays triaged and open, unaffected by this fix and correctly so: `resolvePlayer` already consults that URL (tier 1) before every write, so a duplicate-URL pair can never arise there the way it did for teams — an index would only make an already-safe path faster, which is the optimization #42 was triaged as, not a defect.
- 2026-07-31 · bug · (#46, PR #52, found integrating #44/PR #51 — a MERGE-BORN defect neither branch had alone) `runMigrations` gained a detailed, copy-pasteable multi-line recovery for the duplicate-`tennisrecord_url` migration failure, written against `db migrate`'s `console.error`, which passed newlines through. Concurrently and independently, #44 moved `db migrate` onto `emitSummary`'s one-line `key=value` summary — whose `quoteSummaryValue` → `sanitizeValue` replaces EVERY control character, newlines included, with a space. Both sides green in isolation; merged, the recovery collapsed into a single ~1200-character run of prose with `mv -i -- '<src>' '<bak>' && tn db migrate` buried mid-paragraph inside a quoted field, i.e. exactly unusable by the human staring at a failed migration it was written for. `git merge` reported one conflict, in `docs/findings.md`, and none in either changed file — the defect was 100% invisible to the text merge and to both suites, and was found only by running the REAL `tn db migrate` against a seeded duplicate-pair database after resolving, which is the practice :180 already prescribes and this run followed. Two general shapes worth keeping. First: the same message travelling to a DIFFERENT CONSUMER is a merge hazard with no textual signature — an error string's format is a contract with whatever renders it, and #44 changed the renderer while #46 changed the string. Second: the fix is not to escape harder but to make the message single-line BY CONSTRUCTION and let the long form live in the runbook, since markdown has room the summary line does not. Guarded from BOTH sides of the seam — a no-control-characters assertion on the message itself, and an end-to-end assertion in `test/cli-db-migrate-command.test.ts` that the rendered CLI line still contains the whole `mv` command contiguously — because a guard on only one side would go green again the next time the other side moves
- 2026-07-31 · process · (#46, PR #52) the branch-protection PreToolUse hook (`.claude/hooks/enforce-branch-creation.sh`) DEADLOCKS an agent that starts a rebase: mid-rebase HEAD is detached, the hook reads a detached HEAD as a protected branch and fails closed, and it matches on the string `git rebase` — so it refuses `--continue` AND `--abort` AND `--quit`, which are the only commands that can leave the state it is objecting to. The work was never at risk (the branch ref stays at its pre-rebase value until a rebase completes, so all four commits and the pushed remote were intact throughout), but the run could not self-recover and needed the HC to run `git rebase --abort` outside the tool. The hook's own header states the intent it violates here — "on every other branch an agent is free to write, commit, and push at any time; this guard must never get in the way there" — and this run WAS on a feature branch the entire time. The workaround that kept the run moving was integrating with `git merge` instead, which never detaches HEAD; that is a real alternative rather than a bypass, and it is what surfaced the merge-born defect above. The general shape: a fail-closed guard whose predicate is a TRANSIENT state must not also block the transition OUT of that state, or it converts a safe refusal into a trap. Left unfixed here on purpose — the guard is shared config governing every agent in the repo, so changing it is not in scope for a bug fix about team identity, and it wants its own issue rather than a fold
- 2026-07-31 · note · (#46, PR #52, Codex adversarial review round 4, rated high, DISPUTED as pre-existing and recorded rather than fixed) a control character in `TN_DB_PATH` — legal in a POSIX filename — is folded to a space by `sanitizeValue` before any `emitSummary` line is printed, so the path a summary DISPLAYS can name a different file than the one the command actually used. Raised against #46's migration-recovery message, but the reachability check says it is not that message's defect: the identical corruption is demonstrable on the SUCCESS path, `db migrate status=ok path="..."`, which contains no code from this PR — a `TN_DB_PATH` holding a literal newline prints as `path=".../we ird.db"`, naming a file that does not exist. It is a property of the one-line `key=value` summary contract itself and applies to every path-bearing field of every command, so fixing it means either validating configured paths at the boundary or choosing a lossless encoding for the summary — a CLI-wide decision that does not belong inside a bug fix about team identity. What WAS accepted from the same finding is narrower and real: the new no-control-characters assertion hand-listed a character range while claiming to enforce "single-line", missing DEL, the whole C1 block and every `\p{Cf}` format control — a check whose comment outran what it enforced, so it now uses `sanitizeValue`'s OWN class (`\p{Cc}\p{Cf}` plus U+2028/U+2029). The shape worth keeping: when a guard's job is "matches what module X strips", the predicate must BE module X's predicate, not a hand-copied subset that drifts the moment X widens
- 2026-07-31 · bug · (#46, PR #52, Codex adversarial review round 4, rated high) the recovery runbook told the reader to restore captain notes by pasting exported CSV values into `tn player note '<player>' "<note>"`. A captain note is arbitrary text a human typed, so it can carry apostrophes, quotes, backslashes, newlines, or `$(...)`/backticks — and inside DOUBLE quotes a shell evaluates the last of those. The documented recovery could therefore mangle, drop, or EXECUTE the very content it exists to preserve, and an apostrophe in an ordinary name (`O'Brien`) breaks the single-quoted forms too. Checked rather than assumed on both sides: `sqlite3 -csv` quotes and doubles correctly, so the EXPORT is lossless (a note with embedded quotes, commas and newlines round-trips through a real CSV parser intact) — the hazard is entirely on the way back IN. Fixed by directing anything non-trivial through the `player_note` MCP tool, which takes the note as a structured JSON argument so no shell ever parses it, and which is independently the only route that can restore a PAIRING note; the CLI forms stay documented but are explicitly scoped to simple values and switched from double to single quotes. The general shape, and the reason this was a doc bug rather than a code bug: a runbook that says "substitute your data here" IS an interface, and the data it substitutes is as untrusted as any other input — round 4 found it in prose in the same PR whose code had already been hardened three times
- 2026-07-31 · bug · (#46, PR #52 — promoting the #44/PR #51 triage at :225 from optimization to defect, with the reasoning for the promotion) vitest's default 5000ms per-test timeout makes this suite NONDETERMINISTICALLY RED, and that is a flaky gate rather than a slow one — the distinction is why #44 triaging it as an optimization was right for #44 and wrong to leave standing: under the `attested` merge gate a run must demonstrate green AT the delivered SHA, and a suite whose failing set differs every run cannot do that, so the flakiness is load-bearing on merge, not cosmetic. This run hit THREE separate instances: `fixtures-vocabulary-complete` (4549ms idle), then — after that one file was patched — `cli-db-migrate-cwd` (2419ms) and `cli-main-exit-code` (2748ms) on the very next full run. That sequence is the repo's own most-repeated shape arriving in the test harness itself: patching the file that FAILED left the class open, and the class re-presented immediately through two different files. The cause is not slow code — it is honest work colliding with parallel load (real on-disk SQLite, real `tn` subprocesses, whole-HTML tokenization), all of it comfortably under 5s alone and over it once eight workers compete. Fixed as ONE number in ONE place, `testTimeout: 30_000` in vitest.config.ts, and the per-file override added earlier in this same PR was REMOVED rather than left beside it: a per-test timeout equal to the global is dead config that reads as a special case to every later reader. 30s is ~6.6x the slowest observed case, so a genuine hang still fails fast. Verified by three consecutive full green runs rather than one
- 2026-07-31 · bug · (#46, PR #52, Codex adversarial review round 6, rated high) the fail-safe added in round 5 rendered an unrenderable database path with `JSON.stringify` and CLAIMED that was lossless. It is not: `JSON.stringify` escapes the C0 controls but leaves DEL, the whole C1 block, every `\p{Cf}` format control and U+2028/U+2029 LITERAL — and `sanitizeValue` replaces precisely those with spaces one layer downstream. So a path containing U+2028 or RIGHT-TO-LEFT OVERRIDE reached the reader neither losslessly nor control-free while the message asserted both, i.e. a claim outrunning what the code enforced — the same shape logged at :177-178 and :183, now in its third distinct costume in this one PR. The galling part: `src/cli/emit.ts` ALREADY documents this exact `JSON.stringify` shortfall (it was written after a bidi override reached a terminal through `team show --json`), so the knowledge was in the repo, one directory away, and was not applied. Closed with `losslessPath`, which escapes every character in `sanitizeValue`'s OWN class to `\u{XXXX}` and escapes backslashes FIRST so a path legitimately containing the text `\u{2028}` cannot be confused with an escape the renderer produced (the same ordering argument `quoteSummaryValue` already makes). Regressions cover one Zl-adjacent character (U+2028) and one Cf (U+202E), asserting `sanitizeValue(message) === message` rather than a hand-listed absence — the fixed-point assertion is the only form that cannot drift from the sanitizer. The transferable rule: 'escape it' is not a property, it is a property RELATIVE TO A CONSUMER — the correct escaper is the one whose character class is the consumer's, and any other choice is a guess that happens to look safe
- 2026-07-31 · bug · (#46, PR #52, Codex adversarial review round 7, rated high) `losslessPath` and the fail-safe branch were both derived from `sanitizeValue`'s character class — deliberately, as round 4's own lesson ("a guard whose job is 'matches what module X strips' must call X"). That instinct was right and the conclusion was still wrong, which is the interesting part: `sanitizeValue` is only ONE of two mechanisms between the message and the reader. The other is Node's UTF-8 encoder, which destroys a LONE UTF-16 SURROGATE — `\p{Cs}`, a class the sanitizer does not touch at all — writing `x\uD800y` as the bytes `78 ef bf bd 79`, i.e. U+FFFD. So a surrogate path skipped the fail-safe entirely and got a copy-pasteable `mv` naming a replacement-character sibling rather than the real database. Corrected rule, and the transferable one: derive the guard from the consumer only when that consumer is the ONLY thing between you and the reader — otherwise the predicate has to be the UNION over every transform on the path out, and the way to find the others is to ask what encodes, not just what sanitizes. Reachability was checked rather than argued and is worth recording because it cuts the other way from the fix: a lone surrogate CANNOT arrive through `TN_DB_PATH`, since an environment variable is decoded from UTF-8 bytes and that round trip already replaces it — the only route is a direct `runMigrations()` call from JS. Fixed anyway, because the cost was one character class in a shared constant and the alternative is a guard correct only for the inputs someone happened to think of. Mutation-pinned: dropping `\p{Cs}` from the class reddens the new regression and nothing else
- 2026-07-31 · process · (#46, PR #52 — caught by CI, not by the local suite or eight rounds of review) the lone-surrogate regression written for round 7 passed on macOS and FAILED ON LINUX CI with `caught` undefined: `runMigrations` simply never threw there. The test seeded a database at the U+FFFD-encoded filename and then called with the lone-surrogate STRING, on the reasoning that both encode to the same UTF-8 bytes and therefore name the same file — true of this machine, not true of the CI runner. What it actually asserted was a FILESYSTEM'S NAME-RESOLUTION COINCIDENCE rather than the module's behavior, and that is invisible on the machine where the coincidence holds. The root cause is more general than one test: a lone surrogate cannot exist as a real filename on ANY platform, because every OS stores path bytes and encoding one to UTF-8 already replaces it — so an END-TO-END test of that input was never well-defined in the first place, and reaching for one was the mistake. Fixed by exercising the pure functions (`UNRENDERABLE`, `losslessPath`) directly, exported with the reason stated at the export, and leaving end-to-end coverage to the NEWLINE case, which is a legal filename everywhere and passed on both platforms throughout. Two rules. When an input cannot exist in the real world the test is modelling, test the function and not the world — an end-to-end test of an impossible input asserts the harness, not the code. And a green local suite says nothing about platform-dependent behavior: this survived a local run, three consecutive full runs, and EIGHT adversarial review rounds including one that specifically examined this test, because every one of those ran on the same OS
- 2026-07-31 · process · (#46, PR #52) the migration-number collision :180 documented from #17 happened AGAIN, and the recorded resolution held exactly: #49 landed 0006/0007/0008 on `main` while this branch carried its own 0006, so the resolution was to DELETE mine, take main's `drizzle/` wholesale from the index stages, and re-run `drizzle-kit generate` against the merged schema — never hand-edit a snapshot. Mine renumbered to 0009 cleanly. What :180 did not record, and what cost the most attention this time, is the SECOND-ORDER work a renumber creates: every reference to the old number is now a reference to SOMEBODY ELSE'S migration. Here that was six source/doc files plus the user-facing `.pre-0006.bak` backup suffix — and, subtlest, `buildLegacyMigrationsFolder(5)`, which encoded "everything before MY migration" as a literal 5 and silently became wrong when that answer turned into 8. The suite would have stayed GREEN on the stale 5 (it still models a valid older database), so the drift was invisible to the gate — a magic number whose meaning is relative to a moving sequence needs its meaning re-derived on every merge, not its value preserved
- 2026-07-31 · bug · (#46, PR #52, found by RENDERING the output, after eight adversarial review rounds passed over it) the migration-failure message was assembled as `CAUSE.slice(0, -1)` + `(${source})`, which printed as "...unique index cannot apply.(/path/to/db). Recover..." — the database orphaned after a full stop, in the one sentence a human reads while their migration is broken. It survived a Codex round that specifically reviewed this function, and my own reading of the same diff, because reading source does not render it: the defect exists only in the concatenation's OUTPUT. Caught by running `tn db migrate` against a seeded duplicate-pair database as a post-merge check, which is the same practice :180 prescribes for behavioral merge hazards and which keeps paying out for a different reason each time. Fixed by making the message a FUNCTION of the path (`cause(where)`) instead of string surgery around a fixed prefix, so neither branch can drift into malformed punctuation. The general shape: for anything whose product is text a human reads, the review artifact is the RENDERED text — a diff of the generator is not a substitute, and a static reviewer cannot supply one
- 2026-07-31 · note · (#46, PR #52, Codex adversarial review round 10, rated high, ACCEPTED AS MECHANISM and DOCUMENTED rather than fixed) renumbering this branch's migration 0006 to 0009 after #49 landed leaves one broken upgrade path, and the mechanism is worth knowing independently of this PR: drizzle's sqlite migrator applies a migration only `if (Number(lastDbMigration.created_at) < migration.folderMillis)` (`sqlite-core/dialect.js`) — a single TIMESTAMP WATERMARK, not a per-migration hash check. So a database migrated on this branch BEFORE the renumber recorded the old 0006's `when` (1785511662427), which happens to sit BETWEEN #49's 0006 (1785511384473) and its 0007 (1785515827922). The consequence is not the obvious one: 0007, 0008 and 0009 all still apply, only #49's 0006 is silently SKIPPED, and 0009 then fails because the index it creates already exists — leaving the roster-retirement columns missing AND a failed migrate. A renumber is therefore not a rename: it moves your migration's position relative to every OTHER branch's timestamps, and any migration whose `when` falls under a stale watermark is skipped without comment. Severity turns on reachability, checked rather than argued (the practice :181 established for the structurally identical #17 PR A round 6 window): a full-filesystem search found nadal databases ONLY in /tmp scratch directories from verification runs — none in any `data/`, which is gitignored — and the branch is unmerged, so the window closes PERMANENTLY at merge since every database created afterwards applies 0000..0009 in order against one consistent journal. Documented in `docs/runbooks/db-migration-recovery.md` with the same one-line recovery as the `is_home` window it sits beside, rather than carrying migration-reconciliation machinery in production forever for a state with no instance on a database the spec makes a disposable cache
- 2026-07-31 · bug · (#18, PR #54 round 5, Codex adversarial review, rated CRITICAL under PROJECT.md's Review Severity Framework — raised from the reviewer's own High, "security hole" ⇒ Critical) `match_add`'s `sourceImage` was validated only as a non-empty string, then `readFileSync`'d and copied into `raw/scorecard/` by `archiveScorecardImage` — BEFORE `addMatchFromScorecard` checked a single thing about the rest of the payload. A syntactically valid payload naming `sourceImage: "/Users/x/.ssh/id_rsa"` or `"/etc/hosts"` persisted those bytes and THEN returned a refusal: `match_add`/`tn match add` was an arbitrary local-file-read-and-copy primitive reachable by anything that could reach the MCP server, gated by nothing PR #48's fd-anchored destination hardening ever touched (that hardening constrains where the archive WRITES, not what it READS). Fixed with four independent checks, all in `archiveScorecardImage` (`src/ingest/match-add.ts`), each refusing before the next runs: (1) containment — `sourceImage` must resolve (real path, no symlink anywhere in the chain, checked via the new `assertInputPathSafe` in `src/fs/output-root.ts`, the READ-side counterpart to the existing write-side `assertOutputPathSafe`) inside a newly-configured root, `TN_SCORECARD_PHOTOS_PATH` / `scorecard-photos/` by default, mirroring the `TN_DB_PATH`/`TN_RAW_PATH`/`TN_REPORTS_PATH` idiom exactly; (2) size — `statSync` before any read, refusing anything over 25 MiB without ever loading it into memory; (3) extension allow-list (`.jpg`/`.jpeg`/`.png`); (4) content — a magic-byte sniff of the bytes actually read, independent of what the extension claims, so a non-image file renamed `.png` still refuses. Separately, and just as load-bearing: archiving used to run unconditionally whenever `sourceImage` was present, so a REFUSED ingest (unknown team, unresolved player, anything) still persisted the photo. The new `addMatchFromScorecardWithArchive` (called by both `tn match add` and `match_add` — the one shared orchestration point, matching how `archiveScorecardImage` itself was already shared) runs the DB write FIRST and archives ONLY after it succeeds, so a refusal now persists nothing. Tests: outside-root, symlinked, non-image-content, and oversized sources are each refused (unit level); an unknown-team and an unresolved-player refusal each leave `raw/scorecard/` untouched even with an otherwise-valid `sourceImage` (service level, and the unknown-team case again end-to-end through the CLI); the happy path still archives correctly with its provenance sidecar (regression guard, both levels)
- 2026-07-31 · decision · (#18, PR #54 round 5) reordering archiving to run AFTER the DB write creates an interaction Codex explicitly asked to be resolved deliberately rather than left implicit: the write can no longer share ONE failure boundary with the archive step, because by the time archiving runs the write has ALREADY COMMITTED — a bad path, an oversized file, anything, and there is no transaction left to roll back into. Chosen: `addMatchFromScorecardWithArchive` returns the service result EXACTLY as `addMatchFromScorecard` produced it (including `ok: true`) plus an optional `archiveError`, never swallowed and never thrown as an opaque error that would hide a successful `teamMatchId` behind what reads as total failure. The CLI surfaces this as `status=partial` (exit 1, but every successful field — `teamMatchId` included — still printed), reusing the EXACT precedent `tn team pull` already set for "the write committed, a requested detail did not" (`skippedRosterEntries`, `docs/findings.md` — the same status string, the same reasoning, not a new invention); the MCP tool returns `archiveError` as a plain field alongside a normal successful return, matching how `team_pull`'s MCP handler already surfaces `skippedRosterEntries` without throwing. A caller that fixes the path (or omits `sourceImage`) and re-runs the identical payload is safe either way, since the ingest is idempotent on `(playedOn, the team pair, slot)`
- 2026-07-31 · note · (#18, PR #54 round 5, stated limitations rather than silent gaps) three things this fix deliberately does NOT close, alongside the naming choice that avoids a real collision: (1) `assertInputPathSafe` is NOT fd-anchored the way the write side's `openNewOutputFileSafely` is — it checks the path, then the caller reads it in a separate step, leaving a narrow check-then-use window a sophisticated local attacker racing the filesystem could in principle exploit; closing it would need the same open-then-verify machinery the write side already has, not yet built for reads. (2) Only JPEG and PNG sniff as valid — HEIC (many phones' default capture format) and WEBP are not supported yet; a real in-the-field HEIC photo will refuse until re-saved or converted. (3) The new root is named `TN_SCORECARD_PHOTOS_PATH` / `scorecard-photos/`, deliberately NOT `TN_SCORECARD_INTAKE_PATH` / `intake/` — this repo already has an unrelated "Intake Pipeline" (Watchlist voices, `scout`/`clip`, AGENTS.md → *Skills*), and reusing that word for a second, different meaning right next to it would be exactly the kind of same-word-different-meaning collision worth avoiding on sight rather than discovering later
- 2026-07-31 · fact · (#18, PR #54, correcting this run's own sweep entry above after merging #46) the two sweep sites this PR deferred are BOTH closed upstream, and the entry above is stale the moment #46 (PR #52) merged: `teams.tennisrecord_url` is now DB-UNIQUE (migration 0009), so `resolveTeam`'s tier-1 `idMatch[0]` is single-row by construction rather than an unguarded pick, and `resolveTeam`'s tier 2 gained the same `exact.length > 1` ambiguity check this PR added to `findTeamByName`. Two things worth keeping. First, #46 arrived at the IDENTICAL fix for `findTeamByName` independently and in parallel — the same guard, on the same line, for the same reason — which is convergent evidence the class was real rather than an artifact of one reviewer's taste; the merge produced no duplicate because both sides wrote the same guard. Second, the process point: this correction exists because the merge was read for CONCEPT rather than treated as mechanical. A findings entry naming a column's constraint is falsified by any migration that changes that constraint, and nothing in the toolchain checks prose against schema — the entry above would have sat in the log indefinitely asserting a non-uniqueness that had already stopped being true, which is the same 'a true sentence, later read one inference too far' failure this log keeps recording, arriving by a different route: not overclaimed when written, just never re-read when the ground moved
- 2026-07-31 · bug · (#18, PR #54 round 5, Codex adversarial review, second round-5 finding — HC-ruled a deliberate, narrow EXCEPTION to this PR's own "upsertTeamMatch/team-pull.ts stay untouched" scoping, for this one change only) `team-pull` passes `eventId: null` UNCONDITIONALLY for every parsed schedule row (`src/ingest/team-pull.ts:301`, a hardcoded literal — verified at the call site, not assumed, since the HC's whole safety argument rested on it), and `upsertTeamMatch`'s id-less update branch wrote `values.eventId ?? null` straight through with no regard for what the stored row already held. So: ingest a scorecard for an event-linked fixture, then re-pull either team from a schedule containing that SAME id-less fixture (same unordered team pair, date, normalized time/site) — the event link on the parent row is SILENTLY ERASED, even though PR #54 round 2's lenient scorecard-side matching never lets a re-ingest MOVE the event to a different one (`upsertTeamMatch`'s own matching logic, untouched by that change, is what the pull goes through). Fixed with the identical fill-in-blanks idiom `upsertLenientTeamMatchForScorecard` (src/ingest/match-add.ts) already uses for `scheduledTime`/`site`: `existing.eventId ?? values.eventId`, and two DIFFERENT non-null event ids now throw a plain `Error` rather than silently overwrite — matching this file's own existing error posture (`upsertPlayer`'s bare `throw new Error(...)`, not a new refusal type), which team-pull's existing generic `catch (err) { ... return { kind: "error", message } }` already surfaces without any new plumbing. Tests (in `test/ingest-upsert-idempotency.test.ts`, the file's own established style for id-less `upsertTeamMatch` scenarios rather than a full fixture-driven `pullTeam` call, since team-pull's entire contribution to this defect is the literal `eventId: null` at its one call site — already independently confirmed, not additionally exercised by routing through HTML fixtures): the reviewer's named regression (an event-linked row survives team-pull's own `eventId: null` shape); the reverse ordering (a blank stored event is filled in by a later supplied one); two different non-null events refuse with nothing overwritten. All pre-existing `upsertTeamMatch`/`team-pull` tests in both files re-run green, confirming this is additive, not a behavior change to any other path
- 2026-07-31 · optimization · (#18, PR #54 round 5, found while checking whether an `ambiguous`-result message could read confusingly in a new situation, triaged by the HC as display-only) every render site of an `ambiguous` name-resolution result — ~13 of them across `src/cli/commands/*.ts`, `src/mcp/tools.ts`, `src/ingest/match-add.ts`'s `describeMatchAddRefusal`, and `src/ingest/errors.ts`'s `AmbiguousIdentityError` — joins the candidate list with `candidates.join(", ")`. That format assumes a candidate NAME never itself contains a comma, which is false for this dataset's own naming convention: two team rows named `"Norbury, Nova"` and `"NORBURY, NOVA"` (the differently-cased-duplicate scenario `findTeamByName`'s round-4/5 fix now refuses as ambiguous) render as `ambiguous target: Norbury, Nova, NORBURY, NOVA` — four comma-separated segments for what are actually two candidates, each with an internal comma. Not a functional bug: the correct candidates are named, the correct refusal happens, and nothing is silently guessed — a reader who suspects the casing-collision cause can still work out what happened, and this is no less informative than the same `.join(", ")` shape already in use for the pre-existing fuzzy-near-miss ambiguity case. Pre-existing (not introduced by this PR's `findTeamByName` fix), spans a display surface far wider than #18's own call path, and is a readability issue rather than a defect — hence optimization-shaped, logged for HC triage rather than folded into this PR. A real fix would need a delimiter (or structured list) that cannot collide with a legal name character, applied consistently across all ~13 sites at once
- 2026-07-31 · optimization · (#18, PR #54, found while checking whether a fix's refusal message would read badly — not a defect) every `ambiguous` refusal in this app renders its candidates with `candidates.join(", ")`, at ~13 render sites across `src/cli/commands/*`, `src/mcp/tools.ts`, `src/ingest/match-add.ts`'s `describeMatchAddRefusal` and `src/ingest/errors.ts`. Team names in this dataset contain commas ("Norbury, Nova"), so two such candidates print as `Norbury, Nova, NORBURY, NOVA` — visually FOUR comma-separated segments for what are actually two candidates, and a reader who does not already suspect a casing collision will misparse it. Pre-existing and not #18-specific, but #18 makes it more likely to fire, since the `findTeamByName`/`resolveTeam` tier-2 ambiguity guards added in #18 and #46 surface exactly the differently-cased-duplicate case whose candidate names are most likely to collide this way. Display-only: the candidates are correct, the refusal is correct, nothing is silently guessed — which is why it is logged as an optimization for triage rather than folded into the running PR under the bugs-we-fix rule. The fix when someone takes it is a shared candidate-list formatter (quote each candidate, or use a separator that cannot appear in a name) applied at all ~13 sites at once, not one site at a time — the value is in the consistency, and a per-site fix would leave the same trap at twelve others
- 2026-07-31 · process · (#35) the findings-log rule was not merely unreachable — the instruction chain COMMANDED THE OPPOSITE in four places (three found at assess, a fourth by the Reviewer), each sitting exactly where an agent stands when it decides, and every recorded violation was an agent obeying the reachable instruction rather than failing to find the unreachable one. `rules/self-review.md`'s Anti-Patterns (Tier-1 Lean Core, so resident on every run) say "promote it now ... or open a tracked enforcement issue"; `PROJECT.md` -> *Rule-suggestion disposition* shipped as `autonomous-fold`, read by `final` at Step 1 — the exact moment of temptation — telling it to fold into the Rules Layer or "defer ... to a tracked follow-up issue", and #34's own thread records that mechanism creating it ("opened by the `/ship 15` run as a Rules-Layer follow-up off PR #31's findings"); and the only logging pattern the Config Bundle ships is the Learnings Log, whose `scout` disposition OPENS A PR, so the trained reflex points precisely at the banned move. The issue as filed diagnosed silence and proposed stating the rule somewhere reachable, which would have left all three standing and handed the adjudication back to the reading agent — the same adjudication that produced #34 and the "AC asked the HC to adjudicate an exception the header already answered" incident. The general shape: before adding a statement of a rule that keeps being broken, grep the always-resident text for instructions that CONTRADICT it — a rule stated three times and contradicted three times is decided by whichever the agent reads at the moment of action, and that is the config value sitting in the skill's own step, not the one in a spec directory
- 2026-07-31 · process · (#35) the findings-log type vocabulary has drifted 8-vs-4 and nothing noticed, because the SHAPE conformed perfectly the whole time. The spec declares `process | bug | idea | data`; all 222 entries as of `03c577d` match `date · type · gist` exactly, but the types in use are eight — bug 95, process 86, note 16, fact 14, risk 6, idea 3, perf 1, optimization 1 — and `data` has never once been used. Deliberately NOT settled in #35's PR: codifying the eight or rewriting 219 append-only lines to four is an HC triage question, and #35 sidestepped it by citing the log's own header for the format instead of restating the vocabulary in `PROJECT.md` (a third statement site would have drifted next). Worth carrying: a format check written from the spec would have gone red on 38 of those 222 lines while the file was in fact perfectly consistent with ITSELF — when an artifact and its spec disagree, check which one the writers were actually reading before treating either as the defect. (The 36 is itself a correction: this line first shipped "134 of 219", arrived at by subtracting only the `process` count instead of summing the five off-vocabulary types, and was caught in the same verify pass as the two overclaims below — a wrong number inside a correct argument, which is the whole shape being described)
- 2026-07-31 · idea · (#35) a statement-parity test asserting that `PROJECT.md` -> *Findings-Log Discipline* and `docs/findings.md`'s own header state the same disposition vocabulary — the shape ace already uses in `voices_roster_parity_test.rb` — considered for #35's PR and declined, because it would enforce format and statement-parity while NONE of the three recorded violations would have tripped it: every one happened in a GitHub artifact and in judgment, not in the working tree. Shipping it as the remedy for this issue would have retired vigilance in exchange for a green that asserts less than it appears to (`rules/testing.md`: "a check that certifies an invariant it cannot see is worse than no check"). Still a real drift-guard once both statements exist and worth triage on its own merits — logged rather than built
- 2026-07-31 · process · (#35, found while scoping the fix) the spec asserts "nadal never edits vendored files; parity check fails CI on drift", and the second half is not true. `scripts/parity_check.rb` verifies STRUCTURAL invariants — required sections, frontmatter, resolvable links, the gate/reviewer values — and never byte-compares a vendored file against ace, so an edited `rules/backend.md` or a silently reworded skill body passes green. The whole no-vendored-edits discipline therefore rests on agents choosing not to, with no backstop, and every scoping decision that says "we can't touch that, parity would catch it" is reasoning from a check that does not exist. Same shape as the 2026-07-31 commit-signing finding: a control that is BELIEVED to be enforcing something, where "is anything actually verifying this?" was a one-grep question nobody had asked. Not fixed in #35 (out of scope, and the fix is upstream in ace where the checker lives) — but the belief should stop being cited as if it were a gate
- 2026-07-31 · process · (#35, caught in my own self-review before the PR) the section I wrote to state a rule's limits accurately contained TWO overclaims in its first draft, and both were the exact failure mode the section exists to warn about. I wrote that `scripts/parity_check.rb` "reads no prose, here or anywhere" — false: it matches three fixed patterns (a heading advertising itself as unenforceable, and whether a skill body names the `Human Gates` / *Reviewer* host values), which one grep for the regex constants showed. And I wrote that all three overridden instructions are "always-resident text an agent reads on every run" — true only of the Tier-1 rule; `final` Step 1 is read at delivery and the `scout` reflex only when scout runs. Neither error changed the conclusion, which is what makes the shape dangerous: an overclaim inside a correct argument reads as emphasis, not as a factual assertion anyone will check. The mechanical tell is the same one this log recorded on 2026-07-31 for test titles — underline the nouns and the quantifiers ("no", "any", "every", "always") and verify each against the artifact, because those are the words that widen a true claim past what was checked. Writing about rigor does not confer it
- 2026-07-31 · process · (#35, verify pass, after the self-review pass had already caught two) the same overclaim shape hit FOUR times in one PR whose subject is stating limits accurately, and the last two survived a self-review that had just found — and logged — the first two. The verify pass found: (a) a findings line still asserting "three always-resident places" after I had corrected exactly that phrase in `PROJECT.md`, so the PR contained a line documenting the catch and a line still containing the error; (b) "134 of 219" where the arithmetic gives 36, produced by subtracting one type's count instead of summing five; (c) "no in-repo check could see it even if one were written", which is false for two of the four banned spawn targets — a local `rules/` file and an ADR both leave in-repo traces; only an Issue and a PR do not. The generalizable part is about WHERE a corrected error hides: fixing the instance in the file I was editing left the identical claim standing in the artifact I had written FIRST, because the fix was scoped to the sentence rather than to the claim. Sweep by concept across every file the PR touches — the same lesson this log recorded for docs invalidated by their own PR (#17 rounds 8-10), now shown to apply to a PR's own findings entries. And the disposition worth keeping: all four were fixed before the Reviewer was summoned, not filed as known limitations, because "fix drift now, don't document it for later" is what makes an adversarial review confirm rather than correct
- 2026-07-31 · process · (#35, PR #55, Codex adversarial review round 1, rated high) I wrote a section whose entire job was to ENUMERATE the instructions contradicting a rule, enumerated three, and the reviewer found a fourth in a file I had already quoted from — `rules/self-review.md`'s asks-ledger ("each ask is either delivered or handed to a tracked follow-up"), stated THREE times in that file and executable in `ship`'s own `asks_ledger` contract as `status: "handed-off"`. So the orchestrator running the PR carried a mechanism that routes an undelivered ask to a tracked issue, and if the ask is a process learning that is the banned move, mandated by the running skill's own contract. Two things worth keeping. (1) I had read that file's Anti-Patterns closely enough to quote one bullet verbatim and still missed the bullet four lines above it, because I was searching for the CONCEPT I already had ("promote a learning") rather than for the ACTION I was trying to prevent ("open an issue") — searching by the phrasing of the instance you know is the same instance-scoped sweep this PR had already been burned by twice. Searching for the action, `grep -n "tracked follow-up" rules/self-review.md`, returns all three in one line of output. (2) The fix that generalizes is not "add a row" but naming what the findings line COUNTS AS: the completion rule fires when an agent is trying to finish, and "handed to a tracked follow-up" reads as the responsible option exactly when the alternative feels like dropping something. Declaring the findings line to BE delivery removes the pressure instead of forbidding the outlet. Third consecutive round in this PR where the defect was a sweep scoped to an instance rather than to a class
- 2026-07-31 · process · (#35, PR #55, Codex adversarial review round 1, rated high) I set the rule-suggestion disposition to the shipped value `present-to-hc` and redefined its "and waits" clause in prose, reasoning that staying inside the allowed value set avoided drift. The reviewer's refutation was sharper than my reasoning and reversed it: `final`'s canonical body defines `present-to-hc` as present-AND-WAIT, so an agent executing `final` would hold two authoritative, mutually exclusive meanings for one token — and the in-set name makes the divergence LOOK LIKE CONFORMANCE rather than announcing it. The decisive fact was one I had already established and then failed to apply: `grep -rn "autonomous-fold" scripts/` returns 0, so nothing parses this value and "in the allowed set" bought no mechanical safety whatever — only its appearance. Now declared as `log-and-continue`, a token `final` does not recognize, which forces a reader to the paragraph explaining it. The general rule: when a host must diverge from a config value's semantics, DIVERGE ON THE NAME. Reusing the name to stay nominally compliant optimizes for a checker that does not exist, at the cost of the reader who does — and "we stayed inside the allowed values" is only a real defense when something actually reads them
- 2026-07-31 · process · (#35, PR #55, round 2) FOURTH false quantifier in one PR about stating claims precisely, and this one was in the sentence written to JUSTIFY a rule with evidence: I wrote that docs/findings.md "carries a large majority of `bug`-typed entries" when bug is 95 of 222 — 42%, a plurality. The evidence was real and the quantifier inflated it, which is the more dangerous version, because a reader checking the claim finds the underlying fact true and stops. What is worth institutionalizing after four instances: every "majority / most / all / none / always / never / no X" in a PR is a CLAIM WITH AN ARITHMETIC OBLIGATION, and the obligation is one command. I have now written four of these while explicitly hunting for them, so the counter-practice cannot be vigilance — it has to be mechanical: grep the diff for the quantifier words before pushing, and for each one either run the count or delete the word. Also note the vocabulary numbers in the entry above went stale mid-run because main advanced under the branch (219 -> 222 entries), so a count is only true relative to a stated base — name the base in the line
- 2026-07-31 · process · (#35, PR #55, round 2) the classifier I wrote to remove an adjudication introduced a hole big enough to strand a broken build: sorting observations into "product change = work" vs "how agents work = findings line" sends a BROKEN CI WORKFLOW, a broken script, or a broken git hook to a log that nothing spawns from, because none of those is nadal-the-product. The reviewer was mid-investigation of exactly this when its process died, and its interim note ("whether the classifier leaves CI/tooling failures without a work path") was enough to find it. Fixed by reordering the questions rather than adding a category: ask FIRST "is something broken?" — covering repo infrastructure as well as the product — and only then "is this a learning or a proposal?". The rule was always about learnings and proposals, never about things that are simply broken, and the product/process axis silently smuggled in a second claim about severity. General shape worth keeping: when a two-way classifier is built to end an argument, the failure mode is not that the boundary sits in the wrong place, it is that some real input belongs to NEITHER side — enumerate a few concrete inputs against a new classifier before shipping it, and pick the awkward ones (the toolchain, the test harness, the CI) rather than the central examples that motivated it
- 2026-07-31 · process · (#35, PR #55) a Codex adversarial-review job DIED mid-review with its runtime status still reporting "running" — the pid was gone, the log stopped, and `result <job-id>` answered "No job found" while `status --all` still listed it as running. Two practical consequences. The status field alone is not a liveness check: confirm with `ps -p <pid>` before waiting on a job, or a run waits out its whole bounded window on a corpse. And the partial output was still worth reading — the dead job had captured an interim assistant message naming a "live factual regression" and the CI-classifier question, and BOTH turned out to be real defects, found and fixed from that fragment alone before the retry. A review that crashes is not a review that found nothing; read the log before discarding it. Also worth noting the log showed an early `{"verdict":"approve","findings":[]}` interim message minutes before the real analysis — an approve captured mid-stream is not a verdict, which is a good reason never to parse the first verdict-shaped object out of a job log
- 2026-07-31 · process · (#35, PR #55, Codex adversarial review round 2, rated high) all THREE candidate values for a config key were defective, and the reviewer's two findings were the pincer that showed why: `autonomous-fold` (shipped default) directs the two banned actions; `present-to-hc` gives `final` two mutually exclusive meanings for one token (round 1); and the out-of-set `log-and-continue` I chose to fix round 1 matches NEITHER branch of `final` Step 1's conditional, so an agent may perform neither — including failing to append the log line — which is silent undefined behavior at the exact step that spawned the original violation. The reviewer's own recommendation ("do not ship the out-of-set value until canonical `final` recognizes it") was unimplementable: `skills/` is vendored, so waiting on upstream blocks the fix indefinitely, and both other values are already known-defective. The escape was to stop arguing about WHICH VALUE and notice that a bare value only works if the consuming procedure has a matching branch — so the host config must state the disposition as an IMPERATIVE PROCEDURE ("read this as final's Step 1; it replaces both bullets"), with the value demoted to a label. Explicitly telling the reader "if you found no matching branch, that is expected, this block IS the branch" is the sentence that closes it. General rule worth keeping: when a host overrides a vendored enum it cannot extend, override the STEP, not the value — a value is a request for behavior that lives elsewhere, and elsewhere does not know about it
- 2026-07-31 · process · (#35, PR #55, Codex adversarial review round 3, rated high) writing a replacement for a vendored procedure, I substituted the DECISION and dropped the DELIVERY. My five-step override of `final` Step 1 said "append the findings line ... continue straight to Step 2" and omitted the canonical branch's explicit "Commit and push the folds so Step 2's checks run on the folded diff" — so the appended line could sit in the working tree while the SOW claimed it was delivered, or get committed and silently move the head off the SHA the `attested` gate binds. The canonical branch states BOTH obligations and I had transcribed only the first, because the interesting part of the override was what NOT to do (don't fold, don't file, don't wait) and the boring part was what still had to happen. The rule that generalizes: when replacing a step, diff your replacement against the ORIGINAL STEP'S FULL OBLIGATIONS — every downstream thing it feeds (here: Step 2's checks, the attestation ordering, the SOW section) — because an override is written with attention on the delta and the dropped obligations are invisible precisely because they were never the point. Also worth keeping: "wait for nothing" governs the HUMAN gate only; it does not license skipping a commit or a re-review, and stating that explicitly was necessary because the two readings are genuinely available from the same three words
- 2026-07-31 · process · (#35, PR #55, Codex adversarial review round 4, rated high) my round-3 fix said "re-enter `verify` to summon the Reviewer on the post-append head", and the reviewer pointed out that verify is NOT a summon-only operation — it re-reads the whole PR against the approved plan, so a `docs/findings.md` commit made after plan approval must be classified as unsanctioned scope creep (removing it breaks the step that mandated it; keeping it leaves a finding open), and the path can CYCLE, because the extra pass may itself produce a learning that appends, moves the head, and needs another review. Two rounds in a row I had reached for a bigger delivery-time mechanism when the actual fix was to move the write EARLIER: append the line in the phase that learned it, committed with that phase's own work, so it is already inside the diff the Reviewer sees and `final` mutates nothing. The tell I should have caught myself: I was writing a procedure that mandated re-running a STAGE, and a stage has obligations beyond the one sub-step I wanted from it — "re-enter X to do Y" is almost always wrong when X is a lifecycle stage rather than a function. Worth noting the correct design was already what this very run had been doing by hand for four rounds (every findings line was appended and committed with the phase that produced it); the written procedure had drifted from the practice that was working right in front of me
- 2026-07-31 · process · (#35, PR #55, Codex adversarial review round 5, rated high ×2 + medium) three rounds of findings finally showed they were ONE root cause, and it was a category error rather than three bugs: I was legislating LIFECYCLE MECHANISM from a host config file that has no authority over it. Round 3 caught a delivery-time append that was never committed; round 4 caught the fix mandating a `verify` re-entry that `verify` does not own and that can cycle; round 5 caught the next fix declaring "a findings-log append is never scope creep", which would force `verify` to either emit a false plan-alignment record ("only files in the final approved plan changed") or disobey the host — plus a concrete abuse, pairing an unplanned change with a purported findings line to pre-exempt it. Each patch moved the same defect one step sideways because I kept trying to SOLVE the mechanism instead of noticing nadal cannot. The fix that held: state the standing append as a HOST-REQUIRED plan task that every posted plan must carry, so the append is inside the approved plan and verify's normal drift test passes truthfully with nothing suppressed by filename (this line first said "a plan item `devise` includes", the same false descriptive-vs-prescriptive claim round 8 caught in PROJECT.md three rounds later — corrected here at round 12, which is the NINTH time in this PR a fix was scoped to the file being edited and left the identical claim standing where it had been written first); stop promising "no extra review round", because a learning arriving after the Reviewer responds moves the head and the attested gate charges for it exactly as it does for any `listen` fix; and name the residual — the lifecycle has no cheap terminating path for a post-summons durable append — as an upstream gap rather than a thing this file settles. Two rules worth keeping. When a host override keeps generating findings in DIFFERENT places, stop patching and ask whether the thing being overridden is even in the host's authority. And a config file may add content to an artifact the AC authors (a plan item) but must never instruct a vendored check to look away — the first is supplying input, the second is corrupting the audit record that check produces
- 2026-07-31 · process · (#35, PR #55, round 5) the medium finding was a coverage gap I had written past twice: "append in the phase that learned it (invoke, verify, listen)" silently excludes assess, devise, and the orchestrator, and those phases have NO BRANCH to commit onto — so an early learning had no executable destination and would be lost or smuggled in as an unplanned write later. The tell is generic and cheap to check: when a rule enumerates the cases it covers, read the enumeration against the FULL list of cases that exist, not against the ones that motivated the rule. I had listed exactly the three phases where I happened to be standing while writing it. Fixed by routing pre-branch learnings to the stage's own durable artifact (the assessment or plan comment on the issue, which is where `ship` already records stops and asks) and transcribing them in `invoke`, the first phase that has a branch
- 2026-07-31 · process · (#35, PR #55, Codex adversarial review round 6, rated high) my "standing plan item" fix was sound for runs that POST a plan and silently assumed every run does. The lifecycle expressly permits two planless shapes — a trivial fix skips Plan, a documentation-only change runs Implement -> Deliver — and on those a process learning had no truthful path: appending was unplanned drift, omitting violated "one findings line, always". Not theoretical, since this very PR is documentation-shaped. The resolution was not a new mechanism but noticing the symmetry I had missed: on a planless run there is no approved plan for ANYTHING in the diff, so `verify` has no plan-alignment baseline for the FIX either — the findings line is exactly as sanctioned as the change it rides with, and no more, because the HC's decision to compress is the authorization for the whole diff. The generalizable tell: when a rule is written against the ONE workflow shape you are currently executing, it will assume that shape's artifacts exist; check it against every row of the lifecycle's own skip/compress table before shipping. Also worth recording that the reviewer affirmed the distinction the previous round turned on — supplying required CONTENT to a plan is materially different from instructing a CHECK to ignore a file — which is the line I want to keep: a host may add to what the AC authors, never subtract from what a vendored check reports
- 2026-07-31 · process · (#35, PR #55, Codex adversarial review round 7, rated medium) the PR DESCRIPTION went stale while the code it describes was corrected seven times, and nothing checks that: `verify` Step 6 requires an accurate PR description, but a description is prose and every gate in this repo reads code. Two claims survived six rounds of edits — a Known Limitation still saying the run executes under `present-to-hc` (superseded in round 1) and another asserting no in-repo check could observe a violation (superseded by the rules-file/ADR asymmetry fix). Both were in a section I had edited twice without re-reading whole. The mechanical lesson, eighth instance of this shape in one PR: a long-running PR's DESCRIPTION is an artifact that drifts exactly like a doc or a comment, and patching it section-by-section is what let two stale claims survive — past about three rounds of substantive change, REWRITE the body from the current state instead of editing it, and diff the claim list rather than the prose. Worth noting the reviewer found this only after the section itself had gone two rounds clean, i.e. once the code stopped being wrong the stale narrative ABOUT the code became the top defect
- 2026-07-31 · process · (#35, PR #55) `main` advanced TWICE under a long-running branch (PR #49 mid-run, then PR #52), and each time it silently invalidated counted claims: the findings-log totals moved 219 -> 222 -> 234, so entries written as "all 219 entries" and "36 of 219" were true when written and false a few commits later. Two habits come out of it. State a count against a NAMED COMMIT ("as of 03c577d"), never against a floating phrase like "at this PR's merge base", because the base itself moves when you re-integrate. And re-fetch before trusting any figure derived from the base — the first recount only happened because a `git show origin/main:` result disagreed with a number computed twenty minutes earlier, which is a weak signal to rely on. Both merges also produced the same `docs/findings.md` conflict (both sides append to the tail); resolving by explicit verified line-range splice, then checking all four zdiff3 marker types plus `git diff --check` plus an entry-count reconciliation against both parents, caught nothing wrong but is the cheap proof that nothing was lost
- 2026-07-31 · process · (#35, PR #55, Codex adversarial review round 8, rated high) I wrote "`devise` includes it in every plan it posts" about a task the vendored `skills/devise/SKILL.md` does not mention anywhere — a DESCRIPTIVE claim about shipped behavior where I only had a PRESCRIPTIVE requirement, and the whole plan-item mechanism rested on it. The consequence was exact: a normal planned run learns something during implementation, finds no approved-plan authorization for the append, and must either omit the line (violating the disposition) or create the unplanned drift the mechanism exists to prevent. What makes this worth recording is that the same PR had ALREADY solved this shape once and I did not transfer it: the `final` Step-1 override says outright "if you looked for this in the vendored body and found no matching branch, that is expected, this block IS the branch", and the plan-item paragraph needed the identical sentence and did not have it. So the tell is narrow and checkable — whenever host config says a VENDORED skill "includes", "does", or "already handles" something, grep that skill body for it; if it is absent the sentence must be rewritten as a requirement the host imposes, with the absence stated. Descriptive phrasing about someone else's file is a claim about the world; prescriptive phrasing about your own file is a rule. Only the second is yours to write
- 2026-07-31 · process · (#35, PR #55, Codex adversarial review round 9, rated medium) asked to attack the disposition from the FAIL-OPEN direction rather than the fail-closed one — "can an agent be led to SKIP the findings line entirely" instead of "can it be led to file an Issue" — the reviewer immediately found a path eight rounds of the opposite question had missed: my pre-branch rule said record the learning in the issue comment and transcribe it "in the first phase that has a branch — `invoke`", which silently assumes `invoke` always happens. An emergency stop, an abandoned option, or an issue closed without implementation all terminate before any branch exists, and the learning then lives only in an issue comment that is not the triage artifact. No check, no stop, and no required action exposes the omission. Fixed with a labelled terminal debt rather than a new mechanism: a run ending before `invoke` must list each untranscribed learning under a literal `Untranscribed findings:` heading in its terminal comment, and any later run reaching `invoke` on that issue transcribes the block. The methodological lesson is the one worth keeping and it is cheap to apply: a rule with a REQUIRED ACTION has two independent failure directions — doing the forbidden thing, and quietly doing nothing — and adversarial effort naturally concentrates on the first because that is where the motivating incidents were. Ask for the second explicitly. Eight rounds of "can this be led to file an Issue?" never surfaced this; one round of "can this be led to skip?" surfaced it on the first pass
- 2026-07-31 · process · (#35, PR #55, Codex adversarial review round 10, rated medium) my round-9 terminal-debt rule covered "the run never reaches `invoke`" and stopped there, missing the symmetric ending: a run that DOES reach invoke and whose PR is later closed unmerged discards the branch and every findings line committed onto it. Self-demonstrating — closing PR #55 unmerged would have thrown away every one of its own entries, including the ones recording this class of mistake. (That sentence first shipped a literal count, which the reviewer caught one round later: a self-referential count in an APPEND-ONLY log falsifies itself the moment the next line is appended, and the entry containing it is itself the next line. Sixth quantifier error in this PR, and the only one that was arithmetically unfixable rather than merely wrong — the correct move is not a better number but no number.) The generalization is about where a durable-write rule actually completes: committing is not landing, and a rule that says "append and commit with the phase work" has only made the line durable ON A BRANCH, which is a location that can be deleted wholesale by a decision nobody thinks of as data loss. Ask of any persistence rule: what is the LAST point at which this can still evaporate, and does the rule reach it? Here the answer was `main`, two endings short of where I had stopped. The second half of the finding was equally worth fixing: "transcribes every such block it finds" left DISCOVERY undefined, so recovery depended on luck. Fixed by mandating one canonical location — the block goes on the ISSUE, always, even when a closed PR triggered it, because a closed PR is not where the next run looks — which reduces the search scope to a single deterministic read of that issue's comments. A recovery mechanism whose search scope is unstated is not a mechanism
- 2026-07-31 · process · (#18, PR #54 round 6, Codex adversarial review — a duplicate-entry note, not a new finding) `:225` and `:227` are the SAME finding, arrived at independently on two different PRs and merged together without either author noticing the overlap: both diagnose vitest's default 5000ms per-test timeout making `npm run test:coverage` nondeterministically red under full-suite parallel load, `:225` from this PR's own `invoke` (one named victim, `fixtures-vocabulary-complete.test.ts`) and `:227` from #44/PR #51 (a general diagnosis with its own two named victims and the "failing set moves between runs, every failure is a timeout" discriminator). `:227` is the fuller treatment — it states the general mechanism, the diagnostic method, and the severity reasoning, where `:225` is narrower supporting evidence for the identical underlying cause. Both are also now SUPERSEDED by `:241` (#46, PR #52), which promoted exactly this from optimization to defect and fixed it as one number (`testTimeout: 30_000`) — left standing per this log's append-only rule rather than edited or deleted, and left as two entries rather than merged into one for the same reason. The append-only shape makes near-duplicates like this reachable only by a reader who checks new entries against the EXISTING log before writing, not by any tooling; worth naming as its own small instance of the class this file's own `:195`/`:230` entries already track under a different name
- 2026-07-31 · bug · (#18, PR #54 round 7, Codex adversarial review, rated High) `upsertLenientTeamMatchForScorecard`'s candidate query filtered `isNull(teamMatches.sourceMatchId)`, mirroring `upsertTeamMatch`'s own id-less query — but that mirroring was wrong for THIS function's job: it skipped every row `team-pull` had already written with a real `mid=`, and that is reachable in the documented workflow, not a corner case. `team-pull` records a completed schedule fixture WITH its source id; a scorecard for that exact fixture (same unordered team pair, `playedOn`, and normalized `scheduledTime`/`site`) then saw no candidate and inserted a SECOND, source-less parent — one real fixture split across two `team_matches` rows, corrupting every derived read of either (head-to-head, record, lineup history). Fixed by dropping the `isNull` filter entirely: a source-backed row is now just another candidate, subject to the identical exact-then-lenient precedence and identical ambiguity refusal the lenient matcher already had. Preserving `sourceMatchId` needed no new code — the update's `.set({...})` never mentions that column (a scorecard payload has no `mid=` of its own to offer), so it is left exactly as stored whether the row is source-backed or not, the same "nothing to fill in with" shape `homeTeamId`/`visitingTeamId`/`homeCourtsWon`/`visitingCourtsWon` already had on this path. Tests: a hand-seeded source-backed row is attached to rather than duplicated (unit level); the reviewer's own named integration case — a REAL `pullTeam` run against the `tennisrecord/team` fixture (`mid=181505`, 04/09/2026, Norbury vs. Granborough) followed by a matching scorecard — ends at one `team_matches` row with `sourceMatchId` intact and the court attached to it, not two rows
- 2026-07-31 · bug · (#18, PR #54 round 7, Codex adversarial review, rated Medium) `slot` was accepted by `scorecardPayloadSchema` as any non-empty string and then compared by RAW equality by every downstream consumer — the duplicate-slot count and `upsertCourtMatch`'s id-less write key both — so `"D1"` and `"D1 "` were two different courts as far as either could tell: a single payload containing both evaded the duplicate-slot refusal, and a re-ingest correction spelled with the trailing space created a SECOND court row instead of replacing the first, quietly reopening (by a different route) the exact defect rounds 1–3 spent closing. Fixed by canonicalizing ONCE, in the schema (`slot: z.string().trim().min(1)` — trim runs before the length check, so a whitespace-only slot still fails rather than silently becoming an unlabeled court), so every consumer downstream sees the identical value — the same "one ladder, one notion of a name" shape `nameKey` (team/player names) and `normalizeTimeKey`/`normalizeSiteKey` (parent-match discriminators) already close, arriving now through the slot field instead. Tests: schema-level trim-and-reject-whitespace-only; service-level same-payload duplicate (`"D1"` + `"D1 "` in one submission, refused) and retry (`"D1"` then `"D1 "` across two calls, one row, not two) — both built through `scorecardPayloadSchema.parse` rather than hand-typed `ScorecardPayload` objects, since the fix lives in the schema and a test that skipped parsing would not exercise it. Swept every other payload field for the same shape rather than assuming slot was the only one: `homeTeam`/`visitingTeam` and `homePlayers`/`visitingPlayers` already go through `nameKey`-folded resolution (`findTeamByName`/`resolveRosterPlayer`); `scheduledTime`/`site` are normalized at COMPARISON time by design (the Finding-B HC decision, `normalizeTimeKey`/`normalizeSiteKey`); `playedOn` is regex-anchored to a real calendar date with no whitespace variance possible; `winnerSide` is a zod enum. One adjacent case found and NOT fixed, reported rather than silently left: `payload.event` is looked up via `eq(events.name, payload.event)` — an EXACT, unfolded string comparison, so a trailing-whitespace event name would refuse as "unknown event" rather than match. Not a new instance of this PR's own defect class, though: every other event lookup in the codebase (`src/query/events.ts`, `src/query/availability.ts`) uses the IDENTICAL exact-match-only pattern against the same DB-unique `events.name` column, so this reads as an existing, consistent, codebase-wide design choice (events are human-typed via `tn event add`, never scraped, so no spelling variance was ever expected) rather than an oversight local to match-add — recorded for HC triage rather than fixed under this PR's own narrower scope
- 2026-07-31 · bug · (#18, PR #54 round 8, Codex adversarial review, rated High) round 5 fixed `eventId` erasure in `upsertTeamMatch`'s ID-LESS branch and left its SIBLING — the `sourceMatchId`-keyed `onConflictDoUpdate` branch a few lines below, in the SAME function — writing `eventId: values.eventId ?? null` unconditionally, the identical shape already fixed once next door. Round 7 made this reachable in a way it was not before: a scorecard can now attach to (and set an event on) a `mid=`-bearing row, so re-pulling that same `mid=` — which always supplies `eventId: null`, per `team-pull`'s one call site — silently erased the event the scorecard had just linked. The shape worth naming, per the HC's own framing: this is "fix one branch of a function, leave its sibling branch a few lines away" for the THIRD time in this PR (within-court, then across-courts; lenient-pass, then exact-pass; now id-less branch, then source-id branch) — round 7's own commit message said the candidate-query fix was "mirroring `upsertTeamMatch`'s own id-less query", written while looking directly at this function, and still only saw one of its two write paths. Fixed with the identical preserve/fill/refuse semantics, expressed differently because `onConflictDoUpdate` has no separate SELECT step to hang a JS `existing.eventId ?? values.eventId` off of: a PRE-SELECT by `sourceMatchId` runs first (safe with no added race window — better-sqlite3 is fully synchronous, so nothing can interleave between the select and the upsert immediately after it), throwing the SAME conflict error shape on two different non-null events, and feeding the SAME JS expression the id-less branch already computes into `onConflictDoUpdate`'s `set` clause as a plain value, rather than a hand-rolled SQL `coalesce` — one shared notion of "how an event is written" between the two branches. Every OTHER column in this branch keeps its existing unconditional-overwrite policy, deliberately: unlike the id-less branch (reconciling two possibly-stale observations of an unidentified fixture), a `mid=`-keyed conflict means team-pull re-scraped the SAME identified page, and a fresh authoritative parse of it is exactly what should win for orientation/courts-won — only `eventId` is a detail team-pull can never truthfully report on. Tests mirror round 5's id-less trio exactly, now for the source-backed branch: survives-a-null-re-pull, filled-in-by-a-later-supply, two-different-events-refuse
- 2026-07-31 · bug · (#18, PR #54 round 8, Codex adversarial review, rated Medium) round 7's `trim()` closed the WHITESPACE variant of "two spellings, one identity" for `slot` and left the CASE variant standing: `"D1"` and `"d1"` still compared unequal to the duplicate-slot count and the id-less court upsert's write key, so the identical duplicate-slot-evasion and re-ingest-duplication defects round 7 fixed for whitespace were still reachable through case — the SAME class arriving a third time in this PR, after `nameKey` and `normalizeTimeKey`/`normalizeSiteKey`. Fixed by extending the same schema canonicalization: `slot: z.string().trim().toUpperCase().min(1)`. Deliberately NOT a slot grammar (an S/D token allow-list) even though one would also close this: spec:13 states court-slot format is per-event data, never a constant (Tulsa 2025 ran S1+D1-D4 against Springfield's four courts), so this canonicalizes the REPRESENTATION of whatever string a court names without constraining the VOCABULARY of what it may legitimately be. Tests mirror round 7's whitespace pair exactly, now for case, at three layers: schema (lowercase → uppercase), service (same-payload `"D1"`+`"d1"` refused as duplicate; retry `"D1"` then `"d1"` updates one row), and — specifically because round 8's own review flagged this as an MCP-surface question, not just a CLI one — a real `match_add` MCP tool call through the actual SDK request/response machinery (`test/mcp-tools.test.ts`), proving the fix reaches that surface and not just the schema or the CLI presenter that already happened to share it
- 2026-07-31 · bug · (#18, PR #54 round 9, Codex adversarial review, rated Medium) round 8's `sourceMatchId`-branch event fix (a JS pre-select, then a JS-computed decision fed into `onConflictDoUpdate`'s `set`) was safe within one synchronous better-sqlite3 connection but NOT across two — exactly how the MCP server and a CLI invocation reach this table, as separate processes each with their own connection. Concrete interleaving: `mid=181505` stored with `eventId=null`; connection A pre-selects (sees null); connection B ALSO pre-selects (sees the SAME null, since A has not written yet) and commits event 102; A then commits event 101 from its now-stale pre-selected null — neither call throws, and B's event is silently overwritten. Fixed by collapsing the conflict check and the write into ONE atomic statement: `onConflictDoUpdate` now carries a `setWhere` gating the whole update on "stored event is null, OR incoming is null, OR they already match" (the only conditions under which writing is safe), with `eventId` computed via SQL `coalesce` on the stored column rather than a JS expression; when none of those hold, SQLite's own UPSERT semantics skip the update entirely, so `.returning().get()` comes back `undefined` — the sole signal used to throw the conflict error (a fresh insert or a permitted update always returns a row). This deliberately makes the two `upsertTeamMatch` branches use DIFFERENT mechanisms for the identical notion (preserve/fill/refuse) after round 8 explicitly asked for one shared mechanism — correctness across connections requires the check-and-write to be one SQL round-trip, which only a real conflict-target column (the id-less branch has none) can support this way; the divergence is documented in the code as deliberate, not drift. Testing note: a literal interleaved race cannot be expressed as a black-box red/green test against `upsertTeamMatch` — its own pre-select was already always fresh at call time, so the vulnerability lived entirely INSIDE one call's read-then-write gap, which synchronous JS test code cannot pause mid-execution to exploit; confirmed by running both new tests against the round-8 code before touching anything and finding them already green. Verified instead with two tests: a CHARACTERIZATION test using raw table operations to reconstruct the exact two-connection sequence team-lead described, proving the underlying hazard is real independent of any one function's implementation; and a cross-connection correctness test on the real, fixed function.
- 2026-07-31 · fact · (#18, PR #54 round 9) answering the class-level question asked directly: the ID-LESS branch (`upsertTeamMatch`'s other half, and `upsertLenientTeamMatchForScorecard` in match-add.ts, which has the identical shape) has the SAME cross-connection exposure — it is a SELECT (`candidates.find(...)`) followed by a JS-decided `db.update(...)`, wrapped in the caller's `db.transaction(...)`, which both `team-pull.ts` and `match-add.ts` call in its DEFAULT (deferred) mode rather than `{ behavior: "immediate" }` — a deferred transaction only takes its write lock at the first WRITE statement, so two connections' SELECT phases can still interleave before either commits, exactly the round-9 shape. NOT fixed here, and not cheap the way the source-id branch's fix was: that fix worked because `sourceMatchId` is a REAL, DB-unique conflict target `onConflictDoUpdate` can gate a `setWhere` on; the id-less branch's identity (unordered team pair + `playedOn` + NORMALIZED time/site) has no backing unique index at all — by design, since the normalization can't be expressed as a plain column constraint — so there is no equivalent atomic-UPSERT mechanism available for it without inventing one (a stored, indexed normalized-key column plus a schema migration, well past this round's scope). The one CHEAP lever that WOULD close it — switching both callers' `db.transaction(...)` calls to `{ behavior: "immediate" }`, forcing the write lock at BEGIN rather than at the first write — is named here rather than applied unilaterally, because it changes the locking/serialization behavior of the WHOLE containing transaction in both pipelines, not just this one race, which is a broader trade-off than this round's narrow fix and deserves an explicit go-ahead rather than a silent widening.
- 2026-07-31 · bug · (#18, PR #54 round 9, Codex adversarial review, rated Medium) `trim().toUpperCase()` (round 7/8) does no Unicode COMPATIBILITY normalization, so a full-width slot spelling ("Ｄ１", FULLWIDTH LATIN SMALL LETTER D + FULLWIDTH DIGIT ONE) reached the service as a genuinely different string from its plain-ASCII form ("D1"/"d1") — the identical duplicate-slot-evasion and re-ingest-duplication shape, a THIRD time, through a different Unicode axis than case or whitespace. Confirmed NOT the Turkish-locale `toUpperCase` class of bug — `toUpperCase()` is locale-independent here; the gap was one step earlier, at normalization. Fixed by adding `.normalize("NFKC")` before `.toUpperCase()`: `slot: z.string().trim().normalize("NFKC").toUpperCase().min(1)`. Still not a slot grammar, same standing reasoning as rounds 7/8. Tests at three layers again, schema plus BOTH surfaces since the reviewer specifically flagged MCP a second time: a real `tn match add` CLI re-ingest correction spelled with a full-width slot (`test/cli-match-add-command.test.ts`), and a real `match_add` MCP tool call (`test/mcp-tools.test.ts`) — both confirmed red before the fix (temporarily reverted `.normalize("NFKC")`, all three layers failed for the right reason, then restored).
- 2026-07-31 · bug · (#18, PR #54 round 9, found by the sweep the reviewer asked for — "confirm rather than assume" `nameKey` already handles this — and it does NOT) `nameKey` (`src/db/name-key.ts`) normalizes with `.trim().normalize("NFC").toLowerCase()` — NFC is CANONICAL normalization (composes combining marks), not NFKC (COMPATIBILITY normalization, which folds full-width/half-width and other compatibility variants) — so `nameKey("Norbury")` and `nameKey("Ｎｏｒｂｕｒｙ")` (a full-width spelling) produce DIFFERENT keys (`"norbury"` vs `"ｎｏｒｂｕｒｙ"`, verified directly rather than assumed). This is the IDENTICAL class as `slot`'s three rounds, but on the CORE identity system every team/player/alias name flows through — not scoped to #18's own call path, and a plausible real-world trigger: a vision model transcribing a scorecard photo has no particular reason to prefer ASCII digits/Latin letters over full-width ones if the source photo (or the model's own tokenization) renders that way. NOT fixed here, deliberately: `nameKey` backs `players.name_key`, `teams.name_key`, and `player_aliases.name_key` across the ENTIRE application, not just match-add's own reads — changing its fold function would require a migration re-backfilling every existing stored key (the same shape `backfillNameKeys`/migration 0004 already exists for), and auditing every consumer of the CURRENT fold's behavior, which is far beyond a scorecard-ingestion PR's scope. Related but lower-priority: `normalizeTimeKey`/`normalizeSiteKey`'s `looseKey` fallback (`src/ingest/upsert.ts`) strips everything outside `[a-z0-9]` rather than folding compatibility variants first, so a fully-full-width scheduled time or site would take the DIFFERENT `lit:` fallback branch instead of matching its plain-ASCII equivalent's `raw:` key — the same shape, on a field far less likely in practice to arrive full-width than a name would. Recorded for HC triage; no issue opened.
- 2026-07-31 · process · (#56, PR for it, follow-up to #46/PR #52 recommended by the Codex reviewer in rounds 8 and 11 AFTER it had approved the PR) the duplicate-`tennisrecord_url` migration-failure message emitted a copy-pasteable `mv` command, and that one line generated a defect in SIX of the eleven review rounds on that PR — round 1 named the wrong database (`rm data/nadal.db` while `runMigrations(path)` takes any path), round 2 clobbered an existing backup, round 3 disputed BSD `mv` and `--`, round 5 let a control-character path name a normalized sibling, round 6 found `JSON.stringify` not lossless for DEL/C1/`\p{Cf}`/U+2028-9, round 7 found lone surrogates skipping the fail-safe entirely. Every one was real, every one was fixed and regression-tested, and the surface was then owned FOREVER: four cooperating mechanisms (`shellQuote`, `untakenBackupPath`, absolute-path resolution, the `UNRENDERABLE` fail-safe) plus `losslessPath` for the fail-safe's own output. #56 deleted the command; the message now emits cause + losslessly-rendered database path + a link to `docs/runbooks/db-migration-recovery.md`, which already carried the command in markdown where no sanitizer eats it. The transferable rule is about WHERE a capability should live, not about how carefully to build it: an error string that hands a human something EXECUTABLE has to be correct against the shell, the filesystem, the encoder and the platform simultaneously, and each of those is a moving target owned by someone else — whereas the same command in a runbook is correct by construction because markdown is not a consumer that transforms it. Prefer diagnosis-plus-pointer to instruction-plus-payload whenever the payload would be executed. Two corrections to earlier lines in this log, recorded here rather than by editing them, since this file is append-only: the seam guard described at :231 now asserts the DATABASE PATH and the runbook link survive contiguously through `emitSummary` rather than the whole `mv` command (the seam it guards, and the merge-born defect class, are unchanged — only the payload moved), and the round-2 clobber regression it references was DELETED rather than retargeted, because with no backup name in the message its premise is unconstructable and `rules/testing.md` forbids a check no fixture can distinguish; its equivalence argument now lives as a comment where the test was, and the surviving no-command-at-all assertion rejects `.bak` outright, which is strictly stronger
- 2026-07-31 · process · (#18, PR #54, spanning rounds 5-6 — the most transferable lesson of the run) a SECURITY residual was written down accurately and shipped anyway, and the sentence that recorded it turned out to describe the vulnerability rather than a footnote to it. Round 5's fix for the `sourceImage` arbitrary-file-read logged, in the code and in this file, that `assertInputPathSafe` is "NOT fd-anchored like the write side — a narrower check-then-use window". Every word true. Round 6 then showed that window WAS the hole: a hard link inside the configured root satisfies containment, symlink refusal, size and magic-byte checks simultaneously, because all four describe a PATHNAME while the thing read is a FILE. The orchestrator (me) read that residual, accepted it, and approved the commit. Two rules worth keeping, and they are different from the usual "don't overclaim" lesson this log already records many times. First: RECORDING a limitation and EVALUATING it are separate acts, and for a security control the second is not optional — the question a residual must answer before it ships is not "is this sentence accurate?" but "what does an attacker do with exactly this?", which is one adversarial minute and was never spent. Second, the sharper one: the fix was the wrong CLASS of check, not an incomplete one, and the tell was available statically — every validation named a NAME while the operation consumed a FILE. When a control validates one kind of thing and the protected operation consumes another, no amount of adding checks of the first kind closes it; that mismatch is the thing to look for, not the count of checks. Compounding it: the correct technique was already in this repo — PR #48's `openNewOutputFileSafely` does post-open symlink/inode/link-count re-verification on the WRITE side — and was not carried across to the read side, so the fix existed in the codebase, one file away, while the hole shipped
- 2026-07-31 · process · (#19) the `Untranscribed findings:` marker (PROJECT.md → *Rule-suggestion disposition*) is a bare literal string, so a `contains`-based scan of an issue's comments matches any comment that merely *describes* the mechanism — on this very issue the only "hit" was the plan comment explaining that the scan would be run. A marker that appears in the prose describing it cannot be found by naive substring search; the scan needs a stricter shape (line-anchored heading) or manual disambiguation, and a run that trusted the count would have transcribed its own plan text as a finding.
- 2026-07-31 · process · (#19) in a git worktree, `git checkout main` fails with "already used by worktree", so the reflexive branch-from-main sequence silently does not run; `git checkout -b <branch>` then bases the new branch on whatever the worktree had checked out — here a *merged* feature-branch tip four commits behind `origin/main`. Caught by checking `git rev-parse HEAD` against `origin/main` before any work. The worktree-safe form is `git checkout -b <branch> && git fetch origin main && git reset --hard origin/main`.
- 2026-07-31 · process · (#19) nothing in nadal's Quality Checks reads `docs/runbooks/`. `test/cli-grammar-parity.test.ts` parity-checks only `docs/cli/GRAMMAR.md`'s `## Commands` table (noun/verb/summary, both directions) and never its prose; `scripts/parity_check.rb` checks the ace config bundle. A runbook naming a command, flag, or positional shape that does not exist has zero automated detection — which is why this phase's dry run was built as an executable test pinning the command spine rather than as a prose walkthrough.
- 2026-07-31 · process · (#19) `docs/runbooks/README.md` advertised `backup-restore.md` as a "`tn db backup / restore` drill" while neither verb exists in `src/cli/router.ts` (both are *Planned* in GRAMMAR.md). An index that names a future CLI surface in the present tense reads as a description of what ships; the runbook that eventually lands then either invents the commands or quietly contradicts its own index entry.
- 2026-07-31 · process · (#19) a plan named a fixture path (`test/fixtures/tennisrecord/profile.html`) for a `player pull --from` step without checking the parser's requirement; that page has no match-history table, so `parseMatchHistory` rejects it and the step could never pass. Naming a concrete fixture in a plan is a claim about a parser contract, and it costs one grep to check at plan time versus one failed implementation round to discover.
- 2026-07-31 · bug · (#19) `src/report/html.ts`/`markdown.ts` print the identical sentence — *"Not available in this build (no home team configured)"* — for BOTH reasons `dossier.team.headToHead` can be `null` (`src/report/write.ts`: `versusTeamId` is `undefined` when no home team exists at all, AND when the team being built IS the home team). Verified against a real build: `tn team home "<team>"` had already run and reported success, and that same team's OWN dossier still printed "no home team configured" — accurate for one of the two cases the message covers, false-sounding for the other. Not fixed (docs-only phase); the runbook's existing "unavailable on the home team's own dossier" explanation is correct about the BEHAVIOR, so only a clarifying note was added there — the on-page TEXT itself needs a wording fix in `src/report/`.
- 2026-07-31 · process · (#19) `sqlite3`'s dot-commands (`.backup`/`.restore`) run their OWN quote-aware tokenizer over the one string-argument they receive, independent of the outer shell's — so `sqlite3 "$DB" ".backup '$path'"` where `$path` contains an apostrophe (`O'Brien.bak`) does not fail cleanly: the apostrophe closes the inner single quote early, `.backup`'s two-argument form (`?DB? FILE`) reads the fragment before the break as an attach-point name, and the LEFTOVER fragment (`Brien.bak'`, unquoted, not absolute) got silently written as a zero-byte file in the shell's own cwd — which in one verification run was this repo's root, caught only by `git status` before commit. Double-quoting the inner argument survives an apostrophe; a broken-quote failure in a tool that opens files should always be followed by a stray-file check, not just a stderr read.
- 2026-07-31 · process · (#19) `docs/runbooks/build-and-print-dossiers.md`'s own preflight step told the operator to read a roster's ratings off `tn team show` — that command's `RosterMemberProfile` (`src/query/team-profile.ts`) carries no rating field at all, by design (ratings live on the player read, `tn player show`, not the team-roster read), so the instruction could never have been satisfied by the command it named. Found only by actually running it against a real seeded database rather than trusting the existing prose; fixed in the same pass rather than logged for later, since Task 5 explicitly scoped "correct drift you find" as in-runbook work, not a triage deferral.
- 2026-07-31 · process · (#19, correction to the `bug` line directly above, appended rather than edited since this file is append-only) that line closed "Not fixed (docs-only phase)" — it **was** fixed, in the same PR, minutes later. The scoping instinct was wrong twice over: PROJECT.md → *Findings-Log Discipline* asks "is something broken?" FIRST and answers "defects get fixed — in the running PR when the fix is in hand", and the phase's own plan carried a standing rule saying the same. Neither is suspended by a task being labelled docs-only; a task's label describes what it set out to do, not what it is allowed to fix when it trips over something broken. The fix cost one line in each renderer — `TeamProfile.isHome` (`src/query/team-profile.ts:169`) already carried the distinction and neither renderer had ever read it, so the data to tell the two cases apart had been sitting there since the field was added. Worth noticing that the defect was found by a DOCUMENTATION task: writing the runbook meant printing a real binder and reading it, which is exactly the thing no test was doing, and the false sentence had survived every unit test because each one asserted on the section EXISTING rather than on what it said.
- 2026-07-31 · process · (#19, PR #58 round 1, Codex adversarial review, rated High) a runbook that tells an operator to paste a stored value into a quoted command template (`tn team pull "<team name>"`) is the SAME defect class as an error message that emits a shell command (the #56/#57 lesson at :292), arriving from the opposite direction — and the earlier lesson's own framing hid it. That entry concluded "prefer diagnosis-plus-pointer to instruction-plus-payload" and named the runbook as the SAFE destination, because markdown does not transform what it carries. True, and beside the point here: the hazard was never that markdown mangles the payload, it is that a HUMAN following the instruction performs the interpolation, and the shell then parses the result. A team name is scraped data (`A"; touch /tmp/pwn; #` closes the quote and runs `touch`), and the very step above it printed those names raw out of the database. The fix is not better escaping advice — it is to never hand over a template to interpolate into: bind the value to a shell variable and pass `"$team"`, which is expanded as one argument and never re-parsed as syntax, or read it in a `while IFS= read -r` loop. Residual, stated in the runbook rather than papered over: a name containing a literal newline splits into two loop records, which fails CLOSED (an unresolved target, nothing written) rather than executing anything.
- 2026-07-31 · process · (#19, PR #58 round 1, Codex adversarial review, rated Medium) `sqlite3 <path> <query>` CREATES an empty database when `<path>` does not exist, so a read-only-looking enumeration step converts a `TN_DB_PATH` typo into an apparently-valid empty answer: the query prints no rows, the operator reads "nothing to refresh", and a decoy file is left behind that a later `tn db migrate` migrates without complaint. An open-creates-a-file API makes a *read* mutate, so any documented read against a user-supplied database path needs `[ -f "$DB" ]` before it — and the same behavior is what makes a failed `.backup` verifiable-looking, since verifying a backup that was never written opens an empty database whose `PRAGMA integrity_check` answers `ok`.
- 2026-07-31 · process · (#19, PR #58 round 1, Codex adversarial review, rated Medium — the sharpest finding of the round) a synthesized fixture whose optional section is EMPTY makes "the parser correctly found nothing" and "the parser is broken" indistinguishable, and a test built on one cannot tell them apart. `buildRosterPage` emitted a header-only local-schedule table, so gutting `parseSchedule` to `return []` left every assertion in a test that advertises itself as a WHOLE-PIPELINE dry run still passing — while a live pull would have silently dropped every fixture. Proven both ways rather than argued: the induced defect passed before the fix and fails after it. The transferable rule is about what a fixture must contain, not about adding another assertion: a pipeline test's fixture has to carry at least one real row of every structure the pipeline claims to parse, because a structure with zero rows exercises the absence path only, and the absence path is exactly the one a broken parser also produces. Worth noting the fixture was header-only for a GOOD local reason (a real schedule row mints an opponent team, which would have polluted the roster assertions) — the resolution was to name the opponent that already exists, not to accept the blind spot.
- 2026-07-31 · process · (#19, PR #58 round 1) `sqlite3`'s dot-commands re-parse their argument with SQLite's OWN tokenizer, a second quoting layer the outer shell's quoting cannot protect — so `.backup "$BAK"` is not safe merely because `$BAK` was read with `IFS= read -r` and quoted. Two layers of quoting means two layers of validation, and the check has to run BEFORE the destructive half: this runbook now refuses a `"` or newline in the path ahead of both `.backup` and `.restore`, having previously only documented that such a path "does not work" — which is a warning printed next to the loaded gun rather than a safety on it. Verified the guard is not over-broad: a legitimate path containing both a space and an apostrophe still passes it and round-trips every row.
- 2026-07-31 · process · (#19, PR #58 round 2, Codex adversarial review, rated High — and it is a correction to MY OWN round-1 fix at :290) the round-1 fix for shell injection in a runbook swapped the quote character and called it solved: `tn team pull "<name>"` became `team='Paste the name between these single quotes'`. An apostrophe closes a single quote exactly as a double quote closes a double quote, and apostrophes in team names are ordinary rather than exotic — so `x'; touch /tmp/pwn; #` executed during the ASSIGNMENT, before `"$team"` was ever expanded. Confirmed by running it. The lesson is not "use better quoting": **the template itself is the defect**, and any instruction whose final step is "splice this text into a command" is unfixable by choosing different delimiters, because the operator performs the splice and the shell parses the result. Two forms actually fix it, and both remove the interpolation rather than escaping it — read the value at a prompt (`printf …; IFS= read -r team`, what this repo's other runbooks already do, and what nothing in the shell ever re-parses), or carry a transport that cannot hold shell syntax at all (here: each team's stored `tennisrecord_url`, which `tn team pull` accepts directly — percent-encoding means no space, quote or newline can appear in it). Recording it because round 1 FELT like a fix, was written up confidently as one, and shipped the same class of bug one character to the left.
- 2026-07-31 · process · (#19, PR #58 round 2, Codex adversarial review, rated High) round 1 documented a residual — "a name containing a newline splits into two loop records, which fails CLOSED: both halves are unresolved, nothing is written" — and that sentence was simply **false**, asserted from reasoning and never executed. If a team is stored as `North⏎South` while teams named `North` and `South` also exist (ordinary, not contrived), each half resolves EXACTLY against a real unrelated team and live-pulls it, updating and potentially retiring rosters nobody asked to touch. This is the same shape as the security-residual lesson at :294 and it is worth the second entry precisely because that one was already written down: recording a limitation and EVALUATING it are separate acts, and the second is where a "known limit" is either confirmed or exposed as the bug. The tell was available without any tooling — the claim was about what `tn` does with a half-name, and nothing in the run ever ran `tn` with a half-name. A residual stated in a runbook is a testable claim; if it is not worth testing it is not worth asserting.
- 2026-07-31 · process · (#19, PR #58 round 2, Codex adversarial review, rated High) `sqlite3 "$DB" "select …" | while IFS= read -r x; do …; done` reports **the status of the loop**, never the producer's — so an unreadable or corrupt database emits zero rows, the loop body never runs, the pipeline exits 0, and a total enumeration failure is indistinguishable from "there was nothing to do". The same construct hides per-item failure: with items A then B, a failed A followed by a successful B leaves the loop's status 0, so a partial refresh reads as a clean one. Both confirmed by running them. Three changes fix it together and none is sufficient alone: enumerate into a file and check the producer's own exit status; drive the loop with `< file` (a redirect, so it runs in THIS shell) rather than a pipe (a subshell); and accumulate an explicit `failed` flag inside it, because a loop's exit status is only its last iteration's. Generalizes past sqlite3 to every `producer | while read` in any runbook or script.
- 2026-07-31 · process · (#19, PR #58 round 2, Codex adversarial review, rated Medium) a guard was added to the enumeration snippet and **not** to the separately-pasteable backup block, so `.backup` still opened a nonexistent source database — SQLite creates one, `.backup` faithfully copies it, and the result is a real, structurally valid, EMPTY backup whose `PRAGMA integrity_check` answers `ok`. The transferable rule is about runbook structure rather than about sqlite3: **a runbook's unit of use is the fenced block, not the document**, because an operator pastes one block at a time and blocks get run out of order, re-run alone, or copied into a note. Every precondition a block depends on has to live IN that block, even when that means the same two lines appear four times — "it was checked earlier in the file" is an assumption about reading order that the artifact's own format invites breaking.
- 2026-07-31 · process · (#19, PR #58 round 3, Codex adversarial review, rated High) `[ "$failed" -eq 0 ] || echo "STOP: …" >&2` ends the block with **echo's** exit status, which is 0 — so the accumulator that existed specifically to stop a partial refresh reading as a clean one still let the block exit 0, and the prose asserted the opposite. A `cond || action` idiom reports the ACTION's status whenever the condition is false, which inverts its meaning at exactly the moment it fires; `if cond; then …; exit 1; fi` is the form that actually fails. Three rounds in a row now, the same shape: a guard was added, the guard's own exit path was not checked, and the runbook confidently described a behavior nobody had run. Round 1 added the accumulator, round 2 hardened the loop around it, round 3 found the accumulator never mattered.
- 2026-07-31 · process · (#19, PR #58 round 3, Codex adversarial review, rated High) the "a runbook's unit of use is the fenced BLOCK, not the document" rule written into this log one round earlier at :296 was violated in the same file that motivated it: the guard went into step 2's first fence and not its separately-pasteable readback fence, so `sqlite3 "$BAK" "select count(*)…"` on a missing path still CREATED an empty database — which then satisfied step 3's `[ -f "$BAK" ]` and could be restored over a live database. Writing a rule down does not apply it to the artifact retroactively; the round that states a rule should re-audit every existing instance of the pattern in the same pass, not just the one that triggered it. Also fixed properly rather than only guarded: verification reads now open `file:$BAK?mode=ro`, so a wrong path cannot create anything even if a guard is someday missed again — a read that cannot write is stronger than a check that a file exists.
- 2026-07-31 · bug · (#19, PR #58 round 3, Codex adversarial review, rated High) `resolveTargetUrl` (`src/ingest/team-pull.ts:48-53`, and its twin in `player-pull.ts`) accepts ANY `http://`/`https://` target with no host validation, and `pullTeam` then persists that exact string as `teams.tennisrecord_url` (:146). One operator-chosen pull against another host therefore leaves that host in the column permanently — and a bulk refresh that enumerates the column re-fetches it on EVERY run from then on, letting that page rewrite the roster. The distinction that matters is not "can the operator target an arbitrary URL" (they can, deliberately, and a human is in the loop) but "does a one-time target silently become a standing automatic one" — which is what an unfiltered batch converts it into. Closed at the artifact level in this PR: the runbook's batch selects only `tennisrecord.com` hosts and PRINTS anything it skipped rather than dropping it silently. NOT fixed in `src/`: adding a host allow-list changes the ingest contract for `team pull`, `player pull` and the `tr:` prefix together, and whether nadal should refuse a non-TennisRecord host outright, warn, or make it configurable is a product decision with real blast radius rather than an obvious repair — raised to the HC rather than decided unilaterally inside a Phase-7 verification PR.
- 2026-07-31 · process · (#19, PR #58 round 4, Codex adversarial review, rated High) fixing an unsafe pattern in the file that motivated it, while leaving four sibling files demonstrating the same pattern, makes the PR contradict itself: `pre-tournament-full-pull.md` said "never paste a scraped team name between quotes" while `build-and-print-dossiers.md` — edited in the same PR — opened with `tn team pull "<team>" --players` as its first instruction, and three more runbooks did the same. A newly-written rule is a claim about the whole artifact set, not about the file that happens to state it, so the round that introduces one owes a sweep of every sibling for the same shape. This is the docs-sweep-by-concept lesson already in this log arriving with teeth: the concept was "an instruction whose final step is splicing untrusted text into a command", and grepping the CONCEPT (`pull "<`, `home "<`, `show "<`) found every instance in seconds. Also worth keeping: the sweep distinguished *executable* sites (a fenced block or a precondition bullet an operator runs) from *descriptive* prose naming a command form, and only the former were rewritten — a rule applied mechanically to prose would have made four files worse.
- 2026-07-31 · process · (#19, PR #58 round 4, Codex adversarial review, rated Medium — and it is a defect INTRODUCED by round 3's own fix) `file:$BAK?mode=ro`, adopted in round 3 to make a verification read incapable of creating anything, added a THIRD parser between the operator and the file: `?` starts the URI's query string, `#` a fragment, `%` an escape. So a perfectly legal backup path `/tmp/good?x=1` becomes `file:/tmp/good?x=1?mode=ro`, which opens `/tmp/good` — a DIFFERENT database — with `mode=ro` silently dropped, and the runbook then certifies row counts from the wrong file before restoring the unverified backup. Confirmed by building both files and watching the read return the decoy's rows. The shape worth remembering: a fix that adds a layer adds that layer's parser, and "this is read-only now" was reasoned about as a permission change when it was also a syntax change. The repair refuses `?`/`#`/`%` alongside the `"` and newline the dot-command already refused — one refusal covering all three parsers, rather than three escaping strategies — because the set of characters a path may contain is a far smaller thing to get right than the composition of three quoting rules.
- 2026-07-31 · process · (#19, PR #58 round 4, Codex adversarial review, rated Low) a cleanup that runs only on the happy path is not cleanup: `urls="$(mktemp)"` followed by a loop of network pulls leaked the file — which lists every refresh target — on Ctrl-C or session termination, because the `rm -f` sat after the loop and the error branches, and a signal reaches neither. `trap 'rm -f "$urls"' EXIT HUP INT TERM` immediately after the `mktemp` is the fix, and EXIT alone is not enough for an interactive block an operator is likely to interrupt mid-run. Verified by interrupting a real script and confirming the file was gone.
- 2026-07-31 · process · (#19, PR #58 round 5, Codex adversarial review, rated Medium) a signal trap that only CLEANS UP does not cancel anything: in POSIX sh the handler runs and control returns to the next command, so `trap 'rm -f "$urls"' EXIT HUP INT TERM` around a loop of network pulls meant Ctrl-C deleted the temp file and then calmly kept issuing the remaining pulls. Proven by running both forms against SIGTERM — the cleanup-only trap logged `CLEANED CONTINUED CLEANED`, the split form logged `ABORTED CLEANED`. The fix is two traps with different jobs: `trap cleanup EXIT` (which also preserves an already-set nonzero status) plus `trap 'cleanup; exit 130' HUP INT TERM`. Methodology note worth as much as the finding: the first attempt to test this appeared to show BOTH forms continuing, because a background job started by a non-interactive shell has SIGINT set to ignore — the test was wrong, not the fix, and `kill -TERM` showed the real difference. A negative result from a harness that cannot deliver the stimulus is not evidence.
- 2026-07-31 · process · (#19, PR #58 round 5, Codex adversarial review, rated Medium) the round-4 sweep for unsafe runbook shell searched for the paste-into-quotes SHAPE and therefore missed a different hazard in the same files: `in-event-screenshot-ingest.md`'s verification step hardcoded `sqlite3 data/nadal.db …`, ignoring `TN_DB_PATH` entirely, so it read a stale database whenever one was configured — and created a decoy when that default path held nothing. A readback that silently inspects a DIFFERENT database than the write it is verifying is worse than no readback, since it manufactures confirmation. Two rules fall out: a sweep should enumerate every executable site of a KIND (here: every `sqlite3` invocation in `docs/runbooks/`) rather than every instance of the one pattern that prompted it, and any documented command that reads application state must resolve that state the same way the application does — `${TN_DB_PATH:-data/nadal.db}`, matching `dbPath()`, never a hardcoded default. The same step also interpolated a pasted `teamMatchId` straight into SQL; it is now checked numeric first, on the principle that a readback is the last place a value should reach a query unvalidated.
- 2026-07-31 · process · (#19, PR #58 round 5, Codex adversarial review, rated Medium) `mode=ro` was documented as meaning the read "cannot create or modify anything", which overclaims: SQLite's WAL documentation permits a read-only connection to create `-wal`/`-shm` sidecars when they are absent and the directory is writable, so the guarantee is about the DATABASE's contents, not about the filesystem. For a finished, quiescent backup the stronger and more accurate form is `mode=ro&immutable=1`, now used for both backup verifications; the live-database reads keep plain `mode=ro` (immutable would be a lie about a file `tn` may be writing) with the claim corrected to say what it actually guarantees. Honest limit on the verification: attempting to reproduce sidecar creation under `mode=ro` on this machine produced none, so this rests on the documented behavior rather than an observed failure — which is precisely why the fix was to stop overclaiming rather than to assert a new guarantee I had not demonstrated either.
- 2026-08-01 · process · closed · (#36) **the argument that decided #36 was overstated in its first draft, and the overstatement survived the author's own adversarial pass — the independent Reviewer caught it.** The draft said `--pdf` and ⌘P "drive the same Chromium over the same inlined stylesheet", concluding "identical bytes on the paper". Neither follows: the runbook pins no browser at all, and a headless Chromium PDF pipeline is configured separately from a print dialog (backgrounds, scale, headers/footers, printer settings are dialog-side). The supportable claim is much narrower — the print CSS is *inlined*, so a preview needs no network or external stylesheet. What is worth keeping is that the correction pointed the SAME way: if headless output need not match the preview, `--pdf` needs its own fidelity verification, which is work the decision avoids rather than saves. Class **C2**, and a reminder that the load-bearing sentence of a change is the one least likely to be attacked by its own author
- 2026-08-01 · risk · closed · (#36) **nobody has ever printed a real binder** — `build-and-print-dossiers.md`'s "what done looks like" (no horizontal scrollbar, no player block split across pages) is asserted from reading `src/report/html.ts`, never from paper or a print preview. Third recorded instance of the class at :185 and :330 (reading the generator instead of the rendered artifact, which hid six defects in one case); what is new is that it is now the *only* unverified leg of a decision that was otherwise settled. **`closed` is the state after two Reviewer findings on the other two candidates, and the reasoning is the reusable part:** `accepted` claims a limitation "decided against on the record" and lives in a closed `residual` Issue; `open` claims live work and lives in a `docket` Issue. **Neither Issue exists, and an agent may not create one** — promotion is HC triage — so both states assert a tracker record that is not there, and `open` was simply the same defect one step sideways. `closed` is the one state whose storage contract ("`docs/findings.md` — nothing moves") is literally true here, and it is honest because the *lesson* genuinely needs no further action; the *verification* is a *do-now* candidate for the HC's triage pass, which is what this log is for. General shape: when no state's storage exists, take the state that describes where the entry actually lives, and put the unresolved question in the text rather than in a state that implies a record nobody wrote
- 2026-08-01 · process · closed · (#36) the two parity gates are **flag-blind in both directions**, so a CLI flag can be added with no MCP equivalent and every check stays green: `test/cli-grammar-parity.test.ts` matches `` /^\|\s*`tn ([a-z0-9-]+) ([a-z0-9-]+)[^`]*`/ `` — noun, verb and summary only, with the `` [^`]* `` deliberately swallowing and discarding whatever flags the cell names — and `Command` in `src/cli/router.ts` has no flags field at all, so a command's flag set is *declared nowhere*, existing only as the literal `parseArgs(args, [], [])` call. `test/mcp-tool-parity.test.ts` likewise compares tool names to `cliCommand`, never `inputShape` to declared flags. That combination would silently break `agent-chat-over-mcp.md`'s byte-identical-from-either-surface promise. Class **C2**; measured on this PR by mutation, below
- 2026-08-01 · process · closed · (#36) a decision to **not** build something is pinned by nothing unless a test names it, and the gap is measurable rather than arguable: flipping `parseArgs(args, [], [])` to `parseArgs(args, ["pdf"], [])` left **1223 of 1224 tests passing**, so before this PR the runbook's bolded *"there is no `tn report build --pdf`"* was prose that any future implementer could contradict without turning anything red. Worth generalizing carefully — a pinning test is green the moment it is written and therefore cannot be TDD'd, so its failure mode has to be *observed by mutation* or it pins nothing; asserting that it would fail is not the same as watching it fail
- 2026-08-01 · note · closed · (#36) one sentence can carry claims that are false in **different ways**, and the truthful edit differs per clause — spec § Stack named three dependencies: Hono (wrong; stdio shipped instead, already on the record), Playwright-for-PDF (declined by #36), and Playwright-for-scraping (**merely unrealized** — #15 chose seam-first, but #27/TennisLink is open and may still need a browser). The blunt edit the assessment first proposed, "drop Playwright", would have silently pre-decided #27 inside a PR about printing. Generalizes to the whole class: *decided-against* and *not-yet-built* look identical in a stale doc and must not be collapsed when correcting one
- 2026-08-01 · process · closed · (#36, PR #61, Codex adversarial review, rated Medium) a test that pins a **decision not to build something** must assert the OUTCOME, not the rejection mechanism: asserting only `exit 1` + `unrecognized flag --pdf` would still pass if a future change wrote `reports/index.pdf` on seeing the flag and *then* fell through to the same parser error, so the test proved "this flag is not declared" while its own comment claimed it proved "no PDF is produced". Fixed by also asserting the reports root contains no `.pdf` after each dispatch. The general shape is the standing one — assert behaviour, not the path that happens to implement it — but the absence-pinning case is where it is easiest to miss, because the mechanism assertion *feels* like the whole point when the feature does not exist. Note the mutation check did not catch this either: flipping `parseArgs` to accept `pdf` kills the exit-code assertion, so the test looked adequately load-bearing while still under-specifying its own claim. Class **C2**
- 2026-08-01 · process · closed · (#36, PR #61, Codex round 2, rated Medium) **the replacement rationale for a retracted claim is itself unreviewed prose, and it carried a new false statement.** Round 1 killed "same Chromium / identical bytes"; the round-1 fix substituted "nothing here is batch or unattended … `--pdf` would automate a keystroke", which round 2 refuted from the code: `tn report build sectionals` routes to `writeSectionalsDossiers` and writes **every** team's dossier in one command, so printing that binder is one ⌘P-and-save *per team* and `--pdf` would fold N operations into an existing batch. The decision survived on **proportion** (a bounded one-event saving vs. a permanent dependency), which is what should have been written first. The class is the thing to carry: a fix authored to replace a refuted claim gets none of the scrutiny the original got, and the temptation is to reach for a *stronger* substitute than the evidence supports — the second overclaim is easier to write than the first, because it feels like a correction
- 2026-08-01 · process · closed · (#36, PR #61) **the review bound did its job: two fix-verification passes were spent, and the second still found defects in the fixes, so the run stopped at the merge gate rather than opening round 3.** PROJECT.md → *Review Lenses* caps fix-verification at two passes and says escalate rather than iterate; this run reached exactly that state — the delivered head carries fixes that no Reviewer has seen, so `attested`'s SHA-binding condition is unmet and the SOW names it instead of the AC certifying its own last edit. Worth recording because the cap is easy to spend without noticing: pass 1 was the AC's own mutation + check re-run, pass 2 was the Reviewer, and neither felt like "a round" at the time
- 2026-08-01 · fact · closed · (#27, ship run, Task 1 capture session) TennisLink's authenticated URL grammar, read off the signed-in dashboard rather than guessed: the league team page is `leagues/Main/StatsAndStandings.aspx?t=<id>&par1=<>&par2=<>` — **all three params together**, since `?t=<team#>` alone returns `Leagues/ErrorHandling/RecordNotFound.aspx` even for a team the signed-in user captains, and the displayed 10-digit "Team #" is not the `t` value. Identity params carried elsewhere on the site: `PersonID` (`ShowImage.aspx`), `MemberNum` (`Preauthorize.aspx`), `PlayerID` (`RankingHome.aspx`). Page chrome also renders the signed-in user's own name and a 10-digit `USTA Account #` on every page. That last one is the redaction-relevant part: it is the *capturing operator's* identity, not the subject's, so it is exactly the class a substitution map built from scouting targets would omit.
- 2026-08-01 · fact · closed · (#27, ship run, assess) "every `tennislink.usta.com` leagues/ and tournaments/ path redirects to OAuth" (issue #27's premise) is too broad: `tournaments/schedule/Search.aspx` returns 200 with ~174 KB unauthenticated. The **leagues/** module is gated and the **tournaments/** module is at least partly public. Does not unblock #27 — all three artifacts it wants live on the leagues side — but the blanket claim should not be re-derived. Also: the gate chains through TennisLink's own `Dashboard/Main/Login.aspx` before `account.usta.com/authorize?...&audience=tennislink`, so a probe that only checks for an `account.usta.com` Location header sees a same-host 302 and can misread it as reachable.
- 2026-08-01 · process · closed · (#27, ship run, Task 1) driving an authenticated legacy WebForms site from an automation harness failed three independent ways, and the lesson is to stop earlier than I did: (a) the team/player links are `javascript:` hrefs, so synthetic clicks navigate nowhere; (b) setting `<select>` values then firing `__doPostBack` lost sibling field values, because the postback re-renders from server state rather than from the DOM I had just mutated; (c) the browser extension blocks any tool result that looks like bulk query-string data, so hrefs cannot be harvested to reconstruct URLs. The workaround that *did* work for (c) is worth keeping: extracting only the **parameter names** (`([A-Za-z_]\w*)=` over `outerHTML`) passes the guard and is what a detector set actually needs, so the block cost nothing once the query stopped asking for values. The general shape: when three distinct approaches to the same mechanical step fail, the step is the wrong one — a human already at the keyboard navigates a hostile UI in seconds, and the runbook design (HC drives, tool captures) was right the whole time.
- 2026-08-01 · bug · closed · (#27, found while capturing a live signed-in TennisLink page; a fix was attempted across four review rounds and WITHDRAWN — tracked at #80) `NEVER_PUBLISH` in `tools/redact-fixture.ts` enumerated third-party PII — email, mailto, tel, phone, street address — and had **no credential class**, but a page captured from a logged-in session carries the *capturing operator's* secrets too: a real TennisLink league page ships a 172-character `hdnCSRFToken` and a **14,420-character `__VIEWSTATE`** inline. Nothing had leaked (every committed fixture greps clean, because USTA's profile is a client-rendered SPA and TennisRecord is public — the exposure needed WebForms to arrive), so this was latent rather than shipped. The dangerous part was not the leak path but the *refusal* path: the allow-list correctly refuses a 172-char token, and the documented way to clear a refusal is to classify the atom and **add it to the committed vocabulary** — and an opaque 172-character string reads as "boilerplate" to someone working that loop mechanically. So correct adherence to the written procedure could commit a live session token to a public repo. Fix: empty credential-field values before any sweep (lossless — no parser reads a hidden WebForms field, and the element plus every other attribute survives), with an exact-equality backstop assertion. Emptying rather than substituting is what keeps it out of the refusal report at all, since an empty value reduces to an empty skeleton. The class worth carrying forward: a control that fails *closed* can still fail *badly*, when the documented remedy for the refusal is the harmful act.
- 2026-08-01 · bug · closed · (#27, in the since-withdrawn fix — see #80; caught by executing the claim against the real page instead of only the synthetic fixture) the first version of the credential sweep matched `<input type="submit" id="btnCsrfRefreshPage" value="Refresh Page">` — an id containing "csrf" whose value is the **visible button label** — and emptied it, silently altering user-visible markup for no privacy gain. The synthetic test fixture I wrote from the diagnosis contained no such element, so the unit tests were green while the behaviour was wrong on the only page that mattered. Fix: skip `type=submit|button|reset|image`, whose `value` is a label by definition. This is the *fixes-authored-with-the-diagnosis-are-least-reviewed* shape again, and the thing that caught it was cheap and repeatable — run the actual matcher over the actual page and report counts, which needs no secret to enter the transcript.
- 2026-08-01 · process · closed · (#27, ship run, HC redirected scope mid-`invoke`) the run was planned as three TennisLink parsers and delivered a runbook plus a security fix instead, because the HC asked the right question — *is TennisLink what you actually need?* — and the answer was no: TennisRecord already supplies court slots, partners, opponents, scores and dynamic ratings, and USTA supplies NTRP/WTN/ids, so two of the three planned parsers duplicated existing coverage and the third (championship scorecards) is corroborative. Worth recording as a *plan* lesson rather than a scope-creep one: the assessment correctly identified TennisLink as off the critical path and still recommended building it, because the option set was framed as "how much of #27 do we do" rather than "what does Springfield need from this source that it does not already have". A source-level marginal-value question belongs in Assess, before the options are drawn — the plan-revision that followed was cheap only because nothing had been committed yet.
- 2026-08-01 · bug · closed · (#27, PR #79, Stage-4 adversarial pass, fail-open lens — a defect in this run's OWN fix, found before any reviewer saw it) the credential sweep anchored its attribute patterns with `\b`, and `\b` matches after a hyphen: `\btype=` therefore also matches `data-type=`, and `\bname=` also matches `data-name=`. One direction fails open — `<input data-type="submit" type="hidden" id="hdnCSRFToken" value="…">` was read as a button label and **skipped**, leaving a live token in place — and the other over-strips a benign field whose `data-name` happens to match. Fixed at the time by anchoring to a real attribute boundary, `(?<![-\w])`, rather than a word boundary — though the control that fix lived in was later withdrawn entirely (#80), so nothing in the tree does this today. Two things generalise: an HTML attribute matcher must anchor on the attribute boundary and never on `\b`, since `data-*` prefixing is ubiquitous; and the same pass replaced a hand-copied duplicate of the value regex inside the backstop assertion with the shared constant, because a backstop that disagrees with the thing it backs up is not a backstop. The lens that found it was *fail-open*, chosen at the summons — a round-count bound would not have asked the question.
- 2026-08-01 · risk · open · (#27, PR #79, Stage-4 adversarial pass, permanent "what class is not on this list?" lens; **the first draft of this entry overstated the gap and was corrected by a later review round — the correction is the more useful half**) TennisLink addresses its league pages as `StatsAndStandings.aspx?SearchType=3#&&s=<33-char token>`, and the entity reference lives only in that token, so it cannot be dropped from a provenance sidecar. I first recorded this as "no layer examines a URL token". **That was wrong**: `capture-fixture.ts` runs `sourceUrl` through `redact()` *with the vocabulary*, so the allow-list atomises it like any other content and an unrecognised token **refuses the capture**. The boundary holds. What is genuinely missing is a *credential-specific classifier* — nothing can say whether that token is an entity key or session state — so the refusal drops the operator into the vocabulary loop, where the tempting move (commit the token to make the refusal go away) is exactly the harmful one. Left `open` because the question "is that token session-bound?" is unanswered and will be reached by whoever captures the first TennisLink fixture; the cheap test is whether the same URL resolves from a different session. The lesson worth more than the finding: **I asserted a gap without checking whether an existing layer already covered it**, which is the same error as claiming a protection that does not exist — overstating a hole erodes trust in the log exactly as overstating a safeguard erodes trust in the code, and both were caught only by a reviewer reading the claim against the source.
- 2026-08-01 · bug · closed · (#27, PR #79, Codex adversarial review — three confirmed defects in my own fix, which triggered PROJECT.md's escalate-on-recurrence rule) hand-written regex over HTML attributes produced a NEW lexical defect every round rather than converging: round 1 `\b` matched after `-` so `data-type` skipped a live token; round 2's `(?<![-\w])` did not cover `:`, so the legal attribute name `data:type` skipped it too; and independently `<(?:input|meta)\b[^>]*>` truncated the tag at a `>` INSIDE a quoted value, so `value="abc>SECRET"` survived untouched — **with the backstop assertion sharing the identical bypass because it reused the same matcher.** Still unexplored at that point: unquoted values, mixed quotes, malformed tags. The escalation rule fired correctly and honoring it was right: the answer was not a fourth delimiter patch but a different design — locate with the real parser (`cheerio` + parse5 `sourceCodeLocationInfo`, giving authoritative `[startIndex, endIndex)` spans and parsed `attribs`), then splice the value inside the parser-supplied span. That closes hyphen, colon and quoted-delimiter simultaneously, because `data-type`/`data:type`/`type` are simply distinct keys and tag boundaries are the tokenizer's problem. **The design was already the house pattern** — `src/parsers/` locates with cheerio, and courtgrab2 before it used Nokogiri `.css()` — this one function had deviated from it. Carry forward: when a tag- or attribute-matching regex produces a SECOND delimiter defect, stop and reach for the parser that is almost certainly already a dependency; and a backstop that reuses the matcher it backs up is not a backstop.
- 2026-08-01 · bug · closed · (#27, PR #79, caught by TDD immediately after the cheerio rework) building a regex with `new RegExp(\`…\${attr}…\`)` inside a **template literal** ate one layer of escaping before the RegExp constructor ever saw it: `\\s` became `s`, so the pattern read `[s/]value s* = s*` and matched nothing. The control silently stripped **zero** fields while `tsc` passed clean and the code read correctly at a glance — a security control that had become a no-op. Fixed by using static regex **literals** keyed by attribute name instead of composing patterns from strings. The general rule: a regex that must survive two layers of escaping (template literal → RegExp) is one layer too many for anything security-bearing; write the literal, close the input set, and let TDD prove it fires. Worth noting the failure mode is *silent and passes review* — nothing about `[s/]value s*` looks wrong unless you execute it.
- 2026-08-01 · bug · closed · (#27, PR #79, Codex adversarial review round 2 against the REWORKED design, rated critical) a parsed attribute map is a **lossy summary of the bytes**, and the credential control trusted it. Given `<input id="hdnCSRFToken" value="public" value="SECRET">` — malformed but browser-tolerated — parse5 keeps only the FIRST `value` and discards the second, so reading `attribs.value` emptied the decoy and left the real token in the file; the backstop then **agreed the output was clean, because it read the same lossy view**. Same on `<meta content="ok" content="SECRET2">`. Fixed by making the writer patterns global (empty every occurrence in the span) and, more importantly, by having the assertion scan the span's **raw bytes** instead of `attribs`. The rule this encodes and the reason it is worth more than the individual fix: **use the parser to DELIMIT, read the BYTES to decide** — anything that will be written to disk must be checked as written, not as parsed. Note this is a genuinely different class from the three delimiter defects that preceded it (those were the tag matcher being lexically wrong; here the parser was correct and the gap was between its normalized view and the file), which is why it warranted a fix rather than a second escalation. Also worth keeping: the regression test asserts the backstop rejects the ORIGINAL input, not merely that it accepts the cleaned output — the weaker assertion passes for a backstop that does nothing.
- 2026-08-01 · bug · closed · (#27, PR #79, Codex adversarial review round 3, rated critical — and it is the SAME CLASS as round 2's, which is what finally settled the design) an unquoted attribute value containing `=` bypassed both stripper and backstop: `<input type=hidden id=hdnCSRFToken value=x=98765432101234567890>`. The HTML spec forbids `=` in an unquoted value, but the spec's own *error handling* appends it, so parse5 read the whole `x=9876…` while my spec-shaped character class stopped at `x` — producing `value=""=98765432101234567890`, a dangling token tail that the backstop then certified as clean. Round 2 was the same shape (parse5 keeps the first of duplicate attributes; my pattern read the other), so this was the second consecutive round in which **a hand-written byte pattern disagreed with the parser about what an attribute value IS**. Patching the character class would have been a third instance. The class only closes by deleting the byte pattern, so the tag was **rebuilt from the parser's attribute map** instead, making duplicates collapse, quoting normalises, and `=`/`>`/mixed quotes/case/self-closing stop being questions this code answers. The cost — a credential tag loses its original formatting — was acceptable precisely and only there, because these are hidden inputs and metas no parser in `src/parsers/` reads. That design is recorded for #80 rather than shipped: round 4 broke it again and the control was withdrawn. Carry forward: when a byte-level pattern and a real parser disagree twice, stop reconciling them and delete the pattern.
- 2026-08-01 · bug · closed · (#27, PR #79, found by sweeping my OWN traces after the round-3 redesign, before any reviewer saw it) the duplicate-attribute class applied to the **exemption** rather than the value, which is where it is most dangerous: `<input type="submit" type="hidden" id="hdnCSRFToken" value="SECRET">` — parse5 keeps the first `type`, so the label-bearing exemption fired and the credential field was skipped **entirely**, token intact. Every other duplicate defense I had added covered `value`/`content`; none covered the attribute that decides whether to look at all. Fixed by refusing the exemption whenever `type` occurs more than once in the raw span — a duplicated attribute means the parsed view cannot be trusted about that element, and an exemption must fail closed. The general lesson is about where to look after fixing a class: I had propagated "duplicates are lossy" to the *values* and stopped, when the same fact invalidated the *guard condition* one line above. A fix for a class is not done until every consumer of the same underlying assumption has been re-checked — including the ones that decide whether the fixed code runs.
- 2026-08-01 · process · closed · (#27, PR #79, after Codex adversarial review round 4 — the decision to WITHDRAW rather than ship) a partial security control that operators trust is worse than an absent one they do not, and that is the argument that settled it. Four rounds produced seven-plus confirmed defects in a credential stripper, and the last three were **one class** (parse5's collapsed attribute map disagreeing with the raw bytes; every raw-span regex written to reconcile them missed a new duplicate form: duplicate `value`, unquoted `value=x=…`, duplicate `id`, bare boolean `type`, `type=` inside a quoted value). PROJECT.md's escalate-on-recurrence rule had already fired once and been overridden by an HC decision to proceed; the second time, the right move was not a fifth patch but removing the feature from the PR. What tipped it was not the defect count but the **claim**: the runbook I had written told operators "session credentials in known fields are handled for you", so shipping with known bypasses would have stopped them looking — strictly worse than the honest "nothing strips these, here is exactly what to grep for" the runbook now says. The reusable shape: when deciding whether to ship a partial control, ask what the DOCUMENTATION will claim and who stops checking because of it, not just what fraction of cases the code covers. Withdrawn to #80 with every trace written up, so the next attempt starts from the failures rather than rediscovering them.
- 2026-08-01 · process · closed · (#27, PR #79, the whole-run shape) this run planned three parsers, delivered none, and its most valuable output was a runbook plus a **withdrawn** feature — yet it was not wasted, and the reason is worth keeping. The `/ship` lifecycle's terminal artifact is a mergeable PR, which creates quiet pressure to land *something* from every stage; what actually protected quality here was that the two governance rules pointed the same way at the end (escalate-on-recurrence said stop patching; `attested` said no merge with open must-fix findings), so "ship less" was the only conforming move. Two lessons. First, the **capture attempt was the valuable part even though it produced no fixture**: everything real in this PR — the URL grammar, the `javascript:`-link obstacle, the credential class, ten findings entries — came from touching the live system, not from planning. Second, **a run that ends by removing its own feature and filing the gap is a successful run**, and the lifecycle should be read that way rather than as a failed delivery; the SOW says so explicitly so a later reader does not mistake #80 for abandoned work.
- 2026-08-01 · process · closed · (#27, PR #79, Codex adversarial review — the sixth consecutive round to find a stale or overstated documentation claim, and the sharpest of them) **when a control is withdrawn and replaced by a manual procedure, the procedure inherits the control's failure modes and nobody re-derives them.** The manual grep I wrote to replace the withdrawn credential stripper matched field *names* and instructed the operator to "delete the value by hand" for any non-empty hit — which on TennisLink's real `<input type="submit" id="btnCsrfRefreshPage" value="Refresh Page">` means deleting the words off a button. That is the *exact* false positive the automation had hit and that I had already fixed in code, three days of review rounds earlier; the fix simply did not travel from the code into the prose that replaced the code. Two lessons, and the second is the one worth carrying: (1) a documented procedure needs the same adversarial review as the code it replaces, because prose failure modes are invisible to every check in the repo — this one survived six review rounds precisely because nothing executes it; (2) the moment a fix is *deleted*, every lesson embedded in it becomes homeless, so withdrawing code should trigger an explicit pass over the docs asking "what did that code know that this prose now has to say out loud?" — the button-label exclusion, the value-vs-label distinction, and the attribute scoping were all knowledge the deleted function encoded and the runbook silently dropped.
- 2026-08-01 · bug · closed · (#27, PR #79, Codex adversarial review round 7 — a control-flow deadlock in prose, which no check in this repo can see) the runbook told an operator to scrub session credentials **from the redacted fixture**, at a step after the capture. That fixture does not exist: `tools/capture-fixture.ts` runs the allow-list **before either output file is written**, and the runbook itself says a 172-character token is refused — so on a credential-bearing page the capture produces nothing to edit. An operator following it literally is stranded, and the only exit from that refusal is the single move the same page forbids: adding the token to the vocabulary. Fixed by moving the scrub to a new step **4**, operating on a `.scrubbed` copy of the saved page *outside the repo*, before any capture runs, with an explicit instruction never to re-run the capture against the unscrubbed original. Two things generalise. **A procedure has control flow, and it can deadlock exactly like code — but nothing executes prose, so no test, linter or type-checker will ever catch it; only a reader walking it as an operator will.** And the specific trap here was ordering-by-narrative: "read the fixture before committing it" *reads* like the right final step, which is why the error survived six earlier review rounds that were all checking whether individual sentences were TRUE rather than whether the sequence was EXECUTABLE. Truth-of-claims and executability are different audits and the first does not imply the second.
- 2026-08-01 · bug · closed · (#27, PR #79, Codex adversarial review round 8 — the SAME deadlock as round 7, reopened by an incomplete fix) round 7 moved the credential scrub before the capture and I updated the step's *prose* — "run the capture, against the **scrubbed** copy from step 4" — but left the fenced **command block** reading `--file <the saved page>`. The command is the thing an operator copies, so the deadlock was fully intact for anyone who did what the runbook literally says: the unscrubbed original goes back into the allow-list, the capture refuses, no fixture exists, and the only exit is the forbidden vocabulary workaround. The prose and the command contradicted each other four lines apart. Generalises as: **in a procedure, the executable artifact is the claim — prose describing a command is commentary, and when they disagree the reader follows the command.** A fix to a procedure is not applied until the copy-pasteable part is changed, and reviewing "did I fix step 6?" by reading its sentence is precisely how this survived. Related and worth pairing: round 7's lesson was that truth-of-claims and executability are different audits; this one adds that within an executable audit, the *runnable* text outranks the text around it.
- 2026-08-01 · bug · closed · (#27, PR #79, Codex adversarial review round 9 — the round-8 fix broke the OTHER variant, which is the same class inverted) fixing step 6's command to name `<the saved page>.scrubbed` made the login-gated path correct and the **public** path impossible: `.scrubbed` is only created by the login-gated scrub step, so a TennisRecord operator was handed a command referencing a file their workflow never produces. I had "handled" it with a parenthetical — *(on a public page there is no scrub step — pass the saved page itself)* — which is prose contradicting the command four lines above it, exactly the failure round 8 had just diagnosed. Fixed by giving the public variant **its own fenced command** in its own section rather than annotating the login-gated one. The class: **a single parameterised example serving two workflows is a latent contradiction, because the example can only be literally correct for one of them.** When a procedure branches, branch the *executable text*; a note saying "for the other case, substitute X" is commentary, and round 8 established that readers follow the command, not the commentary. Also worth recording that rounds 7, 8 and 9 are one defect surfacing three times — scrub-ordering, then its prose/command split, then its other branch — which is the signature of fixing an *instance* rather than the *shape*, and the shape here was "this procedure has two variants and only one is written down".
- 2026-08-02 · process · closed · (#63, invoke) `node_modules` had not been fully populated at the start of this run — `cheerio`, `domhandler`, and `@modelcontextprotocol/sdk`'s subpath exports were declared in `package.json`/`package-lock.json` but absent on disk, so `npm run typecheck` failed with ~50 errors (`TS2307`/`TS7006`) scattered across files this PR never touches. Confirmed pre-existing (not something my diff caused) by `git stash`-ing every change and re-running typecheck against the clean base, which reproduced the identical error list. `npm install` resolved it in one step. Worth recording because the failure reads exactly like a real typecheck regression at a glance — before assuming a fresh worktree's dependency tree is complete, check `node_modules` state against `package.json`, especially when the errors are all "cannot find module" for packages the manifest already lists.
- 2026-08-02 · process · closed · (#63, invoke, Task 3 — the `slotIndex`/NaN comparator regression test) a comparator that returns `NaN` on an out-of-range comparison is not reliably testable by "reproduce the buggy code path and watch it fail": `Array.prototype.sort`'s behavior on a `NaN` result is unspecified, and for small arrays V8's sort often leaves the compared pair's relative order UNCHANGED on `NaN` — so a fixture built without checking the actual row order can pass whether or not the fix is present, which would have shipped a regression test that certifies nothing. Verified by writing a throwaway Node script that ran both the buggy and the fixed comparator over several row orderings BEFORE writing the vitest test, picked the one ordering where they diverge, then confirmed the vitest test itself fails when the fix (`slotRank`'s `?? Number.POSITIVE_INFINITY` fallback) is reverted and passes when restored. Worth repeating whenever a "silently unstable sort" is the regression under test: the mechanism (NaN in a comparator) reproducing is not the same as the OUTCOME (wrong order) reproducing, and only the latter is what the test needs to pin.
- 2026-08-02 · bug · closed · (#63, invoke, test/query-events.test.ts, found before any reviewer saw it) a pre-existing test built a second `addEvent(db, {...})` call by spreading `other` — the `AddEventResult` returned by an EARLIER `addEvent` call — into the new call's input object. That was harmless while the two types happened to share every field name, but `AddEventResult` gaining `format: EventCourt[] | null` this PR (`addEvent`'s own result, distinct from `AddEventInput.format?: string`) made the spread forward a `format: null` the writer's input type does not accept, crashing at runtime (`parseEventFormat(null)`) with **no compile error** — TypeScript's excess/mismatched-property check does not fire on a spread source the way it does on a literal's own properties. Fixed by building the second call's input explicitly rather than spreading the first call's result. The general shape: a service's OWN result type and its OWN input type sharing field names is a latent coupling invisible until one of them grows a field the other cannot accept — a caller (or a test) that treats "the thing I just got back" as "the thing I pass in next" is the exact pattern that goes uncaught.
- 2026-08-02 · bug · closed · (#63, verify, Stage-4 adversarial pass, rated medium) an ORM column mode can relocate a decode OUT of the guard that was written for it. `events.format` was declared `text("format", { mode: "json" })`, so drizzle called `JSON.parse` while mapping **every row of the events table for every reader** — before `readEventFormat`, the module whose whole purpose is to fail closed on a malformed stored value, could see anything. Bytes that are not JSON therefore threw a raw `SyntaxError` out of `eventsForDay` (`tn player avail`), `match add`, and `addEvent` itself: **four commands with nothing to do with court formats**, none of which catch it, so a single hand-corrupted row broke availability recording with a stack trace. The guard was holed in precisely its own threat model — a hand-edited database is the only reason it exists. Reproduced against a real DB before any fix (`SyntaxError: Unexpected token 'o'`), then closed by making the column plain `text` and giving `event-format.ts` both `encodeEventFormat` and the `JSON.parse` inside `readEventFormat`, so exactly one module decodes it and every failure is one named `InvalidEventFormatError`. Verified migration-neutral first: both spellings emit identical DDL (`format text`), and `drizzle-kit generate` reported "No schema changes, nothing to migrate" — a column MODE is a TypeScript-side mapping decision, not a schema one, which is exactly why it is easy to choose without noticing it moved a parse
- 2026-08-02 · process · closed · (#63, verify) a corruption test that seeds THROUGH the ORM cannot reproduce ORM-level corruption, and passes either way. The first draft of the regression above seeded `db.insert(events).values({ format: "not json at all" })` and passed against BOTH the buggy and the fixed code — because under `{ mode: "json" }` drizzle stringifies whatever it is handed on the way IN, storing `"\"not json at all\""`, which is valid JSON. The seed path was the same path the fix changed, so the fixture could never contain the defect. Only a raw `sqlite.prepare(...).run(...)` writes the bytes a hand-edited database would have. This is the SECOND false-green in this one PR caught the same way (the first was the `NaN` comparator, logged above at `invoke`), and the shared remedy is one move: **revert the fix and watch the new test fail** before believing it. The narrower lesson is about fixture provenance — when a fix changes a write path, a fixture built with that write path is evidence about the fix, not about the defect
- 2026-08-02 · process · closed · (#63, verify) two sibling fields encoding one invariant (`slotSource: "observed" | "event-format"` beside `slotEvent: {…} | null`) produced three copies of the same non-null assertion — `slotEvent!.name`, once in each of the CLI, markdown and HTML presenters — because nothing in the type said the pair cannot disagree. Replaced with a discriminated union INTERSECTED with the rest of the record (`type LineupPlan = LineupSlotProvenance & { …rest }`), which narrows on `slotSource` and deletes all three assertions while leaving the JSON shape byte-identical, so no existing caller or test changed. Two things worth carrying: an intersection is what lets a union express a two-field invariant without turning the whole record into a union, and a test helper typed `Partial<T>` is the hole that lets the invalid state back in — the helper had to take the provenance as a WHOLE pair (a separate parameter) rather than as loose overrides, or a test could still construct a plan production code can never produce
- 2026-08-02 · process · closed · (#63, verify) `git diff main...HEAD` reviewed a diff 28 lines WIDER than the PR, because the local `main` ref was three commits behind `origin/main` — the branch was cut from `origin/main`, so the three-dot merge base resolved to the stale local ref and folded two already-merged PRs' changes into "the diff under review". It fails toward MORE review rather than less, which is why it is easy to miss: nothing looks wrong, the extra hunks are real code, and the only visible symptom here was a findings-log count that did not match (33 added entries where the run had written 6). Fixed the count and re-read the true diff against `origin/main`. The rule worth carrying: at `verify`, diff against the REMOTE base ref the PR is actually opened against (`origin/main...HEAD`), never the local branch name — a local `main` is only as fresh as the last `git pull`, and a worktree's may never have been updated at all
- 2026-08-02 · bug · closed · (#63, listen, Codex adversarial review, rated high) a NEW batch-level guarantee shipped without a batch-level mechanism. `tn report build [target] [event]` documented that the named event's format applies to *every* dossier the run builds, but `writeSectionalsDossiers` resolved the event inside `getLineupPlan`, i.e. **once per team** — so a `tn event add` committing between two teams (nadal runs `tn mcp serve` beside a CLI against one WAL database, so that is an ordinary concurrent-PROCESS interleaving, not a contrived one) let a single batch emit a two-court dossier and a four-court dossier that each name the same event. Fixed by resolving the format ONCE in a new Phase 0 and handing the same immutable value to every team, which also moved the refusal earlier — a bad event name now aborts with nothing written, including on a team set that would produce no lineup at all, where the per-team lookup never validated the name. Worth carrying: this module already carried three prior review findings about batch-level guarantees ("validate every leaf this batch will touch before writing ANY of them"), and the new argument reintroduced the same class one field over — **when a function already documents a whole-batch invariant, a new per-item input is a candidate violation of it by default**, and the place to look is where the item loop calls a resolver
- 2026-08-02 · process · closed · (#63, listen) reproducing a cross-PROCESS race deterministically in-process needs a trigger tied to what the FIX changed, not to a call count. The regression here fires from a `Proxy` on `db.select` — but the first attempt corrupted the row right after `select()` RETURNED, and drizzle's builder is lazy, so nothing had executed yet and the corruption landed before the very query it was meant to follow; the test then failed against the FIXED code. Moving the trigger to the start of the *second* select fixed it, because by then the first query has genuinely run. The general shape: with a lazy query builder, "after the call that reads X" and "after X has been read" are different instants, and a test that conflates them is testing an ordering that does not exist
- 2026-08-02 · process · closed · (#63, listen) `git checkout <file>` used to undo a deliberate experiment ALSO discards uncommitted work in that file — reverting the schema guard to confirm five new tests actually caught the bug wiped the fix those tests were written for, and the suite went green again only because the tests were now measuring the reverted code. Caught immediately here (the tests were re-run), but the safe form is to snapshot to a scratch path and copy back, which is what the other three revert-and-verify experiments in this run used. Prefer `cp` to a scratchpad over `git checkout` whenever the file has uncommitted changes worth keeping
- 2026-08-02 · bug · closed · (#63, listen round 2, Codex adversarial review, rated medium) the fix for "the reader accepts values the writer cannot produce" was itself an ENUMERATION, and the next round found the next hole one delimiter over. Round 1 closed padded slots by requiring `slot.trim() === slot`; round 2 pointed out that `parseEventFormat` splits entries on `,` and takes the FIRST `:` as the separator, so a slot containing either delimiter is equally unwritable — and `{"slot":"D:1"}` was still accepted, reaching `derive.ts` as a court no supported command can record or repair. Banning `,` and `:` by name would have closed round 2 and left round 3 open; worse, a blanket "no whitespace/punctuation" rule gets it WRONG in the other direction, because `"D 1"` contains a space and IS writable (`parseEventFormat("D 1:doubles")` yields it), so the blacklist would falsely reject a legal format. Replaced both rules with one DERIVED check: run the decoded list back through `formatToText` + `parseEventFormat` and require an exact reproduction — which asks the only question that matters (*could our writer have emitted this?*) and stays correct for whatever the grammar becomes. The lesson is the one already logged for regex/delimiter parsing, arriving from a new direction: when a guard's job is "accept exactly what X produces", **execute X** rather than describing it
- 2026-08-02 · bug · closed · (#63, listen round 2, Codex adversarial review, rated medium) a value introduced to FIX a provenance race became a new way to break provenance. `ResolvedEventFormat` was a plain exported `{ slotSet, provenance }`, so a caller could resolve event A and event B and pass `{ slotSet: A.slotSet, provenance: B.provenance }` — predicting across A's courts while all three presenters state the courts came from B. No `any`, no malformed data, and it defeats exactly the guarantee the batch fix exists to provide. Closed by carrying only the event's IDENTITY and deriving the provenance inside `getLineupPlan` (removing the second field there was to disagree with), plus a not-exported `unique symbol` brand so only the resolver can construct one. Shape worth carrying: **a fix that introduces a new shared value should be re-asked the same question the original defect asked.** Here the original was "two reads of one fact can disagree"; the fix passed one fact around instead — and then had two fields that could disagree
- 2026-08-02 · process · closed · (#63, listen round 2) a TypeScript `declare const brand: unique symbol` is type-only and does not exist at runtime, so `{ [brand]: true }` in an object literal compiles cleanly and then throws `resolvedEventFormat is not defined` on the first call. `tsc --noEmit` was green; only running the tests caught it. A branded-opaque type needs a REAL `const brand = Symbol("…")` (which TypeScript still types as `unique symbol`) when the brand is materialized in a value — `declare` is only correct for a phantom field that is never constructed. Cheap to hit, cheap to catch, and a clean reminder that a green typecheck is evidence about types, not about execution
- 2026-08-02 · bug · closed · (#63, listen round 3, Codex adversarial review, rated medium) a `unique symbol` brand does NOT make a TypeScript value opaque on its own, and the obvious hardening would have been a fix in appearance only. The brand was an ordinary object-literal property, so it was ENUMERABLE — object spread copies enumerable own *symbol* keys, and `{ ...a, slotSet: b.slotSet }` therefore produced a fully-branded value pairing event A's identity with event B's courts, cast-free and typechecking clean. The recommended remedy (non-enumerable via `Object.defineProperty`, plus a compile-time regression asserting the spread is rejected) is only HALF right, and the second half is wrong: verified against this project's own tsconfig, `{ ...a, slotSet: b.slotSet }` **still typechecks** as the branded type after the brand is made non-enumerable, because TypeScript models a spread from the DECLARED type rather than from runtime enumerability. So the compile-time regression cannot be written, and the non-enumerable brand alone changes nothing a caller would notice. What closes it is a RUNTIME check in the consumer (`getLineupPlan` refuses a value whose brand is absent), with the non-enumerable brand as the thing that makes the check discriminating. General shape: **when a guard has a compile-time half and a runtime half, verify which half is actually load-bearing before shipping the one that is easier to write** — and `readonly` is in the same family, since it is erased entirely and needed a real `Object.freeze` to stop the courts being mutated between teams
- 2026-08-02 · process · closed · (#63, listen round 3) the fix-verification bound (`PROJECT.md` → *Review Lenses*) was reached and the AC stopped and escalated rather than patching a fourth time; the HC chose to apply the patch anyway, which is the gate working as designed rather than being bypassed — the rule exists to move the decision to a human, not to forbid the change. Worth recording because the escalation was VINDICATED in an unexpected direction: writing the patch surfaced that the reviewer's own recommended compile-time regression was unwritable, so the "obvious" version of this fix would have shipped looking closed while leaving the hole open. Three rounds of the same defect moving sideways is a signal about the DESIGN, and the useful output of stopping was not "do not fix it" but "the next patch needs different evidence than the last three did"
- 2026-08-02 · risk · accepted · (#63, final, Codex attestation review of PR #82 at 631a0bc — a correction to a claim this run made, not a new defect) I asserted in a commit message and a PR comment that a compile-time regression rejecting `{ ...a, slotSet: b.slotSet }` is **unwritable**. That is true for a structural object type and **too strong in general**: a NOMINAL class carrying a private field (`#opaque!: void`) makes the spread fail to typecheck, because a spread cannot carry a private field. So the accurate statement is "unwritable for a branded structural type", and the reviewer classifies the nominal-class form as optional hardening rather than a deployment-shape defect for the CLI/MCP paths. Deliberately NOT applied: the runtime assertion already closes the actual trace, and it would be the fifth patch in the sequence the fix-verification bound exists to stop — the HC made one deliberate exception to that bound, and treating that exception as standing licence is exactly the drift the rule guards against. Recorded as `accepted` (a real limitation decided against on the record) for HC triage; the general lesson is smaller and sharper than the residual: **an absolute claim about what a type system cannot express is almost always a claim about the encoding you happened to choose**, and it was worth asking the reviewer to check reasoning I was relying on rather than only to hunt for defects
- 2026-08-02 · bug · closed · (#62, PR pending) `nameKey` had TWO failure modes, not the one the issue described, and the difference decides how reachable it is. Measured against a live migrated DB rather than reasoned about: ONE OR TWO category-Cf characters left an `editDistance` of 1-2, inside `FUZZY_MAX_DISTANCE`, so tier 3 caught them and returned `ambiguous` — wrong, but LOUD, and it halts a pull to ask the HC about a player already on file; THREE OR MORE pushed the distance past the band, so the ladder fell through every tier and created a second row SILENTLY. The issue described only the silent branch, and an upstream page picks that count for free, so the branch the issue named is the reachable one rather than the unlikely one. The transferable rule is about assessment method, not Unicode: the tracked issue stated the defect CLASS correctly and its REACHABILITY not at all, and a fifteen-line probe against a real DB separated them in a way that reading the ladder could not — running the defect is cheap enough that arguing about its shape instead is the expensive option
- 2026-08-02 · process · closed · (#62) EVERY recursive `grep` in this repo silently omits `src/ingest/upsert.ts`, a 573-line core write-path file holding `upsertTeam`/`upsertPlayer` and six `nameKey` references. It contains a deliberate NUL sentinel (`const NULL_KEY = "\x00null"`, :288), so real grep classifies it `Binary file ... matches` and any `-I`/skip-binary wrapper — the harness default here — drops it from results WITH NO WARNING. Three consecutive blast-radius sweeps of this assessment missed it; it was found only by cross-checking a file READ against a grep RESULT and noticing they disagreed. Use `command grep -rn --binary-files=text` (or `-a`) for any sweep of this tree. The general shape is the dangerous part and is not specific to NUL: a deliberate control character in source changes the file's CLASSIFICATION for every content-addressed tool that walks the tree, and each of those tools reports success either way — a search returning fewer files is indistinguishable from a search finding fewer files, so the omission is invisible exactly when a sweep is being trusted to be exhaustive. Test files here now write format characters as `\u` escapes with named consts rather than literals, so the tests for this defect cannot set the same trap
- 2026-08-02 · process · closed · (#62) `ship`'s delegated `assess` exploration sub-agent NEVER RETURNED — three messages (a status nudge, a correction, an explicit final call) all went unanswered, and the phase completed only because the orchestrator ran the exploration inline under ADR 0003's graceful degradation. Two things worth keeping. First, the degradation clause is what made this a non-event rather than a stall, but nothing TRIGGERED it: there is no timeout, no liveness signal, and no way to distinguish 'still working' from 'dead' — the orchestrator has to decide to give up, and an orchestrator that trusts its own delegation policy will wait indefinitely. Second, and more useful: `ship`'s own delegation criterion (`delegate by OUTPUT-WEIGHT, not per phase`) already answered whether to re-delegate the `invoke` loop — a one-line fold change with a dense Unicode test matrix is signal-heavy and output-light, the exact inverse of the offload test — so running it inline was the policy applied correctly rather than a second degradation. The rule the phase table obscures: the table's rows are a DEFAULT keyed on the typical shape of each phase, and the output-weight criterion above it is the actual test
- 2026-08-02 · note · closed · (#62) grepping an issue's comments for the literal `Untranscribed findings:` marker FALSE-POSITIVES on the plan comment that names the marker while describing the mechanism — the run's own instruction to look for the string is itself a match for it. Harmless here (one comment, read directly, no block present), but the check is specified as a literal-string search in `PROJECT.md` § Rule-suggestion disposition and it will keep matching its own description on every run that mentions it. A marker used as a machine-readable sentinel wants a shape its own documentation cannot accidentally satisfy — a line-anchored heading rather than an inline string
- 2026-08-02 · bug · closed · (#62, PR #81, found by the adversarial pass's PERMANENT lens after the tracked fix was already green) stripping `\p{Cf}` closed the category the ISSUE named and left the DEFECT open: nine other invisible-character classes forked an identity in exactly the same way — NUL, ESC, DEL, the whole C1 block, and variation selectors — measured against a live DB, nine forks out of ten probes. The tracked issue named a Unicode CATEGORY; the actual defect is "a character a human cannot see", and a control character is every bit as invisible on a scraped page and every bit as easy for an upstream template to emit. Closed by deriving the guard as a property ALGEBRA rather than a category — `[\p{Cc}\p{Cf}\p{Variation_Selector}]` MINUS `\p{White_Space}` — where the subtraction is the whole boundary: TAB and NEL are Cc but render as a space or a break, so folding `Jane<TAB>Doe` into `JaneDoe` would be a FALSE MERGE, the silent direction spec § Ingestion forbids. Two residuals named in the source rather than left to be rediscovered: HANGUL FILLER U+3164 is category `Lo` (a letter — no property-derived class reaches it without reaching every real letter) and U+2028 is `Zl` with `White_Space`, so both still fork. The transferable rule: when an issue names a category, the review question is never "did I strip that category" but "what is the PROPERTY the category was standing in for" — and the permanent lens is what asks it, which is exactly why PROJECT.md makes it non-optional
- 2026-08-02 · bug · closed · (#62, PR #81) literal control characters DO NOT SURVIVE being written through the agent toolchain, and they fail SILENTLY as the wrong thing rather than as an error. An inline test fixture `[NUL, ESC, DEL, C1, VS1]` arrived on disk as `[" ", "", "", "", VS1]` — U+0000/U+001B/U+007F/U+0090 became PLAIN SPACES — so the loop asserted something about spaces while reading, in full sentences, as though it covered control characters. It was caught only because the space-bearing case then failed for an unrelated reason; had it passed, a green test would have certified coverage that did not exist. Compounding it, the constants that DID write correctly made `test/name-key.test.ts` itself binary to grep (the `upsert.ts` trap, now self-inflicted) while a findings line in the same commit claimed test files avoided exactly that. Both closed by writing every invisible character as a `\uXXXX` escape with a named const: an escape is ordinary ASCII, so it cannot be mangled in transit and cannot flip the file's classification. The rule: a test whose SUBJECT is an invisible or non-printing character must never contain that character literally — the fixture and the defect have the same failure mode, so a corrupted fixture is invisible in exactly the way the defect is
- 2026-08-02 · bug · closed · (#62, PR #81) the `v` regex flag's set-difference syntax (`[[\p{Cc}\p{Cf}]--\p{White_Space}]`) reads far better than the negative-lookahead form and DOES NOT COMPILE here: it requires `target: es2024` and this project targets ES2023, so `tsc --noEmit` errors while VITEST PASSES — esbuild's transform runs the flag happily without typechecking it. Green suite, red typecheck, and the suite is the check most likely to be run first and trusted. Rewritten as `(?!\p{White_Space})[\p{Cc}\p{Cf}\p{Variation_Selector}]`, verified character-for-character identical to the `v` form across all 1.1M code points before switching rather than assumed equivalent. Two rules: the test runner and the typechecker do not agree about what the language IS, so a syntax-level choice is not validated by a green suite; and when replacing one regex with a supposedly-equivalent one, the cheap proof is to run both over the whole input domain, which for a Unicode property class is a few seconds of compute
- 2026-08-02 · process · closed · (#62, PR #81) the HC stop that produced the widened guard was worth its cost, and the shape is reusable: the finding was real but its DISPOSITION was genuinely contested — PROJECT.md § Findings-Log Discipline sanctions a tracked Issue when a defect's fix is "not in hand", while the standing rule this repo runs on says a defect found during a run is fixed in that run's PR and never converted into an issue. Rather than resolve a rule conflict unilaterally mid-PR, the run posted a durable stop and asked, offering a fourth option nobody had named — fold only the subset that renders as NOTHING and leave the whitespace-semantics question alone — which is what was chosen. Two things made the pause cheap rather than expensive: it happened BEFORE the Reviewer summons (under `attested` any post-summons head move voids the SHA-bound review, so folding after summoning would have cost two rounds instead of one), and the question was posed as concrete alternatives with their consequences rather than as "what should I do?"
- 2026-08-02 · bug · closed · (#62, PR #81, Codex adversarial review round 1, rated high — the permanent lens's OWN prediction coming true one round later) `\p{Cc}+\p{Cf}+\p{Variation_Selector}` still missed U+034F COMBINING GRAPHEME JOINER (category `Mn`, NFKC-stable) and the Hangul fillers U+115F/U+1160/U+3164 (category `Lo`) — three of them push the key length past the ±FUZZY_MAX_DISTANCE band, so the silent-duplicate branch was reachable again, reproduced as 2 player rows before the fix was accepted. Closed with `\p{Default_Ignorable_Code_Point}`, which is the property that ACTUALLY means "render this as nothing" and which the three general categories had only been approximating. The galling part is recorded because it is the reusable lesson: the previous revision had NAMED U+3164 as an accepted residual, with an explicit argument that no derived class could reach a category-`Lo` character without also reaching every real letter — and that argument was simply FALSE, since Default_Ignorable does exactly that. "No property reaches this case" is never a fact about Unicode, it is a fact about the properties you happened to think of, and writing it down as an accepted limitation makes an unchecked assumption look like a decision. Widening was paired with its own refutation (combining accents, letters in five scripts, CJK, emoji all asserted to SURVIVE) because widening a strip class is the move most likely to cause a silent over-merge
- 2026-08-02 · process · closed · (#62, PR #81) three of this run's most valuable findings came from ADVERSARIAL PROBES RUN AGAINST A LIVE DB, not from reading code, and the pattern is worth naming: write a ~30-line throwaway script that injects the hostile input class across every adjacent variant and prints the row count after each. Reading `identity.ts` carefully for an hour did not surface that 1-2 invisible characters behave differently from 3; one probe did, in seconds, and it changed the problem statement in the tracked issue. Same method confirmed the reviewer's two findings before either was accepted, and refuted the framing of one of them. The corollary is a discipline, not just a tool: NEVER accept an external reviewer's trace on faith and never reject one on faith either — re-run it. Codex's U+034F trace reproduced exactly; its second finding reproduced as BEHAVIOR but rested on a misreading of the spec clause it cited, and only running both told the two apart
- 2026-08-02 · bug · closed · (#62, PR #81, Codex FIX-VERIFICATION pass 1, rated critical — a defect introduced BY the previous round's fix, which is the class PROJECT.md bounds separately for exactly this reason) adding `\p{Default_Ignorable_Code_Point}` to close U+034F also swept in the Hangul fillers U+115F/U+1160/U+3164/U+FFA0, and that introduced a silent OVER-merge — the direction that actually matters. A filler is invisible ON ITS OWN but is a placeholder completing a syllable block, so deleting it re-groups the surrounding jamo: `<L,Vf><L,V>` and `<L><L,V>` both folded to `U+1100 U+B098`, reproduced exactly as traced, so tier 2 silently merged two names a Korean renderer draws differently. Fixed by subtracting `\p{Script=Hangul}` — DERIVED, and verified to exempt exactly those four code points and nothing else — rather than hand-listing them, which is the same discipline the class itself follows. The settling insight, worth more than the fix: **"invisible" is not a property of a CHARACTER, it is a property of a character IN CONTEXT.** That is why the boundary moved three times in one change (Cf too narrow → +Cc/VS still too narrow → +Default_Ignorable too wide → −Hangul), and why every exemption that survives is a character that is invisible alone and load-bearing beside something else. U+3164 is now a NAMED residual for the second time in this PR, with a correct reason replacing the wrong one
- 2026-08-02 · process · closed · (#62, PR #81) asking the reviewer to ATTACK ITS OWN PREVIOUS RECOMMENDATION is what found the critical over-merge, and the prompt shape is reusable. The fix-verification summons named four numbered questions and marked one of them as the priority — 'does the WIDER class now strip anything a reader CAN see, i.e. did the fix introduce a silent OVER-merge? If you find one, that is Critical' — and that is the question that paid; the other three returned clean. A generic 're-review the fix' prompt would very likely have confirmed the fix worked, because it DID work for the case it was written for. Second reusable half: the same summons asked the reviewer to adjudicate my DECLINE of its own earlier finding, in the reviewer's words rather than mine, and it agreed the decline was defensible — which closed that thread on the record instead of leaving a disputed High finding hanging over the merge gate
- 2026-08-02 · bug · closed · (#62, PR #81, the ESCALATION resolution — HC-directed after the two-pass fix-verification cap was reached) the strip class was revised FOUR times in one change and each revision was shown wrong by the next review pass: `\p{Cf}` too narrow, +`\p{Cc}`/`\p{Variation_Selector}` still too narrow (U+034F), +`\p{Default_Ignorable}` too WIDE (Hangul filler over-merge, critical), −`\p{Script=Hangul}` too narrow again (Mongolian MVS/FVS over-merge, critical). Five scripts had characters in the class. What was wrong was the PREMISE, not the list: there is no Unicode property equal to "safe to delete from a name", because INVISIBILITY IS CONTEXTUAL — every widening eventually reaches a script whose invisible characters are load-bearing. Closed by asking a different question instead of enumerating a better answer: strip an invisible character only if it belongs to NO COMPLEX SCRIPT (`Script=Common|Inherited|Latin`), which exempts Mongolian, Hangul, Khmer, Kaithi and Egyptian under one clause AND the next script nobody has thought of. The reusable rule: when a boundary moves three or more times and every move is a real defect, stop moving it and change the QUESTION — the oscillation is evidence that the predicate is wrong, not that the list is incomplete
- 2026-08-02 · bug · closed · (#62, PR #81) applying the script scope PER NAME ("if the name touches a complex script, strip nothing") reads simpler than per character and hands an attacker a one-character bypass: append an invisible Mongolian vowel separator to a Latin name and the RIGHT-TO-LEFT OVERRIDE in the rest of it survives untouched, reopening the tracked defect exactly. Per character — strip an invisible only when ITS OWN script is Common/Inherited/Latin — closes the same over-merge class with no bypass, since the override is Common and the Mongolian character is not. Caught by writing the two forms side by side and running them over the same nine cases BEFORE implementing either, which cost about two minutes; the per-name form was the one the option had been framed around and it would have shipped unexamined. The rule: when a design is described as "scope it by X", the granularity of X is a second decision hiding inside the first, and the coarse reading is usually the one that got written down
- 2026-08-02 · bug · open · (#62, PR #81, Codex review round 2 on the HC-directed redesign) `U+06DD ARABIC END OF AYAH` is `Cf` AND `Script=Common`, so the complex-script scope does not exempt it and it is stripped — `namesEqual("علي"+U+06DD+"١", "علي"+"١")` is TRUE, a silent over-merge, since the sign renders as an enclosure around the following digits. `U+08E2` behaves identically. `Script_Extensions` is NOT a fix (checked before proposing it: `scx=Common` is also true). The reframing that matters and that took two wrong measurements to reach: this is NOT a regression from widening — U+06DD is `\p{Cf}`, so THE ORIGINAL ONE-LINE #62 FIX HAS IT TOO, and withdrawing the widening would not remove it. Left OPEN because it is the fifth consecutive review round finding the same shape and the AC stopped rather than patching a sixth time; HC to decide between accepting the documented residuals and changing the mechanism to fail-closed refusal
- 2026-08-02 · process · closed · (#62, PR #81) THE VERIFICATION OF THE FIX WAS BITTEN BY THE BUG THE FIX IS ABOUT — three separate times in one run, and the third time it inverted a review outcome. Literal invisible characters written into probe scripts were silently transformed in transit (control characters became SPACES; U+06DD's row came back with `Script=Common: false` and `merges: false`), so a probe built to check a reviewer's finding reported it as a FALSE POSITIVE. Re-running the identical probe with `\uXXXX` escapes reversed both answers: the finding was real. The general rule is sharper than "use escapes": WHEN A TEST'S SUBJECT IS AN INVISIBLE OR NON-PRINTING CHARACTER, THE VERIFICATION APPARATUS HAS THE SAME FAILURE MODE AS THE THING UNDER TEST, so a clean result from it is worth much less than usual. Two habits fall out — never write the subject literally, and when a probe DISAGREES with an external reviewer's concrete trace, re-run the probe before trusting it, because the asymmetry is that a reviewer running its own commands in its own sandbox is not subject to your transport bug
- 2026-08-02 · process · closed · (#62, PR #81) five consecutive adversarial rounds each found a REAL over- or under-merge in the predicate the previous round had just fixed, alternating direction: too narrow, too narrow, too wide, too narrow, too wide. The oscillation itself is the diagnostic and it was legible by round three — when a boundary keeps moving and every move is justified by a genuine defect, the predicate is undecidable rather than merely unfinished, and further rounds buy motion instead of convergence. What was NOT visible until round five is the reframing that makes the whole thing tractable: the last defect is present in the SIMPLEST version of the fix too, so it was never a cost of widening, and 'withdraw the widening' — the instinctive retreat — would not have bought it back. Before running another round on a moving boundary, check whether the newest defect also exists in the version you would retreat to; if it does, the choice is accept-with-reachability-argument or change the mechanism, and never another predicate
- 2026-08-02 · bug · closed · (#62, PR #81, Codex FINAL merge-gate review, rated high) the accepted-residual block asserted that every retained SPLIT is "the safe failure, because it is loud" — and that is FALSE against this PR's own pinned test, which resolves a three-filler spelling as `created` and asserts TWO player rows. A split is loud only while the difference stays inside `FUZZY_MAX_DISTANCE`; past it the ladder creates a row silently, which is the exact threshold the whole change exists to close. So the honest asymmetry is weaker than the one claimed: a MERGE is silent ALWAYS, a SPLIT is silent only SOMETIMES. Two things worth keeping. First, this is a claim-vs-code defect in the very block written to be the honest record of what the code does NOT do — the summary of the limitations acquired its own overstatement, which is the last place anyone re-reads. Second, the reviewer found ONE instance and the class was THREE (the residual block, the design rationale, and a test comment), all seeded from one comfortable phrase copied forward; fixing only the reported line would have left the claim standing twice
- 2026-08-02 · bug · closed · (#62, PR #81, Codex FINAL merge-gate review, rated high) a `|||||||` diff3 conflict marker was COMMITTED into `docs/findings.md` and survived my own post-resolution marker check, because that check grepped for `<<<<<<<`, `=======` and `>>>>>>>` and this repo's merge driver writes diff3 style, which emits a FOURTH marker naming the merge base. `git diff --check` catches it and I had not run it after the conflict resolution — only before. Two rules: grep all FOUR marker forms (`^(<{7}|\|{7}|={7}|>{7})`), and treat `git diff --check` as mandatory AFTER resolving a conflict rather than as a pre-commit habit, since that is the one moment it has something new to say. The near miss is the point — the marker was inside an append-only log nobody reads top-to-bottom, on a PR about invisible characters corrupting records
