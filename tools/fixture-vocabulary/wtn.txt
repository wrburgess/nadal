# Skeleton vocabulary for WTN fixtures (test/fixtures/wtn/*.html).
#
# Source: the World Tennis Number player-profile SPA (issue #128). Because the page is
# client-rendered, its fixture comes from a saved post-render DOM, never from a live fetch —
# `--url` against this host returns a data-less shell.
#
# Auto-written (non-name-shaped) entries by tools/bootstrap-vocabulary.ts, followed by
# hand-dispositioned name-shaped entries in three groups, each with its own reason:
# CSS-module class atoms, static interface copy, and site navigation / organisation names.
#
# The page's ONLY personal identity is its subject, replaced through the out-of-repo
# substitution map; a sweep of the capture found no third-party names (the match tables are
# not rendered in the captured state), so no opponent identities enter this vocabulary.

16x16
180x180
183cm
32x32
7p46zwvpza
Back
Backhand
BodyBottom
BodyTop
Both
Discover
Double
Doubles
Education
Features
General
HeadBottom
Height
Losses
Male
Menu
Modenizr
News
Notice
Overview
Residence
Singles
Smash
Toastify
Top
USA
Unknown
Update
WTNScale
Wins
Women
_blank
_hjSafeContext
_hjSafeContext_59023282
anonymous
avatar
canonical
close
col
container
copyright
days
description
dialog
documentation
en
evenodd
false
favicon 16x16 png
favicon 32x32 png
h
height
icon
infoIcon
languages
ltr
modulepreload
or
otherDate
overview
position
preconnect
results
round
script
someDate
someFilter
stylesheet
testid
true
url 7p46zwvpza
utf
viewport

# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
WTNInfoIcon module_WTNInfoIcon__1dzZp
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
WTNSVGs module_WTNLogoWrapper__1IhAg
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
WTNScale module_WTNScaleBtn__2kJ
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
WTNScale module_WTNScaleBtn__2kJ WTNScale module_WTNScaleActiveBtn__1OBnG
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
WTNScale module_WTNScaleDivider__1gvtN
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
WTNScale module_WTNScaleWrapperBtns__14JsU
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
WTNScale module_WTNScaleWrapperContainer__2RQZD
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
WTNScale module_WTNScaleWrapper__3YbUg
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
accountToggle module__menuToggle___detwj accountToggle module__mobileMenuToggle___pu r
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
avatar module__avatarImgWrapper___53ezf avatar module__lg___CsvDM profile header module__avatar___TLow9
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
avatar module__avatar___sr4az
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
blurredWtnHistoryChart module__blurredWtnHistory___x6Xhb
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
blurredWtnHistoryChart module__filterBar___YhJsd
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
blurredWtnHistoryChart module__filter___rYnfR blurredWtnHistoryChart module__commonSelectInput___1LwUM
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
blurredWtnHistoryChart module__line___uIhWR
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
blurredWtnHistoryChart module__linesContainer___JoE
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
confidenceTick module__confidenceTick___iHHGz statsNote module__tick___sNolZ confidenceTick module__high___ Mx
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
dropdown module__dropdown___Ud o dropdown module__mb32___U yT dropdown_dropdown override dropdown
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
generic components module__customDropdown___x4gmU generic components module__labelPadding___YtQE6 generic components_customDropdown override
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
generic components module__dropdownLabel___8GMrT generic components_dropdownLabel override
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
generic components module__flag___e xb1
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
info box container module__infoBoxContainer___RvgPQ profilePanel module__container___iY4JZ
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
info box container module__infoBoxContainer___RvgPQ profilePanel module__container___iY4JZ player level module__container___LElrv
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
info box container module__infoBoxContainer___RvgPQ profilePanel module__container___iY4JZ profile header module__panel___4IIQg
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
info boxes wrapper module__infoBoxesWrapper___ lD
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
lockedFeature module__blurredContainer___Ge0k6
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
lockedFeature module__blurred___m ff
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
lockedFeature module__content___VgtpR
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
lockedFeature module__info___Q c
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
main container module__mainContainer___Gnq
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__arrowContainer___oBhIz
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__arrowIcon___ ed
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__authButtons___d8lQz
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__authDivider___xWKIp
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__btn___CFYRg mainNavBar module__btnPrimary___b kR
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__btn___CFYRg mainNavBar module__signInLinkDesktop___lu3Qz
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__burger___4mecq
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__container___ir e
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__dropdown___FRyDz
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__headerGroupWrapper___MYlru
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__logo___I2VHi
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__mainNavInner___G sKY
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__mainNav___40Nkz
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__mobileToggleContainer___E4 o mainNavBar module__hideOnDesktop___yHKFN
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__navItem___r O1 mainNavBar module__hideOnMobile___nbWzL
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__navItem___r O1 mainNavBar module__hideOnMobile___nbWzL mainNavBar module__hasDropdown___GSUlI
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__navItem___r O1 mainNavBar module__mainNavControls___L RU mainNavBar module__hideOnMobile___nbWzL
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__navLinkText___LGOP9
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__navLink___Ux7GX
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__navPrimaryContainer___tyE3o
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mainNavBar module__navPrimary___2me2f
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__backButton___6XsGg
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__mobileAuthLink___IjMif
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__mobileAuthLinks___0Zb4F
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__mobileAuthSeparator___fe9OO
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__mobileItemArrow___qHzeq
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__mobileMenuCloseButton___KZmYn
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__mobileMenuContainer___0XFKa
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__mobileMenuHeaderControls___QfVTd
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__mobileMenuHeaderTitle___3oF1I
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__mobileMenuNav___Mcq
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__mobileMenuOverlay___6pcpB
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__mobileMenuTitle___qHflX
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__mobileMenuWrapper___snzDk
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__mobileSubmenuContainer___pFYbl
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__navItem___V lAG
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__navItem___V lAG mobileNavigation module__separator___ekQBV
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__navLink___D8nEB
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__navLink___D8nEB undefined
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__navText___LX9
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__submenuHeaderControls___ eA
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__submenuHeaderTitle___NKOVM
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__submenuList___n d7
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
mobileNavigation module__submenuTitle___ToMFH
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
notificationRibbon module__closeButton___oJ9
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
notificationRibbon module__closeIcon___B6kXL
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
notificationRibbon module__content___AX LY
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
notificationRibbon module__ribbon___Ap4TG
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
playerLevelTable module__data___Sfzic
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
playerLevelTable module__label___I Wv
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
playerLevelTable module__row___Uwg
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
playerLevelTable module__table___NKckK
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profile header module__playerDetailsHeader___p1yB3
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profile header module__playerStatsPadding___X3N39
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profile header module__profileHeaderLeft___VAZ
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profile header module__profileHeaderTop___DJ 6l
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profile header module__profileHeaderWrapper___iadbY
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profile header module__tabContentTitle___RANSk
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profile header module__tabContentWrapper___eXZHB
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profile header module__textWhite___QQsdG
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profile styles module__heading___2pwwT
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profile styles module__highsLowsGraphsWrapper___7DDb5
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profilePanel module__content___jx2
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profilePanel module__content___jx2 player level module__content___2hK O
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profilePanel module__content___jx2 profile header module__panelContentWithSubtitle___aQte4
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profilePanel module__dataListItemLabel___PzmPQ
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profilePanel module__dataListItemValue___RN7hY
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profilePanel module__dataListItem___GDgMo
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profilePanel module__dataList___RSxKm
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
profilePanel module__title___mnN5P
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
ratings range graphic module__highLowGraph___DaDAZ
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
ratings range graphic module__highLowLine___RZmtt
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
ratings range graphic module__highLowRangeGradient___Bex G
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
ratings range graphic module__label___58FSZ
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
ratings range graphic module__wrapper___Wd7KY
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
results doughnut chart module__chartKeyStat___VNi X
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
results doughnut chart module__doughnutChartKeyGroup___ch F results doughnut chart_doughnutChartKeyGroup override
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
results doughnut chart module__resultsDoughnutChartKeys___YqSkS results doughnut chart_resultsDoughnutChartKeys override
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
results summary section module__style2___dPCqB results summary section_style2 override
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
statsNote module__btnModal___EfhTV
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
statsNote module__note___Osg0B
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
tabNavigation module__outerContainer___SdEZm
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
tabNavigation module__tabsContainer___g5w9A tabNavigation module__primary___sAe17 tabNavigation module__squared___IXARg profile header module__tabNavContainer___hwmGm tabNavigation module__overheadTabs___x2o
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
tournament chart module__renderedChartContainer___o50IH
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
undefined results doughnut chart module__losses___WFGnl
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
undefined results doughnut chart module__wins___d8vnn
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
withInfoModal module__container___ PY playerLevelTooltip module__header___ lD64
# reviewed: a CSS-module class-name atom from the WTN SPA's own stylesheet build (`<component> module__<localName>___<buildHash>`). It reads as consecutive capitalised words only because the bundler camel-cases its local names; the value is generated by the build, is identical for every player's page, and carries no personal data. The trailing hash changes on each WTN deploy.
withInfoModal module__modalTrigger___JUXMZ playerLevelTooltip module__trigger___mYcR8
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Age unknown
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
All time
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Average doubles interval
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Average singles interval
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Content css rebrand comp core css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Content css rebrand module overrides css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Content css rebrand react overrides css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Content img Logos WT logo v white png
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Days since last match
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Discover submenu
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
End VWO Async SmartCode
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
End of favicon
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
FAQ s
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Favourite shot
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Global site tag gtag js Google Analytics
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Hotjar Tracking Code for https www worldtennisnumber com
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Join World Tennis Number WTN
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Log in
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Main navigation
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Match activity
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Mobile navigation
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
More info
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
News submenu
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Notice WTN algorithm enhancements will be introduced on Wednesday 12th August Some players may see updates to their rating
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Player details
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Player level
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Player profile World Tennis Number Powered by ITF
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Player tournament matches
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Preferred hand
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Privacy and Cookie policies
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Rating range
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Results summary
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Right handed
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
See https support termly io hc en us articles to learn how to reveal and customize this floating cookie preferences button
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Sign in
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Sign up
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Singles and Doubles win loss record
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Skip link
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Skip to content
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Skip to footer
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Start VWO Async SmartCode
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Start of Favicon
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Termly Cookie Consent Notice end for worldtennisnumber com
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Termly Cookie Consent Notice start for worldtennisnumber com
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
The ITF World Tennis Number WTN is a global tennis rating scale from Track and improve your game by playing against evenly matched opponents
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
These statistics only include players with a
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
To view history you will need to be logged in
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
To view player levels you will need to be logged in
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
Unique singles opponents
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
WTN algorithm enhancements will be introduced on Wednesday 12th August Some players may see updates to their rating
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
We believe you are visiting our site from United Kingdom Select a different country to view the website content in your home language
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
World Tennis Number Powered by ITF
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
World Tennis Number history
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
World Tennis on Facebook
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
World Tennis on Instagram
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
World Tennis on X
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
World Tennis on YouTube
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
about blank
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
account signin returnUrl https worldtennisnumber com eng player profile tennis id
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
account toggle mobile
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
apple touch icon
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
apple touch icon png
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
back to top
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
banner text
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
code eng name English flagUrl Content img Flags en png
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
code eng name English flagUrl Content img Flags en png url eng player profile isCurrent false
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
col float right
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
confidence leve
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
copyright container
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
current language
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
desktop navigation
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
display block box sizing border box height 300px width 300px
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
display block box sizing border box height 400px width 519px
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
display none important width 1px important height 1px important opacity important pointer events none important
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
dropdown toggle
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
enable self submission
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
eng coaches
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
eng contact
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
eng discover
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
eng faq
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
eng features
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
eng how wtn works
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
eng news
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
eng our partners
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
eng player search
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
eng player types
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
eng privacy policy
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
eng terms and conditions
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
fade tab pane
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
fade tab pane active show
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
floating preferences button
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
focus id
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
font size 6px
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
footer bottom
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
footer nav
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
footer style
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
footer top
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
footer top__content container
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
for an account
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
gap 35px padding inline 17px
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
has fixed navbar navbar visible
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
head to head
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
head to head tab nav link
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
header total height 90px
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
hide match results
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https cdnjs cloudflare com ajax libs izimodal css iziModal min css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https cdnjs cloudflare com ajax libs jquery modal jquery modal css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https cdnjs cloudflare com ajax libs simplebar simplebar min css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https dev visualwebsiteoptimizer com
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https fonts googleapis com css family Poppins display swap
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https maxcdn bootstrapcdn com bootstrap css bootstrap min css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https prd itf web clubspark pro Content Public Clubspark itf global img flags US png
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https prd0itf0cspark blob core windows net itf User Photos png
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https world tennis
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https worldtennisnumber com eng news
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https worldtennisnumber com eng news category Case Studies
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https worldtennisnumber com eng news category Education
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https worldtennisnumber com eng news category General
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https worldtennisnumber com eng player profile tennis id
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https worldtennisnumber com eng register
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https www facebook com WorldTennisOfficial
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https www instagram com worldtennisnumber
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https www youtube com worldtennisofficial
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
https x com worldtennis
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
icon social facebook icon
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
icon social instagram icon
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
icon social twitter icon bi bi twitter x
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
icon social youtube icon
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
id 31daaef5be234d66818e473139 dffa name Player Search path eng player search hasDropdown false dropdownItems id e31d3282b b4dc b5ebff37d21 name Discover path eng discover hasDropdown true dropdownItems id 49f8bd82025 e59938b5877962bcd37 name Discover WTN path eng discover icon null id 81d8 dec fb5afc d8 c name How WTN Works path eng how wtn works icon null id cad951ad25284e799def7df63b36ce2a name For Players path eng player types icon null id 7e6f977fe73d47b9bf75372091f7e name For Coaches path eng coaches icon null id bd5acbeb387ce49 name Features path eng features hasDropdown false dropdownItems id 160f551 db db f848fd name News path eng news hasDropdown true dropdownItems
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
id daaef5be234d66818e473139 dffa name Player Search path eng player search hasDropdown false dropdownItems hasArrow false id m e31d3282b b4dc b5ebff37d21 name Discover path eng discover hasDropdown true dropdownItems id 49f8bd82025 e59938b5877962bcd37 name Discover WTN path eng discover id 81d8 dec fb5afc d8 c name How WTN Works path eng how wtn works id cad951ad25284e799def7df63b36ce2a name For Players path eng player types id 7e6f977fe73d47b9bf75372091f7e name For Coaches path eng coaches hasArrow true id bd5acbeb387ce49 name Features path eng features hasDropdown false dropdownItems hasArrow false id f551 db db f848fd name News path eng news hasDropdown true dropdownItems hasArrow true
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
id news all name All News path https worldtennisnumber com eng news id news cat Case Studies name Case Studies path https worldtennisnumber com eng news category Case Studies id news cat Education name Education path https worldtennisnumber com eng news category Education id news cat General name General path https worldtennisnumber com eng news category General
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
ie edge
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
js flexbox flexboxlegacy canvas canvastext webgl no touch geolocation postmessage no websqldatabase indexeddb hashchange history draganddrop websockets rgba hsla multiplebgs backgroundsize borderimage borderradius boxshadow textshadow opacity cssanimations csscolumns cssgradients cssreflections csstransforms csstransform d csstransitions fontface generatedcontent video audio localstorage sessionstorage webworkers no applicationcache svg inlinesvg smil svgclippaths
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
language message
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
main nav header
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
margin left 20px
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
mobile menu items
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
mobile menu main
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
mobile navigation
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
mobile submenu
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
modal wtn portal
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
nav item
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
nav items
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
nav nav tabs
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
news dropdown items
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
no referrer
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
noopener noreferrer
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
noreferrer noopener
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
notification ribbon
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
overview tab nav link active
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
page content
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
page footer
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
player profile
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
post a score desktop portal target
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
post a score mobile portal target
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
profile tabs
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
profile tabs tab h
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
profile tabs tab overview
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
profile tabs tab results
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
profile tabs tabpane h
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
profile tabs tabpane overview
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
profile tabs tabpane results
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site10 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site12 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site14 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site15 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site19 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site2 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site22 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site23 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site24 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site25 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site27 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site29 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site3 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site30 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site31 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site32 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site34 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site35 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site36 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site39 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site4 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site40 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site41 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site42 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site5 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site6 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site7 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site8 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static css main wtn site9 css
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js Anchor wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js Avatar wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js BtnSecondary wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js BtnToggle wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js Button wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js ChartsGlobalConfiguration wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js ConfidenceTick wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js CountryFlag wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js DataKey wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js Fade wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js FormInputGroup wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js HModal wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js HistoryLineChart wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js LanguageHeader wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js LoadMore wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js MainNavBar wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js MatchResultListings wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js MobileFilterSelector wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js ModalWTN wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js Nav wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js NoDataContainer wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js NoopTransition wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js Overlay wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js PlayerFilterBar wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js ProfileRouter wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js RadioFieldWTN wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js RatingWithConfidenceTick wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js ResultsSummarySection wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js SearchBar wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js SearchResultsCounter wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js SearchResultsTableDesktop wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js SelectWTN wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js ThemeProvider wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js TournamentChart wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js Transition wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js TransitionWrapper wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js User wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js VsPlayerSearchBar wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js WatchlistBadge wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js emotion element f0de968e browser esm wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js extends wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js formik esm wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js forms module wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js hasClass wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js hook wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js icn_arrow_down wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js icn_close wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js icn_confidence_tick wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js icn_delete_copy wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js index es wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js index wtn site e js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js index wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js links wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js modalWTN module wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js profile styles module wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js react lifecycles compat es wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js react select esm wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js results actions wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js search actions wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js selectors wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js social matches actions wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js use click outside wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js use countries wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js use date filter options wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js use debounce wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js use fetch wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js use focus trap wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js use keyboard navigation wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js use modal wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js use my club wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js use paginated search wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js use player search with sorting wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js use watchlists wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js use windowSize wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js useEventCallback wtn site e js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js useEventCallback wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js useIsProfileStrengthHidden wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js useIsomorphicEffect wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js useMergedRefs wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js warning wtn site e js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js warning wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js watchlist actions wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js watchlist modal module wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static js withOnlyAllowLoggedIn wtn site es js
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static media missing wtn site png
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
react components esm static media world tennis number logo wtn site svg
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
render script
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
results tab nav link
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
rr ui event key
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
sh JP49dvydjvd qd31grbdqIeExUyLFFIIneoetY cJ eQeJ6ok5Hha kQfIeQ maAMG kf4In3 VKwMufg
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
sh dIhy DDDYfWvU sLgcz ZunLzmYXbdDZoGhBfeHx lUr5dL ixGDaxZDGUNn B O8 klJ2Nj1aYX pNMTg
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
sh ggOyR0iXCbMQ Xipm MD dH 1f j6cY iJTQUOhcWr7x9JvoRx MZw1T
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
sh vr9VoQNQkkCCHGX4BSjg63nI5CI4B n SF2xy4FMOIyH 2M r55 ypsBFAgmLIGXKtRhbbJueVyYZXjA
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
show language header
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
skip links
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
social container
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
social nav
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
stroke miterlimit
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
tab content
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
termly code snippet support
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
termly styles root b60 d termly floating preferences
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
termly theme background color termly theme button background color termly theme button text color termly theme color termly theme font family Roboto Open Sans Arial Helvetica termly theme font size 12px termly z index var termly override z index
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
top 0px transition top 3s ease out
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
unknown country
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
update text
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
user controller
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
verified Singles WTN
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
visibility hidden
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
width 476px height 476px background color rgb
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
width 8px height 2px
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
width device width
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
width left
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
wtn definition wrapper
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
x ua compatible
# reviewed: static interface copy or a technical/layout token rendered identically on every player's profile — column headings, stat labels, control text, style and attribute values. Independent of which player the page is for, so it carries no personal data.
78 skeleton(s) auto-written, 440 pending manual review (518 total).
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
Account Menu
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
All News
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
All Time
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
Case Studies
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
Contact Us
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
Cookie Preferences
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
Discover WTN
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
For Coaches
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
For Players
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
How WTN Works
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
International Tennis Federation
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
Our Partners
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
Player Search
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
Terms Conditions
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
World Tennis
# reviewed: site navigation, branding, or the name of a public organisation (the International Tennis Federation, which publishes the World Tennis Number). Chrome present on every page of the site regardless of subject; not a person.
World Tennis Logo
